# PokerGame 项目说明（产品名：德扑道场 Poker Dojo）

## 项目目标
开发一款支持好友在线开房同玩的德州扑克游戏。**产品名：德扑道场（Poker Dojo）** —— 好友开房、切磋成长。

## 目标平台
Android / iOS / PC

## ⭐ 数据资产与可迁移性（最重要原则）
**数据是这个项目最宝贵的资产，比服务器本身更重要。** 底层代码可随时重新部署，但用户数据与对局数据必须能跟着走。

- **服务器是临时的**：**2026-09-08 已从阿里云香港迁到 AWS 新加坡**（t3.micro，弹性 IP 18.140.12.176；细节见私有 `OPS.local.md`）。这次迁移验证了「代码不变、数据搬走」这套做法可行——**APK 指的是域名，已装客户端零改动自动生效**。将来还会再迁，原则不变：**代码不变，但所有数据必须一起搬过去**。
- **当前数据存储**：服务器内存 + `PokerServer/data.json`（用户库：账号/金币/头像/isAdmin）。该文件**被 .gitignore 排除、deploy 脚本也不覆盖它**（tar 排除 data.json），所以发版不会丢数据——但也**不会自动备份**。
- **迁移/备份必做**：换服务器时，从旧机 `scp` 下 `data.json`（及将来的牌谱库）到新机，否则用户全丢。建议定期备份 data.json。
- **牌谱（hand history）是核心数据资产**（后期务必实现，见下）：
  - 完整记录每一手：各街动作、**思考时间**、底牌、公共牌、下注额、底池、结果、对手、**时间戳**
  - **按玩家分别归档**：每个真人玩家的历史行为是核心资产——个人统计与牌谱回顾都依赖它
  - **再按游戏模式细分归档**：同一玩家在 **SNG 锦标赛升盲** 与 **现金桌** 下的策略不同（短码/ICM vs 深筹码），归档要按 `(玩家 × 模式)` 切分，别混在一起
  - **保留时间先后顺序**：人会学习、策略会随时间演变，所以每手的**先后顺序/时间戳是有效信号**，不是噪声——分析时要保留时序，可建模"某人打法的演变"，也意味着旧数据权重可能要衰减
  - 同一份数据同时驱动：**个人数据统计**（VPIP/PFR/3-bet…）与**牌谱回顾**
- **演进方向**：data.json 适合当前小规模；用户/牌谱量大后迁移到 SQLite/PostgreSQL（database.js 对外接口不变，便于替换）。牌谱建议从一开始就结构化落库，别只存内存。

## 架构概览

### 服务端权威架构
所有游戏逻辑（发牌、判牌、押注结算）均在服务器执行，客户端只负责展示和传送玩家操作。

### 目录结构
- `PokerLogic/` — C# 核心算法原型（Cactus Kev 牌力评估）
- `PokerServer/` — Node.js 游戏服务器（Express + Socket.IO）

### 牌力评估算法
使用 **Cactus Kev + Paul Senzee 完美哈希**方案：
- 每张牌编码为一个 32 位整数
- 5 张牌评估走 Flushes / Unique5 / PerfectHash 三条路径
- 7 张牌枚举全部 21 种 C(7,5) 组合取最低分
- 分数范围 1（最强同花顺）～ 7462（最弱高牌），**分数越小牌越强**
- JS 版需用 `>>> 0` 保持无符号 32 位运算

### 安全（防作弊 / 隐私）
- **底牌不外泄**：`hole_cards`/`my_hand` 用 `io.to(socketId)` **私发**；广播的 `game_state` 只含公共信息（公共牌/筹码/下注/弃牌），**无任何底牌字段**；`showdown_reveal` 只揭示未弃牌者。→ 玩家爬自己的流量看不到对手底牌（服务器权威架构的核心优势）
- **行动校验**：`player_action` 校验"是否轮到你"+ 下注额服务端 clamp，改客户端无法越权/非法下注
- **JWT 私章已加固**：`JWT_SECRET` 不再用写死默认值（公开仓库可见=可伪造令牌）；改为 env > 本地 `secret.key`（首启自动生成 96 位强随机、落盘复用；**不进 git、deploy 不覆盖**，每台服务器唯一）。换私章后旧登录失效需重登一次（账号金币不丢）
- ~~**待办：TLS/HTTPS**~~ **已完成**（2026-07-10 域名 + Caddy + Let's Encrypt，见下文）。②串通 collusion 技术无解（人因，忽略）
  - ⚠️ **生产(香港)需重新 deploy.sh 才会生效 JWT 加固**（部署后会自动生成 secret.key，全体重登一次）

### 发牌随机性（重要：朋友局公平性底线）
- **每手牌都重新 `reset()`+`shuffle()`**（server.js startHand）：Fisher-Yates 无偏洗牌
- **随机源已升级为 `crypto`（CSPRNG）**：`PokerLogic.js` shuffle 用 `crypto.randomInt(n+1)`（无模偏均匀整数），不可预测（看遍历史也推不出后续），替代了原 `Math.random()`（统计公平但理论可预测）
- 每手生成 `deck.lastShuffleId`（crypto 随机 4 字节）并 `console.log('[deal] ... shuffleId=')` 审计，确认确有重洗
- 已自检：两副不同、首张=黑桃A 频率 ≈1/52、各位置分布均匀

## 🔴 行动流程严重bug修复 + 房主功能 + 9max布局（2026-07-24，本地验证通过；测试服5090当时SSH超时未部署）
- **背景**：用户实战反馈多个"卡住牌局"的严重bug。共修 6 个行动流程bug（前 4 个见上一批 commit f985370，后 2 个本批新增），均围绕 `actionOnIdx`/行动推进。
- **上批已修(f985370)**：①弃牌者本手进行中「站起围观」立即 `vacateSeat` splice 打乱 `actionOnIdx`→后面玩家无法行动 → 改为**本手进行中一律延后离座**(`vacateAfter`，`removeBustedPlayers` 处理)；②`findNextActionIdx` 只判 `canAct` 不判"已行动且已跟平"→已跟注者过一圈又被要求过牌 → 加 `needsToAct`；③`reserve_leave` 当前行动者留座→全桌干等到超时 → 弃牌坐出并推进；④加时按钮 400ms 防抖。
- **本批新增(server.js)**：⑤**全押跑马死局**：若所有参与者因盲注/前注**全部全押**(常见于短码 heads-up 双方 all-in from blinds)，`startHand` 里 `actionOnIdx` 会算成 -1 但**没有任何东西触发跑马**→本手永久卡住。修：`startHand` 末尾若 `actionOnIdx<0` 直接 `advanceStage`(进全押亮牌+跑马)；heads-up 时若 SB 已下盲全押则顺延找下一个能行动者(避免超时误弃全押的 SB)。⑥`onActionTimeout` 兜底：`if(!canAct(player)){afterAction;return}`——**绝不把全押/已弃牌者按超时弃牌**(否则剥夺其应得池权)。
- **房主新功能**：**⏸️ 暂停发牌**(`pause_dealing`/`resume_dealing`，owner-only)——暂停后**当前这手打完**不开新局(`game.paused`：`startHand`+`scheduleNextHand` 都 gate)，「继续」立即续局；桌内菜单按钮 `#tmPause` 动态切换文案，中央水印显示「⏸️ 房主已暂停发牌」。**🧍 强制玩家站起**(`force_stand({targetUserId})`，owner-only)——把某玩家移到观战席腾座位(复用 `standUpPlayer` 核心=本手进行中延后离座)，筹码保留至结束结算，TA 可自行回座；入口在**点头像弹层**(房主对其他在座玩家显示「移到观战席」按钮)。
- **9-max布局**(`ringPos`)：M≥6 时顶部座位上提 5% 避开「房间号/盲注」中央水印(修筹码BB数与房间号重合)；「我」的左右两个座位(ringIndex 1 / M-1)上移6%+左右外扩4%，不再和自己挤在一起。
- **点头像「本局盈亏」口径统一**：原取牌谱聚合 `stats.net`(与「当前战绩」面板的 `chips-buyIn` 可能不一致：中途手/补码/房号复用时漂移)→ 改为**同源当前牌桌状态**(`roomStatFor`：在座=chips-buyIn，站起者=vacated.net，已离开=statsHistory.net)，标签改「本房战绩」；手数也改同源。
- **验证**：本地 LOCAL_DEV 起服(127.0.0.1:3000)+socket 测试：站起流程回归(弃牌者mid-hand站起不卡、正常摊牌)✅、暂停发牌(当前手打完停住/继续续局)✅、强制站起(腾座位转观众)✅；⑤全押死局因买入下限约束难自动构造，靠代码走查确认(仅 `actionOnIdx<0` 时触发，隔离安全)。client 内联脚本 vm 语法校验通过。**测试服 5090 当时 SSH 连不上(实验室网络)未部署**，待可达后 `deploy-test.sh`。

## 🎨 牌桌体感三批次（2026-07-03，已部署测试服，待验收）
- **批次1 UI 重构（参考德扑之星 9-max）**：座位改**小方块头像**+名字上·筹码(BB)下 三层固定；**本手下注=头像顶小 chip 徽章**；**状态按需气泡**(弃牌灰/坐出灰/All in 红)；对手牌在头像上方、自己牌在下方、**牌型独立深色徽章绝不盖筹码**(修掉旧的覆盖式)；**All-in=火焰橙光**、**当前行动=呼吸金框+倒计时**(两者区分)；环形半径外扩(rx42/ry41)9 人不挤；顶部**合规横幅**「绿色竞技 远离赌博…」
- **批次2 动画/触感**：下注 chip 弹入(betPop)、**赢额「+X」弹字**(winPopup)、**筹码数字滚动**(count-up easeOutCubic)、收池飞币(旧有)、**手机振动**(轮到我 45ms / 赢牌 pattern，navigator.vibrate)
- **批次3 玩法体验**：**预操作**(`#preaction-bar`：过牌/弃牌·跟注·跟任意注；不轮到我时勾选，到点自动执行；注额变化时「跟注」自动取消、「跟任意注」不取消)；**全押跑马实时胜率%**(`equity.js`：剩余牌少精确穷举/翻前蒙特卡洛 8000 次；server 在 allin_reveal + 每街 `emitEquity` 广播 `equity`；客户端头像左上绿色 % 徽章跳动；自检 AA vs 72o=86.9%、AK vs QQ turn=13.6% 正确、跑马 4 次广播 63→100%)
- 下一步 UI 待用户验收后微调。（2026-08-08：香港已与 main 同步，不再落后）

## 🌐 域名 + TLS + 邮箱账号（2026-07-10）
- **域名 pokerdojo.space**（Namecheap，.space 首年$1）；A 记录 @/www → 47.76.61.168（香港）
- **TLS/HTTPS/WSS 已上线**：香港装 **Caddy**（`/etc/caddy/Caddyfile` reverse_proxy localhost:3000），自动 Let's Encrypt 证书（90 天自动续）。访问 **https://pokerdojo.space**（小锁🔒）；客户端 `io()` 同源 → 自动走 wss，无需改代码。阿里云安全组需放行 80/443。旧 http://47.76.61.168:3000 保留过渡。换服务器只需重指 A 记录 + 同 Caddyfile。
- **邮箱账号系统（测试服，DEV 模式）**：注册改为 **用户名+邮箱+密码 → 邮件 6 位验证码 → 建号**；**忘记密码**（邮箱验证码重置）；**登录支持 用户名或邮箱**。`mailer.js`(nodemailer) 发信：优先 env(SMTP_*) > 本地 `mail.json`(gitignore/不覆盖)；**未配置则 DEV 回退**把验证码打到服务器日志。database.js 加 email 唯一/getUserByEmail/setPassword。E2E 测试：发码→验码→建号→邮箱登录→找回密码→新密码登录 全通。
  - **真发邮件已上线**：香港生产用 **163 邮箱**（IMAP/SMTP，账号+授权码见各服务器 `mail.json`，**不入库**），两台服都建了 `mail.json`(smtp.163.com:465)。老账号无邮箱仍可用户名登录。（早期测试服曾用某 QQ 邮箱，后因发件箱杂乱改用 163。具体地址/授权码只存在服务器 mail.json，公开仓库不记录。）
  - **老账号全清**：香港+测试服原有测试账号已全部删除（带时间戳备份 data.json.bak），改用邮箱重新注册。用户在 https://pokerdojo.space 重新注册后需提供用户名以 `create-admin.js` 升管理员。
  - **「更换/绑定邮箱」（测试服）**：个人主页·资料 Tab 加 `#pi-email` 区，显示当前绑定邮箱+「绑定/更换」内联表单（新邮箱→发码→验码→确认）。服务端 `GET /api/me`(当前邮箱)、`POST /api/bind-email/send-code`+`/verify`（均 requireAuth，pendingBinds 按 userId 键）。E2E：注册→改邮箱→/api/me 反映→新邮箱登录 全通。**已上香港生产**（2026-07-10）。

## 🎉 正式上线 + 签到 + 反馈 + 数据备份（2026-07-10，香港生产已上线）
- **香港正式上线**：`https://pokerdojo.space` 对外开放，管理员账号已设置。生产数据（邮箱/金币/牌谱）为**核心资产必须保全**。
- **数据自动备份**：每日 04:00 cron 在**服务器本机**留最近若干份带完整性校验的快照（现 AWS 保留 14 份）；deploy 脚本 tar 均排除数据文件（不覆盖）。换服务器须手动带走。（cron/路径细节见私有 `OPS.local.md`）
  - 🔴 **纠正一处长期与现实不符的记载（2026-09-08 查实）**：本节原先写着「异地副本 `scp` 到本地 `backups_offsite/`」，**但 `backup-cron.sh` 里从来没有任何 scp/rsync/S3 推送**——异地副本一直是**纯手动**做的（只有 2026-07-25 和 09-08 两次）。也就是说**整机丢失（实例误删/区域故障/账号问题）目前仍会丢掉最近的数据**。这是数据资产链条上现存最弱的一环，待补自动化。
- **每日签到（测试服，待验收）**：连续签到递增奖励表 `CHECKIN_REWARDS=[200,300,400,500,600,800,1000]`（第 1~7 天，7 天后封顶 1000，均值≈543/天），**断签重置**为第 1 天。日边界按香港时间 UTC+8。服务端 `GET /api/checkin/status`+`POST /api/checkin`（原子 `db.applyCheckin` 记 lastCheckin/checkinStreak+发金币）。客户端顶栏 🎁 按钮（未签到红点）+ 7 天进度网格弹窗。E2E：签到+200→重复签到 400→连签+300→断签重置+200 全通。
- **Bug/建议反馈**：顶栏 🐞 按钮 → 文本+联系方式表单 → `POST /api/feedback`(requireAuth)。落库 `feedback.jsonl`（数据资产，纳入备份/deploy 不覆盖）**且同时发一封邮件到管理员邮箱**（`mailer.sendFeedback`，发给 mail.json 的 user，可 feedbackTo 覆盖；未配置则 DEV 打日志）。管理员 `GET /api/admin/feedback`。E2E：提交/空校验/管理员鉴权/UTF-8+emoji 往返/邮件回退 全通。
- **✅ 已上香港生产**（2026-07-10 `deploy.sh`）：签到/反馈/更换邮箱三项已同步香港，端点 checkin/feedback/bind-email 齐全，data.json 未被覆盖（用户数据完好），https://pokerdojo.space 验证 200/401 正常。

## ⚠️ 访问排障备忘：沙特实验室过滤 poker 域名（勿再误判为服务器/大陆问题）
- **现象**：开发者在**沙特实验室网络**打不开 `https://pokerdojo.space`（TLS 被重置 / http 带域名 Host 被注入 503），但**手机流量、国内玩家、超级 Ping 各地、境外全部正常**。
- **真因**：不是服务器/域名/大陆 GFW 的问题——是**沙特实验室那条网络按「poker/赌博」类目过滤了这个域名**（触发点极可能是域名里的 `poker` 关键词/SNI）。服务器与 HTTPS 证书全球健康（Caddy 日志可见境外客户端正常握手反代）。
- **玩家在国内、实测可正常访问**（含超级 Ping），所以**产品无需任何改动**。
- **开发者在实验室的绕过办法**：浏览器直接用 `http://47.76.61.168:3000`（请求无域名，溜过过滤）/ 手机流量 / VPN。**不要为此把 APK 改指 IP**（见下：会破坏迁移便利+加密）。
- 教训：下次有人报「打不开」，先问**在哪个网络**（实验室？国内？手机流量？），别又按大陆 GFW/备案 绕一圈。

## 📱 Android 打包（2026-07-10，Capacitor 薄壳 + GitHub Actions 云端构建）
- **薄壳方案（重要）**：`mobile/` 是 Capacitor 薄壳，`capacitor.config.json` 的 `server.url=https://pokerdojo.space`——APK 里几乎不含游戏代码，启动直接加载线上最新版。**游戏更新照旧 `deploy.sh` 推服务器即可，无需重发 APK**；只有改原生壳（图标/名字/启动图、加原生插件、升 SDK/Capacitor）才需重新出 APK。
- **APK 必须指向域名，切勿指向 IP**：服务器是临时的、迁移会换 IP。指**域名**→迁移只改 DNS，已装 APK 自动生效、零改动（真·一劳永逸）；指 **IP**→一换服务器所有已装 APK 失效需重发，且丢失 https/wss 加密、还要开 Android 明文例外。开发者在沙特实验室的本地不便用 IP 浏览器/流量绕过即可，别固化进 APK。
- **云端出 APK**：`.github/workflows/android.yml`——push `mobile/` 或 Actions 页手动 Run workflow，ubuntu runner 里 `npm install`→`npx cap add android`→`cap sync`→`gradlew assembleDebug`，产物 `app-debug.apk` 作为 artifact `pokerdojo-debug-apk` 下载，可直接侧载安装。本机无需装 Android SDK。
- `mobile/android/`、`node_modules/` 不入库（CI 现场生成）。`appId=space.pokerdojo.app`，**`appName=Poker Dojo`**（桌面显示名）。debug APK 未签名/仅侧载；上架 Google Play 需另配签名 release + 隐私政策（待办）。
- **图标已启用**：`mobile/assets/logo.png`（1500×1500 鸟居+扑克，深绿）→ CI 用 `@capacitor/assets` 生成各密度图标/自适应/启动图（bg `#2f4a3e`）。
- **发布到 Releases**：固定标签 `android-latest`，永久直链 `https://github.com/mishiPIG/PokerGame/releases/download/android-latest/pokerdojo.apk`（public 仓库免登录可下）。已成功出包 ✅（含图标约 6MB）。换图标/改壳只需替换 logo/config 后再触发一次构建。
- 待办：App 图标/启动图、（可选）签名 release、iOS(需 $99 开发者账号 + TestFlight)。

## 🐛 实战反馈修复批（2026-07-16，测试服+香港均已上）
- **🔴 严重bug修复：断线被误判弃牌**——旧 `disconnect` 处理器只要牌局进行中就**无条件立即弃牌**，而 socket.io 网络抖动/传输切换会瞬断重连 → 表现为「突然弹回大厅再自动进来就成了弃牌」。改为**断线不再立即弃牌**，交给行动计时器兜底（`onActionTimeout`：无注自动过牌留在局里、有注才弃牌）；重连(join_room)恢复 away=false 且若轮到他则重启计时。客户端：重连**不再闪回大厅**（`everConnected` 区分首连/重连，重连直接回房 + 「重连中」提示条）。socket 自动化测试验证：掉线(非其回合)folded 保持 false、重连仍在局、整局走到摊牌 ✅。
- **牌面清晰化**：四色**默认常开**（黑桃黑·红心红·方块蓝·梅花绿，删掉只在 `body.four-color` 生效的旧规则，`.card.suit-*` 无条件着色且置于 red/black 之后覆盖之）；**放大数字/花色**（rank 0.62×w/800、suit 0.6×w 居中）——一眼分清花色与点数。
- **找回环形倒计时**：`.avatar-ring` 之前被 `display:none` 藏了（JS 仍在驱动 `--p`）→ 重画成 conic-gradient 环绕头像的进度条，剩 5 秒 `.low` 变红+脉冲、座位 `.lowtime` 红光闪烁；警告音 `sndWarn` 本就有（我的回合剩5秒响）。
- **分池双赢家高亮**：旧客户端底牌高亮只用单个 `winnerId` → 分池时第二赢家底牌被当输家变灰。服务端 showdown 改发 `bestByWinner`(每个赢家各自最强5张)；客户端 `holeOpts` 按赢家各自高亮、`winnerCommunitySet()` 取所有赢家公共牌并集高亮。
- **战绩连续（训练赛口径，已上香港）**：现金桌「退出房间」不再立即兑出——改为**站起保留座位+筹码**（复用 standing 机制），只在**本局结束/解散/全员离开**时统一 `endCashTable` 结算金币；离开后回大厅可「重新进入」(`join_room` 重连分支清 standing/reserved、有筹码回座、局间续局)接上原座位/带入/盈亏（战绩不清零）。leave_room 里全员离桌(无 active 玩家+无观众)自动 `endCashTable` 收尾防空房悬挂。修正客户端 leaveRoom/standUp 文案(不兑出、结束才结算)。E2E 验证：买入扣550→退出金币不变+座位筹码留→重进带入5000不清零+回座→解散才兑出(9288→9796)，全通。

## 🛡️ 防陌生人捣乱：列表只观战、下场需房间号（2026-07-18，测试服）
- 大厅列表所有房间仍可见，但点进=**只观战**（不显示房号）；**下场入座必须用房主私发的房间号加入**。
- 实现：`join_room({roomId, byCode})`——byCode=true(输房号) 才 `socket.playRoom=roomId`(下场资格)；列表点进(byCode 缺省)只 joinAsSpectator。`sit_down` 与 SNG 落座校验 `socket.playRoom===roomId`，否则「观战中」。房主创建即授权；站起回座/成员重进保留资格(vacatedPlayers/players 命中即授权)。SNG 从列表点进也只观战。客户端列表按钮改「👀 观战」、joinByCode 传 byCode、大厅加提示。E2E：房主可坐/观战者被拒/输房号可坐 通过。
- **观战者不见房号**：`room_joined` 下发 `canPlay`；客户端 `iCanPlay=false` 时水印/比赛设置显示「👀 观战中」隐藏房号；点坐下 toast 提示需房号加入。
- **退出不误删房间**：观众离开用 io 房间 `size` 判真空(房主未坐下不误删)；空房不立即关，`scheduleEmptyCleanup` 3 分钟宽限(可凭房号回来；join 清 timer；到点仍空才结算关)——退出≠立即解散。E2E 通过。

## 🗺️ 商业化体验路线图（2026-07-16 用户反馈，参考德扑之星）
- **现金桌到点不打断牌局**：训练时长到点若正有牌局→`onTableTimeUp` 挂起 `game.pendingEnd`(不立即结算)、`match_ending_soon` 提醒(房主自动开比赛设置可加时)，本手结束(scheduleNextHand)后若仍 pendingEnd 才 `endCashTable`；`extendTable` 加时清 pendingEnd。局间到点则直接结算。（照搬 pendingLevelUp「涨盲等本手」模式。）
- **聊天常用语顶部遮挡修复**：面板 max-height 56vh 装不下 chat-list+12条→展开常用语时隐藏消息列表腾空间；toggleChat 打开时复位。
- **站起 vs 留座 区分修复(测试服)**：之前俩功能一样。现**留座离桌**=保留座位(reserved，2分钟自动转…)，**站起围观**=`vacateSeat` 把玩家移出 `game.players` 进 `game.vacatedPlayers`(座位腾空、他人可坐、本人转观众)，**筹码保留只在结束/解散/全员离开时 `endCashTable` 统一结算**(不立即兑出)；`sit_back` 从 vacatedPlayers 带原筹码回空座；state 加 `vacatedUserIds` 供客户端显示「回到座位」。空房清理前若有 vacatedPlayers 走 endCashTable 防金币丢失。E2E：站起离座+筹码不兑→留座保座→带筹码回座→解散才结算 全通。
- **语音模块(朋友开发,已合并)**：`git merge` 零冲突并入。按住 `#voice-btn` 录音→上传→房间内 10 秒可点击气泡播放，不进聊天历史；服务端 `voice_tmp/`(1h TTL)+ `music-metadata` 校验时长(15s)+限流。deploy npm install 会装依赖。
- **菜单精简**：桌内菜单去掉「当前战绩」「牌谱回顾」(已由桌面左右边缘箭头直达)。
- **加注条手机端仍不够顺(HTML 原生 range 限制)**：已去 backdrop-filter 模糊 + rAF 节流 + 拖动时暂停座位呼吸光/环形重绘 + contain；电脑顺、手机仍卡，用户决定**留到做 App 时再彻底解决**(换自定义指针拖动条)。
- **已做(测试服)**：①**加注条手机灵敏**——`#betSlider` 改 `-webkit-appearance:none`+32px 大拇指块+`touch-action:none`（防页面滚动手势抢走拖动，即之前说的"HTML原因"），拖到最大=all-in（滑条 max=全下额，替代几乎没人用的 All-in 按钮）。②**加时选项可读+确认**——`.tier-btn` 提高对比度；`extendMatch` 改二次确认+提示（不再误触无反馈）。
- **待做大改(按优先级, 需用户定序 + 决策)**：
  - **#5 牌谱UI重构（核心已上测试服，待验收）**：①**桌面左右边缘小箭头**(`.edge-arrow`，左=当前战绩 openStats、右=牌谱回顾 openHistory，body.in-room 才显示)——游戏内直达。②**牌谱列表**加底牌+公共牌小卡(`.hi-cards`)+净盈亏。③点列表→**牌谱详情** `openHandDetail`(`#hand-detail` 清爽 breakdown，参考截图3)：各家 头像+位置(handPositions 定 BTN/SB/BB, 2人=D/SB+BB)+底牌(自己/摊牌者亮·弃牌者盖背)+**各街动作徽章**(翻前/翻牌/转牌/河牌, 弃/过/跟/下/加 + 额)+本手净变化(endChips−startChips)+底池；顶部「▶回放」按钮转动画回放。④**回放全屏**(`.replay-box` 撑满、`#rp-table` flex:1)。数据字段核对通过(seats/actions/results/buttonUserId/community)。**待做**：每家牌型名(需服务端存 category 或客户端评估)；美化细调。
  - ~~**#7 点头像看本局数据**~~ **已做(测试服)**：点任意头像(`avatar-block` onclick)→ `#avatar-popup` 弹层显示**本局** VPIP/PFR/3bet/**ATS**/手数/盈亏（`stats.js` 加 `room` 过滤+ATS偷盲率；服务端 `req_player_stats`→`player_stats` 按当前房间聚合）+ 表情行；**给别人发表情=从自己头像飞到对方头像并冒泡**(`flyEmote`，`emote` 带 targetUserId)，给自己=广播冒泡。E2E 接口验证含 ATS 字段。
  - **快捷短语已换**：换成截图4那 12 条扑克梗(竖排整行 `.chat-phrases` flex-column)。
  - **#6 聊天/表情重构**：聊天抽屉=聊天记录+输入框+「常用语」按钮(短语)；表情=点头像发（自己/对方）；**语音**(按住录一段播给全体)后置为语音模块。
  - ~~**#4 高牌定庄+动画**~~ **已做(测试服)**：开赛首手前每人发一张牌比大小(rank，同点花色 S>H>D>C)，最大者本场首庄；`drawForButton` 广播 `button_draw`(各家牌+winnerId)，客户端 `showButtonDraw` 座位上方翻牌+赢家金光(2.8s)，然后正式发牌；`forceButtonSeat` 让首手用抽出的庄不轮转，之后正常轮转。`start_game`/`tryStartHand`→`beginPlay`(首手定庄/之后 startHand)。E2E：赢家=首庄、第二手轮转 通过。
  - ~~**#2 时间卡系统**~~ **已做(测试服)**：初始思考 **18s**；每次 **+15s 消耗 1 张时间卡**，**仍保留单次行动累计 2min 硬上限**(`EXTRA_MAX`，防单手无限拖)。时间卡数量 = **时长(h) × 买入BB × 0.25**（`timeCardsFor`，1h/100BB=25，偏宽松够用）；SNG 无时长按起始筹码给一份。**补码(`chargeRebuy`)、比赛加时(`extendTable`)自动补卡**；站起/回座保留卡数。`add_time` 消耗卡+校验上限；`canAddTime` 加"有卡"判断；state 下发每人 `timeCards`，客户端**加时按钮旁显示 ×N** 剩余卡。E2E：发放25/消耗到17/2min上限(138s)/canAddTime 全对。（⚠️注意：改 ACTION_TIME 那块曾误删 `EXTRA_MAX` 致 broadcastState 抛错、整手卡住，已补回——常量删改务必全局搜引用。）比赛加时**不需全员同意**(用户改主意)，保持房主二次确认。
  - **#6 聊天/表情**：聊天面板**删表情行**(表情已移到点头像)、**常用语改折叠按钮+可滚动**(12条扑克梗)；给别人发表情=从自己头像飞到对方(`flyEmote` CSS keyframe，手机更稳)。
  - **行动浮层容器透传**(`#action-bar` pointer-events:none + 按钮 auto)：自己行动时也能点头像看数据。**站起回座不弹买入框**(带原筹码直接回)。
  - ~~**🎨 牌面样式**~~ **✅ 已做（2026-08-09 夜间批次：四色/标准/黑面/大字 4 种，`body.cs-*`）**。原记录：⚠️ **设置里「扑克风格」的「标准扑克 / 四色扑克」两个选项目前点了没有任何区别**——因为四色已改成默认常开（`.card.suit-*` 无条件着色），这个开关成了死的。要么做成真正有区别的 **标准/个性/黑面/四色 四种样式**（质感+字体不同，可网找或自设计牌面美术），要么先把这个无效开关从设置里去掉，别让玩家点了没反应。
- 牌谱不再是纯文字：点历史某手 → **可视化回放**(`#replay-overlay`)，还原当时桌面：环形座位(自己旋到底部)+位置标签(BTN/SB/BB)+后手深度(BB)+各街下注/底池增长+思考时间+逐步/自动播放(⏮▶⏭ + 0.5~2× 速度)。对手牌仅在**未弃牌走到摊牌**时亮，弃牌者盖背变暗。
- **后端仍存文字/结构化 JSONL**(hands.jsonl)，新增每手 seat/avatar 字段(回放布局用)；amount=该街行动后 currentBet 总额。帧构建数学自检通过(盲注→加注→跟注→收池→翻牌→下注→弃牌，stack/pot 正确)。
- ~~**破产救济/签到送币 已改为"等邮箱账号后再做"**~~：**签到已实现**（见上「签到+反馈」批次，2026-07-10，邮箱账号已上线故成立）；**破产救济**仍待做。

## ⏸️ 暂停点（2026-06-23，记录待办与待验收）
- **待用户验收（已部署测试服 5090，未上生产）**：A 个人栏(资料/生涯战绩/牌谱)+VPIP/PFR/3bet/C-bet/AF/WTSD/盈亏曲线统计；B 桌内聊天+快捷短语+Emoji 表情(座位冒泡)。快捷短语/Emoji 当前是默认集，待用户定具体留哪些。
- **🔴 下一步优先：牌桌 UI 重构批次（体感,参考德扑之星 9-max 截图）**——核心问题是"元素太大太满、该按需显示的常显了、缺焦点感"：
  - **小方块头像座位**：名字在上·筹码(BB)在下·**本手下注=头像顶部小 chip 徽章**；整体缩小外扩、留白，9 人不挤
  - **状态按需气泡**：弃牌/等待/All in 只在该状态冒气泡小旗；**弃牌者隐身变暗、不显示任何牌**
  - **对手牌朝桌心摆**（不盖头像/筹码）；**牌型("一对/两对")独立深色小徽章、绝不盖筹码数**（修掉之前"覆盖式盖头像"的做法）
  - **All-in = 火焰/橙光高亮**包住头像+牌；**当前行动 = 呼吸光环+倒计时**（两者区分）
  - 顶部**合规横幅**"绿色竞技 远离赌博·谨防诈骗 健康生活"
  - (可选)锦标赛外壳：排名 12/35、桌号等，待定
- **动画/触感批次**（接 UI 重构）：下注筹码飞入底池+收池、赢额「+X」弹字、数字滚动(count-up)、过渡缓动、手机振动反馈
- **玩法体验**：①**全押跑马实时胜率%**（算法已讲清：剩余公共牌穷举/翻前蒙特卡洛，server 每街算 equity 广播）②**预操作**（提前勾 过牌/弃牌/跟注，到点自动执行）③下注 UX 更轻盈
- **等域名再做**：邮箱账号系统（注册邮箱验证码 + 忘记密码重置）、TLS/HTTPS(WSS)。
- **更远**：AI 对战、破产救济/签到送币、9-max 紧凑度细调、现金桌可选升盲。
- ~~**⚠️ 生产(香港)落后于测试服**~~ **已不适用（2026-08-08）**：香港现已与 main 同步（含全押离场修复、无效加注、四格房间码、审计脚本等全部批次）。上生产前仍须先查有没有人在玩。

## 📖 开源文档与许可（2026-07-24）
- 仓库 public，加了 `README.md`（英）/`README.zh-CN.md`（中）双语说明、`CONTRIBUTING.md`、`SECURITY.md`。
- **许可证：PolyForm Noncommercial License 1.0.0**（`LICENSE`）——源码可见、允许非商业使用/自建/改/贡献，**商用须作者书面授权**（非 OSI「开源」而是「源码可见」，贴合"防商业窃取"诉求）。产品名/logo「德扑道场 / Poker Dojo」不在代码许可范围，另主张商标。
- 贡献走 **DCO `Signed-off-by`**（`git commit -s`），保留将来双授权/单独商业授权的权利。
- README 刻意不含生产部署/服务器 IP/管理员引导等运维细节（那些在本 CLAUDE.md）；`data.json`/`hands.jsonl`/`secret.key`/`mail.json` 已确认未被 git 跟踪。
- ~~⚠️ 建议（待办）：把运维细节挪到私有文档~~ **✅ 已做**：见 `OPS.local.md`（gitignore）。原记录：本 CLAUDE.md 含生产 IP、SSH 别名、部署与管理员引导等运维细节，随仓库公开——如介意可把这部分挪到私有 ops 文档，CLAUDE.md 只留架构。

## 🩹 边池显示 + 未跟注退还（2026-07-24，测试服+香港已上）
- **现象**：多人 all-in 时出现「一长串边池 边池1/2/3/4/5」，最终结算金额正确但层数虚高、看不懂。
- **根因**：`buildSidePots`/`livePots` 对**每个不同投入额**都切一层——**弃牌者的盲注/半路弃牌零头**会切出一个与相邻档位「有资格赢家完全相同」的**多余边池**；**未被跟到的下注**也会残留一个「只有下注者自己有资格」的单人边池。
- **修 1（合并）**：`mergeAdjacentPots` 把相邻「有资格玩家集合完全相同」的档位并成一个池（结算等价，只是不再显示成一串）；`livePots` 同样按 userId 集合合并。
- **修 2（退还未跟注）**：`returnUncalledBets`——最高投入者超过「第二高投入」的部分无人能跟 → 退回给他（`collectBetsToPot` 后调用、幂等；在 all-in 亮牌 + `doShowdown` 各调一次）。**关键**：修正 **run-it 不等筹码结算**——之前 A 全押 5000 / B 跟 2000 时底池按 7000 均分，B 有机会赢走 A 未被跟注的 3000；现退还后只均分真正争到的 4000。同时消除未跟注残留的单人边池。
- **验证**：不等筹码全押+run-it（A 退还 3000 / 守恒 7000 / B 不超 4000）✅；3 人弃牌盲注→摊牌合并为单一主池 10040 / 守恒 ✅；runit/站起/房主回归全通。

## 🎲 多次发牌 run it N times（2026-07-24，测试服已上）
- **两人 all-in 协商发几次**：亮牌后若**恰两人对局**且还有公共牌未发 → 发起协商——**落后方(equity 低)选发 1~5 次**，**领先方同意**才生效；多人局(3+)固定发 1 次。
- 同意后**底池均分 N 份**，以**已发公共牌为共享底**，从牌堆**依次发 N 组不同 runout**（每组补齐剩余街+烧牌），各自判赢家分得该份（平局再均分该份，余数进第一份）。
- **绝不卡住牌局**（用户第一优先级）：协商 **25s 超时** / 领先方拒绝 / 落后方选 1 → 都回落到**单次跑马**（复用原 `advanceStage`）。
- 服务端 `server.js`：`offerRunIt`(判定+定 decider/leader)→`runit_offer`；`propose_runs({n})`→`runit_proposal`→`respond_runs({agree})`→`resolveRunIt`→`executeRunouts`(发 N 板、判赢、发金、`runit_result`)；`broadcastState.runIt` 供断线重连恢复；`startHand`/`endCashTable`/`dissolve` 清协商残留+定时器。牌谱 `hand.runIt={n,runs}`。
- 客户端 `index.html`：`#runit-panel` 协商面板(落后方选次数按钮 / 领先方同意·拒绝 / 观众"协商中")；新一手/重连自动清理。
- **动画流程（体感，2026-07-24 三次迭代）**：
  - **逐街发牌（慢，同真实跑马节奏）**：`executeRunouts` 建时间线，每组按 **flop→停顿→turn→停顿→river→停顿** 发（`RUNIT_STREET_MS=1.3s`），该组 river 发完→比牌→该份底池**飞向本组赢家**（`flyCoinsToWinner`+筹码到账+底池递减）→下一组；`runit_begin`/`runit_street{run,street,cards}`/`runit_award{run,winners}`/`runit_done`。复用 `game.runoutTimer`（cleanup 已覆盖）。
  - **N 板「都显示出来」不覆盖**：客户端 `#runit-boards` 共享底牌只显一行，每组各占一行留在桌上；**已发 3/4 张后 all-in** 则共享 flop/turn 只显示一次、**只把最后 1~2 张差异牌并列在原位置**（`baseLen<=0` 全并列=N 整行；`rib-slot.ghost` 占位对齐）；发牌时隐藏原单行 `#community`。
  - **牌谱完整记录多次发牌**（数据资产）：`hand.runIt={n,boards[],winners[],amounts[]}` 保留每次 runout；`community` 仍落第 1 次 board 向后兼容（stats/回放不破）；牌谱详情 `openHandDetail` 展示每组公共牌+赢家+金额。
- **本地 socket 测试通过**：发 3 次逐街（每组 flop/turn/river 三街、时序 ~1.3s/街）、逐组飞池合计 10000、收尾摊牌不卡；站起/暂停/强制站起回归全通。

## 🐛 多次发牌残留 bug + 牌堆兜底（2026-07-24，香港已上；测试服待网络）
- **现象**：双方 all-in 发多次后"卡一下→公共牌不发任何牌→直接判胜负"，牌谱看似"只发了一次"。
- **根因**：run-it 用 `#board.runit-on` 隐藏单行 `#community`、改显 `#runit-boards` 的 N 板；该 class **只在 preflop 过渡时 `clearRunit` 清**。若**下一手是普通单次跑马**（不触发 `runit_begin`）且那次过渡清理被漏 → `#community` 被一直藏住 → 那一手"没牌就判胜负"。牌谱**列表本就只显第 1 个 board**，被误读成"只发一次"（其实 run-it 那手是好的，bug 出在**紧跟的下一手**显示上）。
- **修（三层冗余）**：①**自愈**——每个 `game_state` 后 `board.classList.toggle('runit-on', !!runitState)`：只要没有正在进行的多次发牌，公共牌必显，漏清也会被下一个 state 纠正；②`hole_cards`（新一手）+ `showdown_reveal`（普通摊牌=本手非 run-it）都 `clearRunit`。
- **另修 run-it 引入的崩溃**：`maxRunsByDeck` 按剩余牌堆限制可发次数（`offerRunIt` 的 max + `resolveRunIt` 夹取），防 9-max 多人弃牌后牌堆不足发 N 次导致 `drawCard()` 返回 null 崩/卡；客户端次数按钮随 `max`。
- **验证**：jsdom 加载**真实 index.html** 直接跑 `buildRunitBoards`/`runitDealStreet`/`clearRunit`——preflop 2 组各 5 张、翻后全押共享 flop+每组并列 2 张、clearRunit 复原 全对 ✅；socket 回归 runit/sidepot/sidepot2/standflow/hostfeat 全通 ✅。（确认渲染函数本身没问题，就是跨手残留 class。）

## 🌙 夜间批次（2026-08-09，用户睡前授权自主推进，测试服待验收）
> 用户指示：先修小 bug，再按「UI/体验」栏顺序做，做完继续 Straddle 链式；实现细节自行决定、记录存档、次日统一验收。

- **管理面板手机端**：`#admin-panel` 是 `#game-section`(100dvh flex) 的直接子项且 `flex-shrink:0` → 内容顶出视口、自身无滚动条，手机上翻不到底。改为可收缩+自带滚动+限高 78dvh；加 ≤560px 媒体查询（玩家表横向滚动不压扁列、搜索栏输入框各占一行、流水/玩家行改两行式）。
- **弃牌后看不到自己牌型**（玩家反馈）：`emitHandHints` 原本 `if (p.folded) return`。改为继续下发（带 `folded:true`），客户端显示「弃 · 牌型」灰虚线徽章，摊牌阶段也保留。**只用他自己的底牌+公共牌、私发本人，不含对手信息**。⚠️ 测试要三人局——两人局一方弃牌该手立即结束，走不到这条路径。
- **#4 表情与互动**：限频 0.8s → **令牌桶**（连发 6 个、每 0.7s 回一个），点完不自动关弹层（1.5s 无操作才收）；**动态表情**（叉鱼甩杆/鲨鱼咬合/干杯碰杯/送花轻飘/翻滚/弹跳，纯 CSS 零素材）；**连发计数**在目标头上累加 ×N，飞行体带随机偏移像「一串」。实测连点 8 次广播 6 次（旧逻辑仅 1 次），狂刷 30 次仅放行 2 次。
- **#5 牌面样式（修死开关）**：原「标准/四色」完全等效。改为 4 种真有区别：四色 / 标准(传统两色) / **黑面**(深色卡面浅色字) / **大字**(点数占满、花色缩角落)，`body.cs-*` 切换 + 持久化，纯 CSS。
- **#7 牌谱滚动到底自动加载**：`IntersectionObserver` 把原「加载更多」按钮本身当哨兵，进视口自动拉下一页；保留按钮做降级；到底停止不重复拉。
- **#8 牌谱每家牌型名**：服务端 `saveHandHistory` 时算好落库（`results[].category/folded`）——牌力评估只此一份、历史牌谱自带该字段。⚠️ **展示端做隐私判断：只在牌面本来可见时才显示**，否则会泄露弃牌者从未亮过的牌型。
- **#9 收件箱结构化**：标题 / 我的名次(盈亏两色高亮) / 分节 / 称号 / 排名行(序号圆点)；格式由我们自己发出故解析稳定，未知行原样显示，全程 escapeHtml（含 XSS 断言）。
- **#11/#12**：下注额变大时从头像抛一枚筹码到下注徽章（补「推筹码」手感）；下注面板加一行「剩余 X · 占底池 Y% · 对手需跟 Z」，全下特别标注。
- **Straddle 链式（re-straddle）**：UTG 贴 2BB → 立刻问下一位要不要 4BB → 8BB…每接受翻一倍，可一路到翻前全押。开手时按顺序逐档贴出并**逐档重新校验**（位置没变/筹码够/金额是该档应有值），任一档不满足就在那里截断；`currentBet`/`lastRaiseSize` = 最后一档；**翻前第一个行动的是最后一个 straddler 之后那位**（straddler 享有最后行动权）；牌谱逐档记录。
  - 踩坑①：`afterBB` 名单少算一位——**按钮位也能 straddle**（翻前顺序 UTG→…→CO→**BTN**→SB→BB），应为「总人数 − 2」。
  - 踩坑②：沿用了「SHOWDOWN 不准备决策」的判断，而 **straddle 邀请本来就在局间(SHOWDOWN)弹出** → 玩家接受后根本轮不到下一位，链永远只有 1 档。查这个是靠临时打点日志定位的。
- **顺带修：run-it 决策方离开后全桌空等 45s** —— 协商中若离开的正是 decider/leader，立刻按超时同一结果回落成发 1 次（结论一致，只是不必再等）。这个是从「两个 all-in 测试偶发失败」里挖出来的真问题，不是测试抖动。
- **回归**：14 项 socket + 10 项 jsdom + 朋友 25 个单测 全通。

## 🧿 商业化打磨线 A1–D（2026-09-09，**测试服已验收 2026-09-10**，未上生产）

> 阶段转折：**「几个好朋友能玩」这个目标已经达到了**，下一步是成熟的商业化作品，
> 所以这一批全是「不加新功能、只把已有的做精细」。优先级与估算见私有 `ROADMAP.local.md`。

- **A1 全站配色统一到道场绿**：原本三套配色并存（道场绿牌桌 / 深蓝面板 / 黑登录页），
  切换界面就断裂一次。统一到一套：底 `#22392e`→`#12201a`，文字 `#eaf3ee`/`#dfe9e2`/`#9db9a9`，
  金 `#f4d35e`，蓝 `#3a86ff`=可操作，薄荷 `#06d6a0`=已选中。
- **A2 金色收敛**：75 处降到 61 处 —— 金只留给「钱 / 赢 / 我 / 奖励 / 品牌」。都金 = 没有重点。
- **A3 节奏统一**：动效 33 种时长收敛成 4 档（0.15/0.3/0.5/0.8s），圆角 16 种收敛成 4 档（6/10/14/999px）。
  `0.64s` `9px` 这种随手值单看都没问题，**放一起就是「说不上哪儿不对但不够精致」的来源**。
- **B1 键盘快捷键 + 自定义绑定**（F 弃 / C 过跟 / R 加注 / A 全压 / Enter 确认 / 空格准备）：
  含绑定 UI、冲突检测、恢复默认；**加注面板支持鼠标滚轮调额**（步长可设 0.5/1/2/5 BB，Shift ×10）。
- **B2 无障碍最小集**：11 个纯 emoji 图标按钮补 `aria-label`（明眼新用户也猜不出那排图标是干嘛的，
  不只是读屏问题）、桌面播报区加 `aria-live`。
- **C i18n 服务端结构化**：服务端不再 emit 中文原文，改发 `{ k, p }`，翻译在客户端字典，
  **各客户端按自己的语言渲染**。⚠️ 见下方「估算教训」。
- **D 牌桌视觉层级**：见下。

### 验收轮里玩家报的问题（都已修）
- 牌谱回放的牌「数字花色撑爆牌面」：**缩小牌面必须改 `--card-w`，不能只写 `width`** ——
  rank/suit 字号是 `calc(var(--card-w) * …)`，只改 width 会让 `--card-w` 停在 `:root` 的上限。
  已加 `frontend-structure.test.js` 守卫。
- 比赛设置里两行标签看不清：`.cfg-label` 是深蓝 `#3a4d70` 落在深绿底上。
  ⚠️ **A1 的扫描只覆盖了静态 HTML，这块是 `renderMatchInfo` 用 JS 拼的** —— 改配色要连 JS 生成的 DOM 一起扫。
- 设置面板内容太多 → 拆成 外观/牌桌/操作 三个 Tab。第一版每切一次整框高度都在跳，
  改成 **grid 同格堆叠**（`grid-area:1/1` + `visibility` 切换）让高度恒等于最高的那页。
- **点开聊天框后键盘仍触发快捷键**：快捷键的「有面板打开就不响应」判断用 `offsetParent`，
  而聊天面板是 `position: fixed` —— **fixed 元素的 `offsetParent` 恒为 null**，判定永远失效。
  改用 `getClientRects().length > 0`。
- ⚠️ **全押快捷键静默失效**：我写的是 `sizeFor`，真实函数叫 `sizeForQuick`。
  **而 vm 测试里我按自己的错误假设 stub 了 `sizeFor`，所以测试一路全绿。**
  根治办法不是改那一行，是加结构性断言：**快捷键用到的每个外部全局、每个 `typeof X` 守卫，
  都必须在真实源码里找得到声明**（`hotkeys.test.js`）。

### D 的三档层级契约（改牌桌前先读 `20-table.css` 开头那段）
问题不是「元素太多」（常驻 11 个、每座位再叠 7 层，每个都有用），是**权重是平的**。
- **T1 焦点**：公共牌 · 底池总额 · 当前行动者 —— **只有这档允许动效**
- **T2 信息**：座位名字筹码 · 各类徽章 · 桌面提示 —— 中等对比、静止
- **T3 背景**：合规横幅 · 房间水印 · 观战横幅 · straddle 旗 —— 低对比、**不许 infinite**

落实的三件事：①行动者放大 8% + **其余座位同时压对比 14%**（两边一起动差距才拉得开，
只做一件几乎看不出变化）；②给「底池+公共牌」垫一层向外淡出的暗影把桌心抠出来
（**只压暗四周不提亮中心**——桌布本身是亮绿，加亮会糊成一片）；③**straddle 小旗原来是
`infinite`**，一面常驻牌子永远在闪、正好和「轮到谁」抢注意力，而它一点都不急，改成闪 6 次即止。
→ `table-hierarchy.test.js` 守住三条可机检的不变量：**常驻动效白名单**（再加一个永远在闪的
东西就挡下来）、**字号阶梯不许倒挂**、**`has-actor` 必须真有人挂**（防「CSS 写了但规则永不生效」）。

### C 的估算教训（差了一个数量级）
原估 3–5 天是按「118 条中文硬编码」算的，实测**真正会出现在玩家眼前的只有 57 条**：
56 条是 `socket.emit` **私发给单个玩家**的 ⚠️ 拒绝提示，另 1 条是 `table_notice`；
其余 56 条广播**客户端只 `console.log`、根本不上桌面**。也就是说清单 #10 里担心的
「一条广播要同时满足两种语言」**只存在于看不见的消息上**，整个难点自动消失，1 天做完。
→ **估算前先数「玩家能看到几条」，不是「代码里有几条」。**

⚠️ `renderServerMsg` 里**不能**用 `t(key, null)` 判「字典里有没有这条」—— `t()` 找不到时
**返回 key 本身**（`fallback` 传 null 也一样），照那么写，缓存了旧 JS 的客户端碰到服务端的
新 key 会把 `"srv.xxx"` 原样弹成一条 toast。必须显式查字典。**同一个误解还让「字典完整性」
那条断言永远为真、一个缺失都抓不到** —— 又一次印证：写完测试必须验反向。
向后兼容：老客户端收到对象，原有 `typeof msg === 'string'` 判断让它静默跳过（不崩），刷新即自愈。

## 🌐 双语排查 + check-i18n 关卡（2026-09-10，**测试服已验收**，未上生产）

> 🔴 **以后做任何新功能都要一开始就写双语**（用户 2026-09-10 明确要求）。
> 目标用户不限中国境内，事后补翻译必然漏。

**玩家实拍**：界面已切成 English，可键盘快捷键那六行、牌桌上「XX 想看河牌」那行提示还是中文。
逐条查完发现真正的问题不是「漏翻了几条」，而是**没有任何东西拦着漏翻**——
加新功能时顺手写一句中文，没人会发现，直到某天有人截图。

### 两个根因
1. **快捷键那六行本来就写了 `L(d.zh, d.en)`** —— 漏的是【切完语言没人重画它】。
   `setLang` 里维护着一张**手写的重渲染清单**，后加的面板永远会被漏掉。
   → 改成**登记制**：各模块在自己文件里 `onLangChange(fn)`，`setLang` 只管遍历。
     加新面板时不必回头改 `setLang`，这类漏就不会再发生。
2. **`table_notice` 是广播**，C 那轮只迁了 `server_msg` 把它漏了。
   同桌两人可能一中一英，一条写死的文案满足不了 → 改发 `{ k, p }`，各客户端自己渲染。

### 补齐的范围（扫全库逐条过）
- **服务端玩家可见的三条通道**：`table_notice`、`invite_error`（4 处）、
  **HTTP 错误响应**（57 处 / 44 条不同文案，覆盖登录注册找回密码绑邮箱语音改名）。
  做法同 C：**仍发中文原文做兼容，额外带一个 `k`**，客户端 `apiErr()` 按 k 查自己的字典。
  （保留中文原文是故意的：老缓存客户端、curl、日志都还读得懂。）
- **客户端 46 处裸中文**：语音模块全部提示、牌谱回放（街名/动作名/底池/后手）、
  断线遮罩、多次发牌板、下注信息条、距升盲/剩余时长、房间列表兜底名。
- **index.html 12 处**静态文案挂 `data-i18n`（准备/开始/等待房主/坐出/观众/底池…）。
- **顺带修 5 处写死的 `toLocaleString('zh-CN')`** —— 英文用户看到的仍是中式日期。
  统一走 `fmtDateTime`/`fmtTime`，locale 跟界面语言走。这和待办 #13「时间显示要脱离服务器时区」
  是同一类问题：**别把某一种区域设置写死**。

### 关卡：`tools/check-i18n.js`（已接入 `npm run check`）
守三条：客户端中文必须包在 `L()`/`t()`/`apiErr()` 里；index.html 中文必须挂 `data-i18n`；
服务端发给玩家的中文必须带结构化 key。**放行要写明理由**（`console.*` 日志、成对的
zh/en 数据表、产品名 → 就地写一句 `i18n-ok: 原因`）。
- **写完立刻用它扫，当场又抓出两条我漏掉的**：改显示名的两个报错。
- 反向对照 4 条全部咬得住（塞裸中文 toast / 加没挂 data-i18n 的按钮 /
  把 table_notice 改回写死中文 / 去掉某个 HTTP 错误的 k）；
  另加 3 个测试守住登记制，含「**`setLang` 里不许再手写清单**」。
- ⭐ **唯二不译**：管理面板（`90-admin.js` + index.html 的 `#admin-panel`）、房间名（房主自输内容）。

### ⚠️ 顺手踩的一个坑（工具类，别再踩）
用 Python 改文件时 `open(p, 'w')` 会**先清空再写**，中途抛异常（当时是 emoji 代理对编码失败）
就留下一个 **0 字节的 `03-i18n.js`**。改成**先写 `.tmp` 再 `os.replace`**，中途失败原文件分毫不动。
（emoji 也别写 `\ud83d\udd34` 这种代理对转义，直接写字面量。）

## 🤝 牌友系统第 1 批 + 牌友号（2026-09-13/14，测试服）

> 产品名叫「好友开房」，可在此之前**没有好友**：想一起玩只能「房主复制房间码 →
> 切到微信发 → 对方切回来输码」，每局重来一遍。

- **关系存两行**（A→B、B→A 各一条，`003-friends.sql`）。之所以不用「一行 + 排序后的 pair」：
  **备注是每人各自私有的**（我给你记的「爱诈唬」你不该看见），一行存不下两份备注；
  而且 `(user_id, status)` 这个索引直接就能查「我的列表」，不必对每条做方向判断。
  → 最容易出的错因此变成**只写了一半**（「我把你删了、你那边还挂着我」，平时完全看不出来），
  所以 `friends.test.js` 里每条关系变更都从**两个人各自的视角**各断言一次。
- **双向确认**：申请 / 同意 / 拒绝 / 删除；「一起玩过」从牌谱 CTE 算（已排除已有关系的人）。
- **牌友号**（`004-friend-code.sql`）：注册时发一个 **8 位随机数字**，永不可改；存量用户开库时补发（幂等）。
  - **随机而不是递增**：递增号会公开「你是第几个注册的」（等于公开用户规模），还能顺着号猜相邻账号。
  - **搜索只认精确匹配**（完整牌友号，或完全一致的用户名）。刻意不做模糊/前缀搜索——
    那等于开放一个把整个用户库枚举出来的接口。
  - ⚠️ 「随机」这条**必须机检成「不是按注册顺序递增」**。第一版只断言「唯一 + 8 位」，
    我把发号换成递增方案照样全绿——**唯一性根本不是随机性**。

### 验收轮里玩家报的问题（都已修）
- **面板被排成一条条竖字**：我写了 `style.display = 'flex'`，而 `.side-panel` 没声明 display
  （默认 block）→ 变成**横向**弹性容器。其余面板全是 `''`（交给 CSS），照抄即可。
  连带一个：`onLangChange` 里判「面板开着吗」写成 `=== 'flex'`，而真实值是 `''` → 永远不成立。
- **点空白处应该关掉当前这个，而不是又开一个新的**：捕获阶段先关面板再 `stopPropagation()`。
- **填备注用了 `prompt()`**：手机 WebView 里会被限制甚至直接不弹，还顶着一行「来自网页的提示」，
  观感像钓鱼弹窗。改成自家的 `note-modal`；`confirm()` 同理换成 `uiConfirm()`。
- **备注太长把名字挤没了**（实拍「admin1 s⋯」）：备注跟名字包在同一个 `.fr-name` 里，
  而那是条 `nowrap + ellipsis` 的**截断行**——**两段长度不受控的文本共用一条截断行，必然互相吃掉对方**。
  改成备注单占一行。→ 关卡见下。
- **牌友号点了没复制也没提示**：`navigator.clipboard` 只在**安全上下文**（https / localhost）里存在，
  测试服是 `http://10.76.x.x:3000` → 它直接是 `undefined`，`navigator.clipboard?.writeText(...)`
  于是**静默地什么都不做**：没复制上、不报错、连一句提示都没有，玩家只能当成功能坏了。
  而邀请码那边**早就有一个带 `execCommand` 降级的 `copyText()`** —— 我只是没用它。
  → 三个复制入口（牌友号 / 版本号 / 邀请信息）统一走 `05-utils.js` 的 `copyText()`，**成败都给一句 toast**。

### 🔴 `/^[⚠✅👥]/` —— 一个字符类把没翻译的广播全弹了出来
玩家实拍：界面已切成 English，却冒出「💺 admin1 留座离座」「🔄 重新连接成功」「🛑 房主提前结束了比赛」。

这三条是**服务端的中文广播，本来只写 console、从没打算上桌面**（见 C 那轮的结论：
玩家真正看得见的只有 57 条，其余 56 条广播客户端只 `console.log`）。把它们捅到屏幕上的是我自己：
`👥` 是**代理对**（`\uD83D\uDC65`），而**字符类在没有 `u` 标志时按码元拆**，
于是 `/^[⚠✅👥]/` 实际等价于 `[⚠✅\uD83D\uDC65]` —— 退化成「**以 `\uD83D` 开头就匹配**」。
💺🔄🛑 全中，🎲🏆🪑 不中（它们是 `\uD83C`/`\uD83E` 开头）。**看不出任何外在症状，只能靠机检。**
→ 改成显式前缀数组 `TOAST_PREFIXES.some(p => text.startsWith(p))`。想匹配一组 emoji 就别用字符类。

### 三条新关卡（`frontend-structure.test.js`，反向对照全部咬得住）
1. **正则字符类里不许出现 emoji**（除非带 `u`/`v` 标志）——扫全部客户端脚本的正则字面量。
2. **`navigator.clipboard` 只许出现在 `05-utils.js`**（即只许走 `copyText()`）。
3. **截断行（`nowrap + ellipsis`）里只许放一段内容**——从 CSS 反查出所有截断类，
   再看 JS 模板里有没有往里塞第二样东西。
   - ⚠️ 这条**第一版抓不到它自己要抓的那个 bug**：我只查了字面 `class="`，
     而出事那次写的是 `${escapeHtml(f.displayName)}${note}`，`class=` 藏在 `note` 变量里。
     改成**同时按插值个数判**（>1 就报）才咬得住。**又一次印证：写完测试必须验反向。**
- **这两条新关卡当场在旧代码里各抓出一处同类问题**（不是这批改出来的）：
  ① `40-profile.js` 的 `/^[🥇🥈🥉]/`（三个都是 `\uD83E` 开头 → 「以 `\uD83E` 开头就算称号行」）；
  ② 回放座位 `.rp-name` 把**位置标签 BTN/SB/BB 和名字挤在同一条截断行**里，名字一长就把位置吃掉——
  而位置恰恰是回放里最该看见的东西。改成名字截断、位置 `flex-shrink: 0` 永远可见。

### 顺带：「测试跑不完」其实是一个泄漏的定时器
`npm test` 一直要跑 **5 分钟以上**，我一度以为是卡死。真因：
`cash-match-time.test.js` 第一条调 `onTableTimeUp`，而它会**武装一个 5 分钟的兜底结算定时器**，
测试里没人清—— `node --test` 于是抱着这个 timer 干等到它到期才肯退出。
**断言全部是对的，错的是没拆弹**。补上 `t.after(() => clearTimeout(game.timeUpGraceTimer))`：
该文件 5 分钟 → **71ms**，全套 108 条 **0.84s**。
→ 教训：测试里只要调到会装定时器的服务，就得自己负责拆。否则症状是「测试很慢/跑不完」，
  根本不会指向真正的原因，而一套要跑 5 分钟的测试等于没人会随手跑。

### 待办
- ~~第 2 批：一键邀请~~ **✅ 已做（2026-09-14，见下）**

## 📣 广播分级 + 「上不上屏幕」改成结构判定（2026-09-14，测试服）

> 承上：把「哪些服务端广播该让玩家看见」逐条过了一遍，只放出**一组**。

### 只放 C 组（19 条），其余留在 console
逐条数完是 **65 条广播 + 7 条私发裸中文**，分成五组后只提升一组：
- **A 每手动作播报（19 条，不放）**：弃/过/跟/下/加注、超时、新一局、盲注、Flop/Turn/River、
  摊牌、赢池、退还… **牌桌本身就在实时演这些**，再弹一遍 toast 是纯噪音，一手能弹十几条。
- **B 座位人员进出（13 条，不放）**：座位上已有 💺📴💤🧍 徽标，信息没丢。
- **C 房主 / 管理员 / 比赛节奏（19 条，✅ 放出来）**：结束比赛、加时、调整结束时间、暂停/继续发牌、
  Straddle 开关、锁定/开放入场、解散、涨盲、升盲、夺冠、训练时长到点、管理员结束/解散。
  → 挑这一组的标准：**它们改变全桌规则或直接终止比赛，而牌桌上没有等价显示**。
- **D 多次发牌协商（6 条，不放）**：已有专门的 `#runit-panel`，重复。
- **E 零散（8 条，不放）**。

### 🔴 「上不上屏幕」不再看 emoji 前缀，改成看【消息形态】
这是这批最值钱的一条改动，因为前缀匹配**连栽两次**：
1. 一开始只弹 `⚠️` → `✅ 申请已发出` 这种确认反馈一条都看不到，玩家点完毫无反应；
2. 改成字符类 `/^[⚠✅👥]/` → `👥` 是代理对、字符类没 `u` 标志时按码元拆，
   整条退化成「以 `\uD83D` 开头就匹配」，把 💺🔄🛑 这些**从没翻译过**的广播全漏上了屏幕。

现在的契约极简：
- **结构化 `{k, p}` = 服务端明确「这条给玩家看」→ 一定弹**，且必然有中英两份文案；
- **裸字符串 = 只进 console 的动作播报 → 一律不弹**。

→ **裸中文在结构上就不可能漏到屏幕上了**，不再依赖任何字符串匹配。

### 关卡自己也有两个盲区（两个都真漏过 bug）
`tools/check-i18n.js` 规则③ 和 `server-msg-i18n.test.js` 那条断言用的是**同一个正则**，
所以是同一对盲区，**两道网一起失效**：
1. **只按【行】匹配** —— 而补码失败那两条是**跨行三元**：
   `socket.emit('server_msg', cond ? \n ? '⚠️ 金币不足…' \n : '⚠️ 补码失败…')`。
   按行匹配对它完全视而不见。**那是玩家补码失败时必定看到的提示，英文界面上一直是中文。**
2. **只认 `⚠`** —— 我把客户端 toast 过滤扩到 `⚠️/✅/👥` 时忘了同步扩它，
   `✅ 已解散房间` 就这么一直漏着。
→ 修法：改成**按整个 `emit` 调用取参数**（括号配平，跨行也行）；
  并加一条**契约断言**——`20-socket.js` 里必须仍是「结构化才上屏幕」，退回前缀判就报错。
  反向对照三条全部咬得住（跨行三元 / ✅ 开头 / 改回前缀判）。

### 新关卡：字典里的每个 `{占位符}`，服务端都必须真的发出来
这批一次加 25 个 key，最容易犯的是**服务端写 `p:{min}`、字典写 `{minutes}`**——
`renderServerMsg` 找不到就代入空串，玩家看到「⏱ 房主加时  分钟」：
**不报错、不乱码，只是少一截，谁都不会发现**。反向对照咬得住。

### 关掉待办 #13，并且【广播不说钟点、只说时长】
`⏱ 房主将预计结束时间调整为 HH:MM` 原来在**服务端**用 `toLocaleTimeString('zh-CN')` 拼好再发
（迁 AWS 时差 8 小时那个）。服务端现已**一处时间格式化都没有了**。

第一版改成发时间戳 + 客户端 `{at|time}` 按自己时区渲染。自洽，但**对广播来说仍然不对**：
广播是【共享语境】，同桌两人不在一个时区就各看各的表 —— 语音里说「打到21点半」，
对方屏幕上写着 16:30，**两边都对、却没法拿这个数互相对话**。
→ 改成发**剩余时长**（`{ms|dur}` → `fmtDuration`）：时长对全桌是同一个数，
  不涉时区、不涉 12/24 小时制、不涉语言。水印本来就一直这么显示。
→ **规矩：发给全桌的播报一律用 `{ms|dur}`；绝对钟点只留给「我自己规划」的地方**
  （比赛设置面板的「预计结束」行，那里各看各的才对），写法是 `{at|time}` / `{at|datetime}`。
→ 面板的「剩余时长」行也改成同一个 `fmtDuration` —— 否则面板说「约 80 分钟」、
  toast 说「1 小时 20 分」，同一个数两种说法。

### 剩下没做的
A/B/D/E 四组 **46 条广播仍是中文，但它们在结构上到不了屏幕**，所以不算漏翻。
以后要放出来哪条，就把那条改成 `{k, p}` 并补中英字典 —— 关卡会逼着补齐。


## 🎟️ 牌友系统第 2 批：一键邀请（2026-09-14，测试服）

房主在桌内邀请在线牌友 → 对方弹窗同意 → 直接拿到下场资格。
**不替代复制房间码**，两条路并存（跨设备 / 发微信还是得靠码）。

### 三条产品约束（用户定的）
- **只邀请【在大厅】的牌友**：在别的牌桌上的人要先从那张桌摘下来，牵扯离座/结算一整条链路；
  产品上也不该把人从一手牌里拽走。**这一条变相绕开了一整类「跨房间切换」的 bug。**
- **房主自己锁了入场就不绕过**：静默放行会变成「我明明锁了，他怎么进来的」，而解锁只要点一下。
  **替玩家做主不如把话说清楚。**
- **只能邀请已经是牌友的人** —— 否则这就是一个骚扰任意用户的接口。

### 🔴 两道闸，少一道都不算数
1. `invite_friend` 校验 `ownerUserId`。客户端那个 `.owner-only` 只是「别让人看见按钮」，**不是闸**。
2. `invite_respond` 必须在服务端 `FRIEND_INVITES` 里找得到那条待办记录（用完即删，3 分钟过期）。
   没有这一条，**任何人知道房间号就能自己 emit 一个「我同意」进来** —— 大厅列表里房间号是公开的。

**邀请事件里绝不带 `joinCode`**。全库只有 `emitRoomInviteInfo` 一处发房间码，
而它第一行就是 `ownerUserId` 校验 —— **单一卡口，别再开第二个口子**。
（被邀请者若拿到码，转手发给别人，「房主控制谁能进」就没了。）

### 验证
`friend-invite.test.js` 9 条，全部从**攻击者视角**写：不是「功能能用吗」，是「绕得过去吗」。
**反向对照五条全咬得住** —— 拆房主校验 / 拆待办记录 / 塞 joinCode / 邀请用完不删（可重放）/ 拆牌友校验，
各自精确打挂对应那条。
另跑真服务器端到端（单测用的是桩，要验事件名、授权、落座整条线）：
伪造同意进不来 / 邀请不含房间码 / 同意后 `canPlay=true` 且真能坐下 / 非房主拿不到房间码 /
进来后不能再拉人 —— 八步全通。临时库连同 `test~test4` 跑完即删。

### 🔴 牌友一多会把弹窗撞开 —— 根子不在列表，在 `.modal-box`
玩家验收时问「牌友多了会不会显示出问题」。会，但问题比列表高一层：
**`.modal-box` 原来既没有高度上限、也不能滚**，而 `.modal-mask` 是 `position:fixed` + `align-items:center`
→ 内容一超视口就**上下各溢出一截、而且滚不到**，连「关闭」都点不着。
实拍里**只有 1 个牌友**时弹窗已经占了约 640px。
这条规则底下挂着**买入 / 补码 / 备注 / 确认 / 邀请所有弹窗** —— 不是某一个的问题。
- 修：`.modal-box` 加 `max-height: calc(100dvh - 40px)` + `overflow-y: auto`（`.modal-mask` 也加一层兜底）。
  **用 `dvh` 不用 `vh`** —— 手机地址栏收起前后 `vh` 是错的（全站本来就用 dvh）。
- 列表自己也得封顶：`min(180px, 30dvh)` —— 写死 180px 在横屏手机（视口才 ~380px 高）会吃掉半屏。
- 超过 8 人才出现筛选框（人少时多一个输入框只是碍事）；标签旁边显示计数 `(30)` / 筛选中 `10/30`。
  ⚠️ **筛选框写在 index.html、不在重渲染的那块 DOM 内** —— 否则每敲一个字就把 input 重建一次，光标当场丢。
  ⚠️ 计数单独一个 span：外面那个标签挂着 `data-i18n`，JS 一写就会被 `applyLang()` 覆盖回去（规则④）。
- 关卡（`frontend-structure.test.js`）：**弹窗必须被视口限住且可滚（且用 dvh）、长度不受控的列表必须自己封顶**。
  反向对照三条全咬得住（拆上限 / 改回 vh / 拆列表封顶）。
  ⚠️ 这条关卡第一版自己就是错的：写了 `/dvh/`，而 `100dvh` 里 `0` 和 `d` 都是词字符、**中间根本没有边界**。

### 顺带
`npm run check` 里的 eslint **当场抓到我漏解构的 `db`**（`invite-events.js` 从没用过 db）。
这正是那道闸存在的理由：这类错要跑到「有人真去邀请」那一刻才崩，而那时是线上。

## 🛡️ 运维两件（2026-09-16）：牌桌公告 + 备份看门狗

### A 管理员向牌桌/全服发实时公告
本文件待办 #11 点名过的缺口：`/api/admin/broadcast` **只写 📬 收件箱**，正在打牌的人当场看不到。
2026-08-10 要通知玩家重启时撞过一次（只能靠站内信 + 口头说），**2026-09-16 又撞了一次**——
想上 1.7.0，可三个人正在 preflop，只剩「干等」或「硬切掉他们」两个选择。这条给出第三条路。

`POST /api/admin/table-notice { roomId?, text }`
- 指定房间 → 只发那一桌；**房间不存在返回 404，不静默成功**（管理员以为发出去了却没发，比报错糟）
- 留空 → 全服，**含还在大厅的人**。重启通知正是这一种：只发牌桌的话，在大厅等开局的人完全不知道。

客户端用**独立事件 `admin_notice`**，不复用 `table_notice`：① 后者只显示在公共牌下方那一行，
大厅的人根本看不到；② 公告是管理员自由输入的，没法走 `{k,p}` 结构化翻译，**也不该翻**
（和房间名同理，属于用户自输内容）。收到后 toast 9 秒 + 牌桌上另留一行。

### B 备份看门狗 `tools/check-backups.js`（生产 cron `45 4 * * *` 已挂）
备份脚本本身很扎实（一致性快照 + **立刻校验那份备份文件**，不 ok 就失败退出），
缺的纯粹是**它失败之后没人知道**——只往 `backup.log` 里写一行。
2026-09-08 就这么静默失败了 **11 天**。**一个只写日志、没人读的检查，等于没有检查。**

查五件事：**异地副本还在不在拉** / 新鲜度（≤26h）/ **那份文件真的能打开**（integrity_check）/
内容合理性（用户数>0、牌谱数不比上一份少）/ 份数。
- 「备份成功但是个空库」比没有备份更危险：**它看起来一切正常，直到你拿它去恢复。**
- 刻意**不复用 `backup-cron.sh` 的任何 helper**：看门狗要能在「备份脚本自己坏掉」时仍然说得出话。
- 🔴 **只认 `pokerdojo-<日期>_<时间>.sqlite`，不认 predeploy 的**。否则部署很频繁、
  predeploy 文件天天更新，每日 cron 停了三个月都看不出来——**这是这个看门狗最容易被做废的地方**。
- `backup-watchdog.test.js` 8 条正反都验（正常不叫 + 七种坏法各自必须叫）。
- 已在生产实跑 ✅，**并且故意让它失败一次验证告警链路不是哑的**
  （`process.exit()` 会在异步邮件发出前结束进程，必须 `await` —— 审计那条 cron 踩过）。

### 🔁 异地副本的闭环（这条绕法值得记住）
本机 `ops/pull-offsite-backup.sh` **每周**把生产最新备份拉回来（计划任务
`PokerDojo-异地备份周拉取`，`StartWhenAvailable=True` 所以关机错过了会补跑）。
但**本机不是 24/7、也没配发信**，`LAST_PULL.txt` 写在本地同样没人读——
和当初 `backup.log` 一模一样的问题，只是挪到了本机。

→ 做法：**本机拉成功后回头在服务器上打一个时间戳** `/root/PokerGame/.last_offsite_pull`，
  由那台 **24/7 且发信链路已验过**的服务器来喊「异地副本已经 N 天没拉了」。
  **让能说话的那一方报警，而不是指望不会说话的那一方。**

⚠️ **阈值必须跟实际拉取频率对得上**：现在是每周拉，所以 `--offsite-max-age-hours` 取
**192（8 天 = 一周 + 1 天余量）**。定得比拉取间隔短，它会每周都报一次——
**告警一旦变吵就会被忽略，那才是真正的危险。** 以后改成每天拉，记得把这个值一起改小。

（用户 2026-09-16 明确：**暂时保持每周，需要时再说改每天**。）

## 👁️ 看不见的欠账：让「坏了」自己叫出声（2026-09-16，**1.8.0 已上生产**）

> 承上（备份看门狗那批）：同一类缺口还剩四个 —— **坏了，但没有任何人会知道**。
> 这四条谁都不加新功能，只是把「静默失效」一条条堵上。

### A1 存活监控 `tools/check-alive.js`
服务器半夜挂了没人知道，直到有玩家来抱怨。pm2 会自动重启，但**起不来**的时候
（端口占用 / 依赖装坏 / 磁盘满）它就一直躺着。
- **探两个地址**：域名（DNS → Caddy → TLS → 应用，玩家的真实链路）+ 直连 `IP:3000`（只测应用）。
  两个一起报就能区分「应用挂了」和「应用好好的、是证书/反代/中间网络的问题」——
  **这个区分决定你半夜先看哪儿。**
- **200 还不够**：要解析响应里的 `label` 字段确认「回话的确实是本服务」。反代把请求打到别处也会 200。
- **两条防吵**：①连续失败 N 次才报（单次网络抖动不报）；
  ②**只在状态翻转时报，down→up 也必须报** —— 否则你只知道它挂了、不知道它回来了。
- ⚠️ **必须跑在另一台机器上**：进程自己探自己，它挂了就没人探了。现在挂在**测试服、探生产**。

### A2 CI `.github/workflows/ci.yml`
`npm run check` 那道关卡此前**只在我本机 deploy 时**跑。朋友提 PR、我换一台机器改，都绕得过去。
- **Node 20.x + 22.x 双矩阵**：生产是 22、测试服是 20.20.2，
  而 `better-sqlite3` 就因为 Node 版本段错误过一次 —— **只测一个版本等于没测另一台**。
- 另两条是部署事故的直接复发防线：**`CLIENT_BUILD` 必须还是 `__BUILD__` 占位符**
  （被谁写死成某个值 = 全体玩家的「你缓存了旧前端」检测永久失效，而且毫无症状）、
  **`.sh` 里不许有 CRLF**（那 11 天静默备份失败的根因）。

### A4 客户端报错上报 `src/ops/client-errors.js`
前端 JS 报错只在**玩家自己的 console 里**，服务端一无所知 —— 表现出来就是「点了没反应」「卡住了」，
而我们这边日志干干净净。`sizeFor` 拼错、`navigator.clipboard` 在 http 下是 undefined、
截断行把名字吃掉 …… 全是**静默失效**，最后都靠玩家截图才发现。
- `POST /api/client-error` **故意不鉴权** —— 登录页本身也会出错，而那正是最看不到的一类。
  代价是必须当成敌对输入：**按 IP 限流 / 同一条错误去重 / 字段长度硬截断**。
  否则「可观测性」就是一个免费的日志注入 + 磁盘填满通道。
#### CI 上线后的两件事（2026-09-16 推送当天）
- 🔴 **第一次跑就红了，Node 20 挂、Node 22 过** —— 而测试服正是 Node 20.20.2。
  可我只拿得到「exit 1」：**下载 CI 日志需要 admin 鉴权**（排 APK 签名时踩过同一个坑）。
  → 改成**失败时把用例名回显成 `::error::`**，annotation 是公开可读的。
  **读不到失败原因的关卡只算半道关卡**：它能拦住你，却不能告诉你为什么。
- ⚖️ **那次失败没能复现**：测试代码一个字没改，第二次 Node 20/22 全绿；
  测试服（真 Node 20.20.2）也全过。排查后**排除了结构性拖拽源**：
  无共用固定路径、无共用默认库（每处都传了 databasePath）、无固定端口。
  → **结论就是「原因未明的一次性」，不编一个根因出来**。下次再红，annotation 会直接告诉我哪条挂了。
  ⚠️ 但要盯着：**随机变红的关卡会被人忽略**，和「告警变吵就没人看」是同一个毛病。
- **去重不是为了省空间，是防挤占**：一个渲染循环里的错误一秒能抛几百条，
  不去重它会把**别的**错误从缓冲里挤出去 —— 那才是真正的损失。
- **刻意不落库**：错误日志不是数据资产（牌谱才是），不值得为它加一张表、一次迁移、一条清理 cron。
  内存环形缓冲 + `console.error`（进 pm2 错误日志），丢了就丢了。
- 上报自动带**前端构建号** → 一眼看出玩家是不是缓存了旧 JS（和 🐞 反馈自动带版本同一个道理）。
- 管理面板 `GET /api/admin/client-errors` 看最近 200 条。

### A5 崩溃告警 `src/ops/crash-report.js`
现状见上方「进程级兜底」那条的 2026-09-16 更正：崩 → 存快照 → `exit(1)` → pm2 拉起来 → 从快照恢复。
**牌局不丢，但没有任何人被通知它崩过。** `deploy.sh` 的发版后自查只看重启后 12 秒，
半夜崩的看不到；pm2 的 ↺ 计数要 SSH 上去才看得见。
- ⚠️ **不让濒死的进程去发邮件**：发信是异步的，`process.exit()` 会在邮件发出前结束进程 ——
  告警是哑的（审计那条 cron 踩过一模一样的坑）。拆成两步：
  ①崩的那一刻**只做一次同步写盘**（文件放在数据库同目录 = 代码目录之外，部署的 tar 不覆盖）；
  ②**下一个健康进程启动时**把它发出去。顺带跨重启不丢、天然能数出「崩了几次」。
- **崩溃循环要节流**（pm2 一秒重启一次，不节流就是一分钟几十封）；
  **邮件发失败就不清空** —— 清掉发不出去的告警，等于把事故悄悄抹掉。
- 有一条测试是**拿真子进程跑的**：验证「同步写盘确实赶在 `process.exit(1)` 之前完成」。
  这是整个设计的前提，而**单测里没有 exit，改成异步写照样全绿** —— 只能用真进程验。

### 顺带：`tools/` 一直在 eslint 的 `ignorePatterns` 里
也就是说**发告警邮件的那几个 cron 脚本，从来没被 `no-undef` 保护过**。
正是这批要堵的那一类：它坏了不会有人知道（cron 只往日志里写）。
移出忽略列表，现有脚本本来就是干净的。

### ⚠️ 关卡对着自己的说明文档报了警
写完 A4 跑 `npm test`，**剪贴板那条关卡挂了** —— 因为我在 `99-bootstrap.js` 的注释里
写了 `navigator.clipboard`（正是在解释为什么不能用它），而那条断言是裸 `includes()`。
→ **假警报和漏报一样有害**：它逼着人要么去改注释、要么把关卡放宽，两条路都是在削弱它。
改成先剥注释（`codeOf()`）再查。**只剥整行注释和块注释，绝不剥代码行的尾部** ——
宁可少剥（顶多留个假警报），也绝不能多剥（**多剥会把真正的违规藏起来**）。
反向对照两条都咬得住：真调用照报 ✅ / 只写在注释里不报 ✅。

### 关于 UptimeRobot —— ⛔ 用户已定：暂不做（2026-09-21）
A1 跑在测试服上，可**测试服和生产在同一个 AWS 区域**——机房级故障会把探针和被探对象一起带走。
真正的「外部眼睛」得是第三方（步骤留在 `OPS.local.md`，要做随时照着做）。
本机的探针解决的是「进程/证书挂了」，第三方解决的是「整个区域没了」，两者不互相替代。

**但用户 2026-09-21 明确说不用管**：理由是**测试服一般宕机立刻就重启**，
探针的空窗期短到不值得再引入一个外部账号。
→ **别再反复提这条**。要重新提得先有一个新事实（测试服出现过长时间宕机、
或生产真因区域故障失联过），不能拿同一个理论担忧再说一遍。
（9-17 那条「最实在的一次论证」已被这个决定覆盖 —— 那次测试服宕了约一小时，
而**生产恰好没事**，所以它论证的仍是「可能性」，不是「发生过的损失」。）

## 📈 运营指标面板（2026-09-16，**1.8.0 已上生产**）

> ROADMAP 的结论是**核心风险已从「牌局出错」变成「做了没人用」**，
> 可我们手上一个能回答这件事的数字都没有 —— 只能靠「最近好像没人反馈」这种体感。
> 管理面板新开一个 **📈 运营** Tab（`GET /api/admin/metrics?days=N`）。

三条口径上的选择，都是为了让这些数字**敢拿来做决定**：
- 🔴 **日界线显式写死 UTC+8，绝不跟服务器时区走**。迁 AWS 那次差 8 小时就是这个教训。
  跟时区走的话，换一台机器所有历史数字会**整体平移一天，而且不会报错**。
  （顺带一个活例子：开发机在 UTC+3，面板里「今天」显示的是 UTC+8 的日期 —— 这是对的，
  表头也写明了 `日界线 UTC+8`。）
- **活跃的定义拆开显示**：DAU = 当天打过牌 ∪ 当天签到，而**打牌人数 / 签到人数分别也给**。
  DAU 涨了能立刻看出是真有人打牌、还是只是来领了个金币。黑箱指标会被各人按自己的理解解释。
- **留存给「几分之几」不只给百分比**，且**未到期的 cohort 显示「—」不是 0**。
  现在就几十个用户，一个 2 人 cohort 多一个人就是 +50%；而 0 会被读成「一个都没留下」，
  跟「还不知道」完全是两回事。

除了 DAU/手数/新注册/留存，特意加了**两个直接回答「做了没人用」的数**，
它们比 DAU 更该先看：**注册了却一手都没打过**的人数（卡在注册→坐下之间的漏斗，
正是 ROADMAP 预测的冷启动死局）、**只玩过一天**的人数（来了一次就没再回来）。

- 没有活动的那天**必须出现且为 0**：折线中间断掉会被看成「还行」，而那天其实一个人都没有。
- 条形图是纯 div 宽度百分比画的，不引图表库（CSP 只放行几个 CDN，为一排条形图拉个库不划算）。
- 实测：测试服 3251 手 / 2024 场，全部查询 **7ms**。管理面板不翻译（全库唯二不译之一）。
- ⚠️ **一条「反向对照没咬住」的记录**：`metrics.test.js` 里「DAU 不许重复计数」那条，
  我把 `UNION` 改成 `UNION ALL` 它**照样全绿**，把外层 `COUNT(DISTINCT)` 去掉也照样全绿 ——
  因为**两道去重各自都够用**，只有两个一起拆才挂。也就是说单独改任何一道都不是 bug，
  但**没有任何测试在守着其中任何一道**。已就地写明，免得以后有人看见其中一道以为多余就删掉。

## 🗑️ 账号删除 + 牌谱匿名化（2026-09-16，**1.8.0 已上生产**）

> 上架 Google Play / App Store **硬性要求 App 内能删除账号**。
> 但难点从来不是「删」，是**删什么、留什么** —— 第一版怎么定，以后就再也改不回来了。
> 完整策略写在 `src/storage/account-deletion-repository.js` 开头，**动这块之前先读那段**。

### 策略：抹掉身份，不删记录
1. **绝不物理删除牌谱**。一手牌是**好几个人共同的记录**——删了它，同桌另外几个人的牌谱、战绩、
   以及筹码守恒审计的证据链一起没了。「A 要删号」不该让 B 的历史出现空洞。
2. 所以是**抹掉身份而不是删掉行**：邮箱/密码/用户名/显示名/头像/牌友号/站内信/好友关系/
   反馈里的联系方式**全部销毁**，牌局记录原样留下，里面的名字换成**随机**匿名代号。
   留下的 `user_id` 是个 UUID，邮箱和用户名都销毁之后**没有任何东西能把它连回真人**。
3. 🔴 **代号必须随机，不能是原用户名的哈希** —— 用户名是低熵的，哈希能被暴力反查出来，
   那就不叫匿名。而且**同一个用户名注销两次必须拿到不同代号**，否则「同名→同代号」
   本身就是一条能把匿名记录连回真人的线索。
4. **软删除是被 schema 逼出来的，不是偏好**：`foreign_keys=ON` 且 **8 张表外键指向 users**。
   好在 `deleted_at_ms` 早就存在、**读取层已经处处在过滤它**（按 id/用户名/邮箱/牌友号查、
   钱包余额查询全都带 `deleted_at_ms IS NULL`）—— 打上标记的瞬间就登不进、查不到、搜不着。
   顺带保证了**注销前签发的旧 token 立刻作废**（JWT 没过期，但 `requireAuth` 查不到人就 401）。
5. **金币作废**、**邮箱释放**（可以拿同一个邮箱重新注册一个全新账号）。

保留的：钱包流水（经济审计证据链，只有 user_id 无身份信息）、签到记录、
反馈**正文**（还没修的 bug 报告）—— 但反馈的 **contact 和 user_agent 必须清**，
那是玩家真填进去的邮箱/微信/手机和设备指纹。

### 三道闸（`POST /api/account/delete`）
① **还在牌局里就不许删** —— 筹码只存在内存的 `roomGames` 里，结算才换回金币；
   坐着的时候把号抹了 = 座位对象指向一个不存在的人、筹码凭空消失、广播时外键写入报错
   把牌桌静默 paused。**站起围观（`vacatedPlayers`）同样算**，那些筹码也还没结算。
② **必须重新输一次密码** —— 挡的是「已登录设备被人捡到」，不是「忘了自己是谁」。
③ **最后一个管理员不许删自己** —— 删完就再也进不了管理面板，只能 SSH 跑 create-admin.js 救。

### 「立刻生效」+「慢慢擦」
牌谱可能上万手，逐条 JSON.parse/stringify 是同步的，一口气做完会卡住事件循环几百毫秒 ——
而本项目的底线是**绝不能影响正在进行的牌局**。所以拆两步：
**身份销毁在一个小事务里完成**（登录立刻失效，这才是要紧的那半），
**牌谱擦除分批做、每批之间让出事件循环**。
擦除按 userId 找、写成固定代号，**天然幂等**；中途重启也不要紧，
`runStartupTasks` 会在启动 5 秒后把没擦完的接着擦完 —— **不留半抹状态**。

### 验证
- `account-deletion.test.js` 11 条，**两个方向都守**：删得不够（个人数据还在）、
  删过头（把同桌别人的名字也改了 / 把牌局删了）。**反向对照六条全咬得住**：
  不清邮箱 / 连同桌一起擦 / 代号改成确定性的 / 不删好友关系 / 不清反馈联系方式 / 允许重复删。
- `deletion-guards.test.js` 5 条守那道房间闸（含「站起围观也算」的反向对照）。
- 端到端跑真服务器 11 项全通：没密码 400 / 密码错 403 / 未登录 401 /
  最后一个管理员 409 / 注销成功拿到代号 / 原密码登不进 / 原邮箱登不进 /
  **旧 token 立刻失效** / 邮箱已释放可重新注册。
- 🔴 **E2E 当场抓到一个单测抓不到的真 bug**：我写的是 `req.user.id`，而 `requireAuth`
  挂的是 **`req.authUser`** —— 单测根本不经过中间件，全绿；一上真服务器每次请求都 500。
  （同一类的老熟人：`sizeFor` vs `sizeForQuick`。**凡是穿过框架的约定，必须用真请求验。**）

### ⚠️ 顺带：`server.js` 被关卡挡下来一次，而它是对的
我把「启动后续擦牌谱」直接写进了 `server.js`，`server-modules.test.js` 立刻报
「server entry stays an assembly module」（要求 <150 行）。这不是测试碍事 ——
它守的正是朋友那次大重构的成果（3000 行 → 组装入口）。
按它的意思把两件启动收尾活（崩溃上报 + 续擦）抽成 `src/ops/startup-tasks.js`，
`server.js` 从 161 行回到 **143 行**，而且两段注释也不再互相串位。
**关卡让代码变好了，不是让它变麻烦。**

## 🔧 测试服重启后服务没起来 —— 顺藤摸出两个真问题（2026-09-17）

> 背景：部署 1.8.1 时测试服 SSH 连续超时，我当成了「实验室网络又抽了」。
> 实际上是 **那台机器重启了**（`uptime` 显示 3 分钟）。
> 数据与代码都在，但 **`poker-test` 没被拉起来**。

### ① 🔴 pm2 自带的 `pm2 startup` 在 pm2 v7 上生成了一个**跑不起来的单元**
`systemctl is-enabled` 说 `enabled`，但一启动就 `Failed with result 'protocol'`：
单元是 `Type=forking` + `PIDFile=~/.pm2/pm2.pid`，而 **pm2 v7.0.1 根本不生成这个文件**
（`~/.pm2/` 里只有 `pids/` 目录）。
- 改成 `Type=oneshot` + `RemainAfterExit=yes`，只负责开机 `pm2 resurrect`，不去追 PID。
- 生产（`pm2-root.service`）**本来就是好的**，已查：enabled、dump 里有 `poker`。这个缺口只在测试服。
- ⚠️ **「配了」不等于「生效了」**。我先后用错了两种验证方式：
  `systemctl restart` 时 pm2 还活着（进程 pid/uptime 根本没变）、
  `RemainAfterExit` 下单元已 active 时 `systemctl start` **是空操作**。
  最后是真的 `pm2 kill`（服务 curl 000）再让 systemd 独力恢复，
  新 pid + uptime 归零 + 服务 200 —— **这才算验过**。

### ② 🔴 部署后错误日志自查：把「我不知道」悄悄变成了「0」
部署输出里突然吐出一大段旧的【演练】崩溃记录，一度以为告警误报。
查实：`crash-report.json` 是空的、日志里「已告警」只出现过 **1 次** —— 崩溃告警没问题。
真因在自查自己：取基准时 `poker-test` 还不在 pm2 列表里（重启后没起来）
→ `ERRLOG` 取空 → `echo 0` → **从字节 0 开始读整个历史日志**。
- 修：取不到就写 `unknown`（并先退回约定路径 `~/.pm2/logs/<app>-error.log` 试一次），
  比对时碰到 `unknown` 就**明说跳过**，绝不猜。
- 教训：**一个会喊狼来了的自查，和吵闹的告警是同一个毛病** —— 很快就没人看了。
  而这条自查是本项目找出两个经济漏洞的那个，不能让它变成噪音。

### ③ 顺带暴露的一个结构性弱点
**生产的存活探针就跑在测试服上**。今晚测试服宕了约一小时，
那段时间生产是**无人监控**的（恰好生产没事，所以什么也没错过）。
这是对「还得有一个第三方探针（UptimeRobot）」最实在的一次论证 ——
当时只是理论上的担忧，今晚它真发生了。
→ ⛔ **但 2026-09-21 用户已定「暂不做」**（测试服宕了一般立刻就重启）。见上方 UptimeRobot 那节。
## 🕶️ 牌谱可见性：揭示改到整桌结束后（2026-09-17，1.8.1）

> 用户问了一句「这个日志有没有暴露的风险」。日志没问题（只打 commit 和 nonce，
> 而 commit 本来就广播给全桌；全库没有任何一处 log 过种子/牌序/底牌）——
> **但顺着这个问题查出了一个真漏洞。**

### 漏洞本身（早就存在，1.8.0 又多开了一扇门）
`/api/my-hands` 直接返回整份 `payload_json`，里面有**同桌每个人的底牌**。
客户端 UI 会把弃牌者的牌盖上，但**网络响应里是有的** —— F12 一按全看见。
1.8.0 的 `fair.serverSeed` 又提供了第二条路：**知道种子就能重算整副牌**。
- 危害是**竞技公平**而不是实时作弊：牌谱只在这手打完后才落库，
  所以**查不到进行中的那一手**。但「你弃牌从没亮过，对手事后能查出你拿的什么」= 免费 HUD。

### 绕不开的张力（值得记住）
**可验证公平要求公开种子，而种子公开 = 整副牌公开 = 弃牌者的底牌也公开。**
这两件事天然打架。给用户的三个选项里他选了 **A：揭示延到整桌结束**。
理由：改动最小、语义干净（「这一桌打完了，一切公开」），**且不损害可验证性** ——
承诺在发牌前就公开了，晚点验一样算数。

### 做法
- `hand-visibility.js`：桌子没散 → 只给【本来就该看见】的（判定**与客户端同源**：
  `showFace = isMe || (!folded && showdown)`）；桌子散了 → 原样返回。
  **两边写成两套规则的话，迟早会出现「界面显示了但数据被删了」的怪状。**
- 牌型名也要过滤 —— 它是从底牌算出来的，留着等于泄露一半（「他弃的是同花听」）。
- 不再逐手广播 `lastReveal`；`fairRevealAfterTable` 告诉面板该怎么说。
- 老牌谱（找不到 match 行）**当作已结束** —— 那些桌子早散了。

### 验证
10 条单测，**六条反向对照全咬住**（不过滤 / 底牌不过滤 / 牌型不过滤 / 种子照发 /
一律当摄牌 / 老牌谱当没散）。端到端跑真服务器 10 项全通：
进行中看不到对手弃牌 / 种子与牌序不发 / 承诺仍在 → 解散后全部公开 →
**导出的牌谱仍能通过离线验算**（这条最要紧：证明延后揭示没把功能做死）。

⚠️ 一条契约被**故意反转**：`fairness-state.test.js` 原来断言「揭示要广播」，
现在断言「一条都不许广播」。改断言不是为了让测试变绿 —— 是设计真变了，
而且新断言比旧的**更严**。
## 🔒 可验证公平 provably fair（2026-09-16，**1.8.0 已上生产**）

> 解决的是一个**我们自己没法自证清白**的问题：洗牌其实早就很干净了
> （每手重洗 + Fisher-Yates + crypto CSPRNG），可**玩家没有任何办法验证这一点**，
> 他只能选择相信我。朋友局里「这牌怎么又是你赢」是玩笑，但要做成商业化产品，
> 「相信我」不是一个能一直用下去的答案。

### 承诺-揭示（commit-reveal）
① **发牌之前**公布 `commit = SHA256(serverSeed)`（房间一建好就有，第一手也不例外）；
② 整副牌完全由 `(serverSeed, clientSeed, nonce=handSeq)` 决定性算出，没有别的随机源；
③ 这手打完之后公布 `serverSeed`，并连同**整副牌序**一起写进牌谱。
→ 任何人都能离线验算：`SHA256(公布的种子)` 是否等于开局前那个 commit、
  重新洗一遍是不是同一副牌、以及**那副牌能不能解释你当时真的看到的牌**。
**服务器在 ① 之后就被自己锁死了** —— 看到底牌之后想换牌，commit 就对不上。

`tools/verify-hand.js` 是给玩家的离线验算器：**不连服务器、不查库、只用标准 SHA-256**，
任何人都能照着那几十行用别的语言重写一份来对答案。牌桌菜单 ☰ 里有「🔒 公平性验证」。

### ⚠️ 这批动的是发牌随机性 —— 两条铁律
1. 🔴 **绝不能引入模偏**。原来的 `crypto.randomInt(n+1)` 自带无偏保证；换成自己的种子流后，
   若图省事写 `字节 % (n+1)`，牌会**系统性偏向前面几张**，而且**看不出任何异常**。
   所以用**拒绝采样**：把 2^32 砍成 n 的整数倍，落在尾巴上就重取。
   - ⚠️⚠️ **这一条不能用统计测试来验**：n=52 时模偏是 **1e-8 量级，跑几百万次采样也测不出来**，
     那种测试只会给人「测过了」的错觉。→ 把取样逻辑单独拿出来、**字节源做成参数**，
     测试里**精确地喂一个落在拒绝区间里的值**，直接看它有没有重取。反向对照（改回取模）当场咬住。
2. 🔴 **本手的 serverSeed 在这手结束前绝不能出现在任何发给客户端的东西里** ——
   泄漏等于把整副牌提前告诉所有人，**比不做这个功能糟糕一万倍**。
   `state-presenter` 只放 commit；`fairness-state.test.js` 专门守这一条（反向对照两条都咬住）。

### 重新跑了「发牌随机性」的自检（本文件写明的公平性底线）
60000 副 × 52 个位置 = 2704 个格子，与**原来的 crypto 洗牌同口径对照**：

| | 最大偏差 | 平均卡方 (df=51，期望≈51) |
|---|---|---|
| 种子洗牌（新） | 4.02σ | 53.8 |
| crypto 洗牌（原基准） | 3.83σ | 51.9 |

→ 两者同一量级，未见系统性偏差。
⚠️ 第一次跑的时候我按「最大偏差 < 6%」判，结果报了「❌ 分布可疑」——
**是判据错了，不是洗牌错了**：2704 个格子里取最大值，本来就期望落在 3.4σ 上下（≈10%）。
卡方 52.0 对 51 才是那次真正该看的数字。**拿一个没校准过的阈值去判随机性，结论必然是噪声。**

### 端到端（用真引擎打一手真牌，再拿验算器去验它）
必须这么验的理由：验算器里写死了一套发牌顺序的假设（每人两张 → 烧 → 翻牌 3 → 烧 → 转 → 烧 → 河），
这个假设要是和 `hand-service` 真实的顺序对不上，**单测再多也发现不了**。
实跑结果：**发牌前广播里的 commit == 事后揭示的 commit** ✅ / 广播全程无种子与牌序泄漏 ✅ /
验算器三项全过（2 家底牌 + 5 张公共牌全对得上）✅ / 改掉一张公共牌验算器立刻报错 ✅。

**端到端顺带抓出三个单测抓不到的问题**：
- `player_action` 的负载**必须带 roomId**，少了服务端直接 return —— 表现是「机器人一直不动、
  全靠超时弃牌」，**没有任何报错**。（我的测试机器人就这么静默失效了一轮。）
- 牌局结束后 `fair.nonce` 忘了往前走一格，导致广播里 `fair.nonce == lastReveal.nonce`，
  **看上去就像「当前这手的种子已经被揭示了」**。实际没泄漏（commit 已经是新种子的），
  但这个广播本身是误导的：玩家和自动检测都分不清这个 commit 到底对应哪一手。已修。
- 我自己的泄漏检测第一版**误报**：一看到 `deckOrder` 就报警，可 `lastReveal` 里的
  `deckOrder` 是【上一手】的、**故意公开**的。改成精确判据：
  「揭示的种子哈希之后等于**当前** commit」才算泄漏。

### ✅ 生产首次真实闭环（2026-09-21）
之前只在测试服和本地验过。生产现已累计 **516 手带可验证公平数据**，
取房 498416 第 325 手（2026-09-21 02:23:51，4 家、走到河牌）实跑了一遍，证据链：

1. **发牌当时**的服务器日志：`[deal] 房间 498416 … commit=4225d17f6adf… nonce=325`
   —— 这是【牌局结果还不存在时】写下的。
2. **打完之后**牌谱里才公布 `serverSeed`。
3. 🔴 **用完全独立的实现核对**（python `hashlib`，不碰本项目任何代码）：
   `SHA256(serverSeed)` = `4225d17f6adf65e0…`，与发牌时日志里那个 commit **完全一致**。
   这一步最要紧 —— 它证明**不需要相信我们自己写的验算器**。
4. 用种子重洗，得到的牌序能解释 **4 家底牌 + 5 张公共牌**。
5. **反向对照**：改掉第 3 张公共牌 → 立刻报错并指出「按牌序应当是 6D」，退出码 1。

→ 至此「发牌前承诺 → 事后揭示 → 第三方可独立重算」在**真实生产牌局**上走通了一次。

### 这个方案证明了什么、没证明什么（别吹过头）
- ✅ **证明了**：服务器没法在看到任何人的牌之后改变牌序 —— 这是玩家真正担心的那件事。
- ⚠️ **没证明**：服务器不能在 commit **之前**反复试种子挑一个对自己有利的（grinding）。
  防这个要靠「玩家在看到 commit **之后**才确定 clientSeed」。现在 commit 已经做到
  **提前一手公布**（每手揭示后立刻滚出下一手的承诺），**钩子留好了**，
  但还没做玩家自选 clientSeed 的入口 —— 目前 clientSeed 就是房间号。
  朋友局里这条偏理论，但**写清楚比含糊过去重要**。

## 🌍 用户来源：地区分布 + 同网段多账号线索（2026-09-21，**1.9.0 已上生产**）

> 用户问「能不能看到用户都分布在哪里」。当下的用途是好奇（现在全是朋友或朋友的朋友），
> 真正的用途在后面：**防同 IP 多账号**。

### 记录
注册**和登录**都记来源 IP（`005-login-events.sql`）。
🔴 **登录也记是关键**——现有 69 个用户的注册 IP 已经**永久丢失**，
只能靠他们下次登录补齐。这个功能从上线那一刻才开始积累，补不回过去。

几个不显然的取舍：
- **独立一张表，不是给 users 加一列**。一个人会登录很多次，加列只能存最后一次——
  换个网络登录一次就把之前的痕迹盖掉了，而「他从哪些地方登录过」正是要看的东西。
- **同网段按【独立账号数】算，不是事件数**（`COUNT(DISTINCT user_id)`）。
  否则一个人登录 50 次就成了「50 个账号」，整张表全是噪音。
- **IPv6 按 /64 聚合，IPv4 按 /24**。IPv6 完整地址按设备分配且会随隐私扩展变，
  不聚合的话同一个人每次登录都像一个新来源。
- 🔴 **取不到就存 null，绝不编一个假的**（比如 `0.0.0.0`）。
  编一个的话，所有取不到的人会被聚成「同一个 IP」——那是在凭空制造假线索。
- **记录失败绝不连累登录**：`record()` 吞掉一切异常只打日志。这是运营数据不是业务数据，
  为了记一条来源让人登不进去是本末倒置。

### ⚠️ 「同 IP ≠ 同一个人」写在三处
SQL 注释、代码注释、面板界面各写了一遍，查询函数也刻意叫 `sameNetworkCandidates`
而不是 `matches`。这里误报会特别多：同宿舍 / 同公司 / 同一个家，
手机 **CGNAT 更是成千上万人共用一个出口**。
→ **返回的东西一律当「待人工复核」，别在调用处写成自动处置。**

### 隐私
- 保留期 **180 天**（`tools/prune-login-events.js`）。IP 是个人数据，不能无限期留着。
- 🔴 **走 cron 不走进程内定时器**——删除不可逆，放进程里意味着「进程活着才会清」，
  而进程崩了/重启了这件事没人保证。（同一个理由让备份、审计、存活探针也都走 cron。）
- 🔴 **注销时连根拔掉**（`forgetUser`，已接进 `anonymizeIdentity`）。
  不能只抹掉 user_id 留着 IP——**那条 IP 仍然指向一个真人**，跟时间戳一拼就能反推回去。

### 🔴 内存：为什么是一次性子进程，不是 require
`require('geoip-lite')` 一次就吃 **+108MB 常驻内存**（实测 32MB → 140MB），
而生产是 **1GB 的 t3.micro**、游戏进程本身才 ~57MB。
在服务里加载它 = 平时白占十分之一内存，只为一个管理员偶尔看一眼的功能。
→ `tools/geo-lookup.js` 做成**一次性子进程**：查完就退、内存立刻还回去，
结果缓存进 `ip_geo` 表，**每个 IP 只查一次**（查不到的也写一行，否则会反复重查）。

### ⚠️ 一个我自己造的静默失效（这批最该记住的）
部署到测试服后地区全是「未知」，查了很久。**两层静默叠在一起**：
1. **npm 那层**：optional 依赖安装失败本来就是静默的；而那台机器 npm 状态还坏了——
   lock 里有 geoip-lite、`npm install` 说 `up to date`、目录却根本不存在，**退出码一律 0**。
   （`rm -rf node_modules && npm install` 才修好。）
2. **我自己那层**：CLI 查不到就返回 null，面板显示「未知」，**不说为什么**。

第二层是我的问题，而且**正是这个项目一路在反对的那一类**（见「看不见的欠账」）。
→ 改成：CLI 用**退出码 3** 明确报「库没装」，服务端转成 `geoUnavailable`，
面板顶部黄字说明原因和修法，并注明**「来源 IP 照常记录，不受影响」**。
依赖仍保持 optional（111MB 下载失败不该挡住游戏本身的部署），但**降级现在是可见的**。

### 上生产时顺带发现：生产内存已经很紧
部署前查到生产 **RAM 88.6% / swap 已用 1.4G**，最大占用者是用户自己在跑的
`peakgto_fetch.py`（**461MB**，是游戏进程的 8 倍）。
→ 因此这次**故意不让 `npm install` 去装 geoip-lite**（服务器上临时放一个
`.npmrc` 写 `omit=optional`，部署完就删）。理由：geoip-lite 的 postinstall 要下载并解析
111MB 数据，在只剩 119MB 可用内存的机器上**可能触发 OOM killer，而它挑的是 RSS 最大的那个进程
——也就是用户跑了一个多小时的抓取任务**。
**功能本身不受影响**（IP 照常记录、同网段照常识别），只是地区显示「未知」+ 顶部黄字说明。
等那台机器空下来再单独 `npm install geoip-lite` 即可。
→ **这正是「让降级可见」当天就兑现了价值**：换成静默版本，我根本不会知道少了什么。

### 🔴 上完生产当场又抓出一个同类 bug（1.9.1）
验收时读 `/api/admin/sources` 的代码，发现库没装时**仍然把那批全 null 的结果写进了 `ip_geo` 缓存**。
而 `unresolvedIps` 的判据是「`ip_geo` 里有没有这一行」，不是「country 是不是空」——
**写进去就等于永久放弃重查**。后果：只要管理员在装上地理库之前点开过一次这个页面，
那批 IP 就被**永久钉死成「未知」**，以后装了库也查不回来。而生产此刻正好就是「库没装」的状态。

根子是 **null 有两种含义被混成了一种**：
- 「**查过了，查不到**」→ 该缓存（否则每次都重复去查同一批）
- 「**没法查**（库没装）」→ **绝不能缓存**

把后者当成前者，就是又一次「我不知道」被悄悄写成了一个确定答案——
和部署自查那次取不到基准就 `echo 0` 一模一样。
→ 修：`if (r.stdout && !geoUnavailable)`。
→ 关卡两条：①仓库层把「写了 null 行就没有重查机会」这件事**钉住**（这条不是在要求那种行为，
是在说明调用方为什么必须自己拦）；②结构性断言 `saveGeo` 必须被 `!geoUnavailable` 挡住。
**反向对照咬得住**（撤掉守卫立刻报出原因）。

⚠️ 装上 geoip-lite 之后，如果此前已经缓存过 null，要先
`DELETE FROM ip_geo WHERE country IS NULL` 再打开面板，否则那批 IP 不会被重查。

### 验证
- 单测 23 条（`client-ip.test.js` 12 + `login-events.test.js` 11），**七条反向对照全咬住**。
- 端到端跑真服务器 16 项全通（`req.authUser` 那个教训：**凡是穿过框架的约定必须用真请求验**）：
  代理链取最左边那个 / 不把代理地址当客户端 / **登录失败的那次不记** /
  不把 127.0.0.1 当真实来源 / IPv6 按 /64 聚合 / 同网段两账号被识别 /
  **注销后该用户记录清零且没连累别人**。

## 📋 待办清单（2026-08-11 复核，仅列**还没做**的）
> 用户明确**不做**：破产救济、现金桌固定升盲（改成下面的动态改盲注）、优雅关停（重启已能恢复牌局）。
> **📌 2026-09-07 改主意：平台上架重新纳入考虑**（原本列在「不做」里）。动机是 ①扩量 ②增加收入（纯游戏币充值 / 激励广告），且**目标用户不限中国境内**（因此 ICP 备案 / 版号这两个制度性卡口消失）。
> **商业化与上架的完整路线、红线边界、执行顺序、上架前必做技术项，全部记在私有 `BIZ.local.md`（gitignore，不入库）** —— 本仓库是 public，收入模型与上架决策不放这里。做上架相关改动前先看那份。
> **📌 2026-09-07 产品评审 → 私有 `ROADMAP.local.md`**（**2026-09-16 已对账更新**：§0.1 进度快照、§7 重排顺序、新增 §10 「评审时没记录的欠账」）：带优先级与工作量估算的完整路线图。结论摘要——**工程质量领先产品完成度，核心风险已从「牌局出错」变成「做了没人用」**；P0 是**冷启动死局**（大厅空 + 列表只观战导致陌生人永远无法入座 + 无机器人 + 无好友系统），解法是**公开桌/私密桌二分 + 规则机器人补位 + 游客模式**。下面这份按功能编号的清单继续有效，两者的关系：本清单是**玩法功能待办**，ROADMAP 是**产品优先级**，动手前先看 ROADMAP 的执行顺序。
> 本次复核逐条查了代码，把已完成的从清单里摘掉了（见下方「已完成，已从清单移除」）。
>
> **📌 2026-09-06 更新（1.3.0 已上香港生产）**：本轮完成并上生产 —— **#3 音效分场景** ✅、**#4 发牌逐张翻出** ✅、**#10 中英文切换** ✅（part1/2/3 全做完，含创建表单/牌桌/行动按钮/结算/颁奖台/收件箱结算消息/12 条聊天梗英文版；`t()` 空串回退 bug 已修；服务端发的单位「筹码/金币」与标题原因在客户端映射掉。**唯一不译：管理面板 `90-admin.js`（用户说不做）+ 房间名（房主自输内容）**）。**故 #3/#4/#10 视为完成。** 剩余见下（#1、2、5、6、7、8②、9、11、12）。
> **✅ 恢复快照排查结论（2026-09-06，之前的"空快照"是误报）**：进行中的局**每次 `broadcastState` 都写快照**（`state-presenter.js` → `game-persistence-service` → `match-repository` INSERT `active_match_states`）。上线时我查到"空"是**查询列名写错**（写了 `updated_at`，实际是 `updated_at_ms`，`2>/dev/null` 吞了报错）。**底线安全，重启能恢复，没有洞。**
> **✅ 已修（2026-09-06）：僵尸局——全员掉线还一直空转发牌/涨盲**。用户实拍：两人开了 SNG 后都退出，牌局一直发牌涨盲不结束，只能手动解散。
> - **根因**：`canPlay(p)=chips>0 && !sittingOut` **没算 `away`（掉线）** → 掉线玩家仍算 live，`scheduleNextHand` 只看 `liveCount>=2` 就继续开新一手；heads-up 两人互交盲注谁也不快速破产 → 无限空转。
> - **修 ①（停空转）**：`scheduleNextHand` / `restoreNextHandTimer` 增加 `connectedLive = players.filter(chips>0 && !sittingOut && !away).length` 守卫——**全员掉线就不再发牌**（停在 SHOWDOWN），有人重连即续。重连恢复分支从「仅 cash」放宽到 **cash+sng**（否则 SNG 暂停后重连不会续局）。重启恢复同样受益：`game-hydrator` 本来就把恢复出来的玩家标成 `away:true`，所以重启后没人连也不会空转。
> - **修 ②（自动解散，用户提的）**：**掉线也挂空房清理**（原来只在主动退出时挂）——`disconnect` 里调 `scheduleEmptyCleanup`，3 分钟宽限**到点再查 `io` 房间还有没有连接**：**只要还有任何人连着就直接不解散**（用户强调的"绝不能把还有人在玩的房解散"由这道 fire-time 复查保证），并且**重连时清掉该计时**（membership-service），playing 中的房不残留待解散计时。全员离线到点才按房型结算：现金→`endCashTable`（筹码兑回金币）、**SNG→`dissolveNow` 路由到 `dissolveSngRoom`**（原来 `scheduleEmptyCleanup` 对 SNG 会误用现金结算，一并修了）。宽限支持 `EMPTY_GRACE_MS` env 覆盖（仅为测试）。
> - **验证**（`zombie.js`）：①正常发牌 ②全员掉线后手数不再增长（停空转）③**有观众连着时绝不解散** ④重连后恢复发牌 ⑤真·全员离线到点自动解散（事后 join 不进、也不在大厅列表）——全通；standflow/hostfeat/sngdiss/sngleave/sidepot/adminroom 回归全通。（`reconnect.js` 失败是**过期测试**：BASE 指向测试服、且用 `join_room` 让第二人入座，早已改为「下场必须用房间码」。）

**玩法**
1. **房主中途修改盲注（需全体同意）** —— 牌局进行中房主发起改盲注 → 所有在座玩家同意才生效（下一手起）。
2. **AI 对战**（练习 / 凑人 / 补空座）—— **路线与接入清单记在私有 `AI.local.md`**。
   本仓库是 public，这部分属于自己的 roadmap，不公开（同 `BIZ.local.md` 的做法）。
   动这块之前先看那份。

**UI / 体验**
0. **「发现」页要能让人做决定（2026-09-16 用户「先记下」）** —— 公开桌已上线，但发现页还是
   「房名 / 人数 / 盲注」，**回答不了「我该不该进这张」**。补：空位数、平均筹码深度（BB）、
   是否进行中；排序按「有空位 > 人多 > 盲注配得上我的金币」；空态给显眼的「开一张公开桌」入口。
   不做这个，公开桌的收益是打折的——能进去了，但还是不知道该进哪张。约半天。
   （同轮讨论里「有人开桌就通知在线牌友」用户明确说**暂时没必要**，不列入。）
3. ~~**🔊 音效分场景细化**~~ **✅ 已做（2026-08-28，测试服）** —— 之前 bet/raise 共用 `sndBet`、且没有收池音。加了 `sndRaise`（三连上扬，区别于下注）和 `sndPot`（下行三音，本街结束筹码归拢时 `20-socket.js` 里 `potCollected` 只响一次）；`playSfx` 里 raise→sndRaise、新增 pot。call/check/fold/allin/win 本就各有音。
4. ~~**🃏 发牌动画：翻牌不要一次性蹦出 3 张**~~ **✅ 已做（2026-08-28，测试服）** —— 公共牌新发的牌改用 `flip-in`（rotateY 翻牌）代替 `deal-in`，逐张 stagger 从 140→**190ms**，flop 清晰地 1→2→3 翻出，转/河同理；`sndFlip(i)` 本就按 i 错开。⚠️ 固定 5 槽位 `emptySlot()` 占位**保留**（i≥comm.length 仍占位），发牌不跳动。
5. **🎭 表情与互动系统（持续优化方向，用户点名「用得最多」）** —— 连发 / 动态表情已做，后续想更多互动形式。
6. **头像自定义上传** —— 内置 27 个已换新；上传要考虑存储与审核。
7. ~~**收件箱 / 结算面板继续美化**~~ **✅ 已做（2026-09-06，1.3.2 已上生产）** —— 结算面板改道场绿氛围底 + 深绿金边卡片 + 入场动效 + 奖杯金色辉光；排名行加渐变描边、「我」那行金色高亮+外发光；返回按钮金色渐变+悬停上浮。收件箱条目加渐变/描边/投影/悬停微浮，**未读由告警红改金色**（顶栏红角标已负责未读计数），侧栏（收件箱/战绩/牌谱共用）背景跟随道场绿。
   - ⚠️ **踩坑记**：颁奖台 DOM 里**本来就有 `.pd-step` 台阶**（见 `renderPodium` 的 `cell()`），我没先看 DOM 就又用 CSS `::after` 加了一层 → **每项两个台阶块、中间被 flex gap 断开**，还挤乱了名字/盈亏（玩家截图圈出）。**教训：改渲染样式前先读它真实生成的 DOM。** 已移除并在 CSS 原处留注释防复发。
8. ~~**🎬 进入游戏的第一印象太单薄**~~ **✅ ①② 均已做（②于 2026-09-06，1.3.1 已上生产）** —— ② 的做法：登录页从纯黑底换成**道场绿氛围场**（深绿径向渐变 + 两团缓慢漂移柔光 + 极淡绿呢斜纹 + 暗角），登录框改深绿玻璃+金边+入场上浮，logo 金色辉光+放大入场，带 `prefers-reduced-motion` 降级；大厅底改**顶亮底暗纵深渐变**（`body:not(.in-room)`，不影响牌桌）；大厅入口卡与房间列表卡加渐变/内高光/投影/悬停上浮。原始需求记录如下：
   - **① 启动那几秒的空白（薄壳最明显、收益最大，先做这个）**：APK 点开 → 白屏 → 突然出现登录页。根因是 `capacitor.config.json` **没有配 SplashScreen 插件**，而薄壳每次启动都要去加载 `https://pokerdojo.space`，这段等待完全没有承接。做法：原生 splash（深绿 `#2f4a3e` + 现成的 `mobile/assets/logo.png` 鸟居扑克）与**网页首屏用同一套配色/同一个 logo**，让两层看起来是一个连贯过程而不是两次跳变。
   - **② 登录页 / 大厅缺「场」的氛围**：现在是纯功能布局（登录页已有 logo+标题，大厅是两张卡片+房间列表）。给个视觉锚点即可——有质感的背景（牌桌绿呢 / 鸟居剪影 / 缓慢流动的光）+ logo 入场动效，不需要复杂动画。
   - ⚠️ **必须遵守的约束（否则弄巧成拙）**：薄壳每次启动都会重新加载线上页面 → **开场动画每次都会看到**，再惊艳的东西看第三次就是负担。所以：**短（≤800ms）**、**点一下可跳过**、**只在冷启动播**（切后台回来 / socket 重连不播）、**与加载并行**（不能"先播完再开始加载"）、**已登录用户直接进大厅**别多等。
9. 加注条手机端彻底解决（用户定：留到做 App 时换自定义指针拖动条）。
10. ~~**🌐 中英文语言切换**~~ **✅ 已全部做完（2026-09-10，并已加关卡）** —— 详见上方「双语排查」一节。原记录： —— **已做**：i18n 引擎 `public/js/03-i18n.js`（`I18N` zh/en 字典 + `t(key)` + `applyLang()` 扫 `data-i18n`/`data-i18n-ph`/`data-i18n-title` + `setLang()`，localStorage 持久、首访按 `navigator.language` 自动判），设置面板加「语言 中/English」开关；已接入 **part1**（登录/注册/找回页 + 大厅入口卡 + 顶栏 tooltip + 设置面板）+ **part2（2026-08-29）**：创建房间表单（SNG+现金桌，滑块标签用「前缀 span + 单位 span」包住 data-i18n，避开 `<b>` 动态值）、桌内菜单、聊天、行动按钮（`下注…/加注…/弃牌/预操作` 用 data-i18n；`过牌/跟注/确认下注` 是 JS 每帧写的 → 在 `80-table-renderer.js`/`70-actions.js` 里改用 `t()`；`setLang` 里切完语言 `render(lastState)` 一次让它立即刷新）、收件箱/签到/反馈弹窗、底部控制栏 tooltip。`setLang` 持久化 + `navigator.language` 自动判。校验脚本确认 118 个 data-i18n key 中英字典全齐；jsdom 中英切换回归通过。**part3（2026-09-02，动态层起步）**：动态 JS 文案改用**内联 `L(zh,en)`**（翻译写在调用处，不为每条散串建 key）——已接入：**房间列表卡片、所有 toast（20-socket/30-room/61-voice/70-actions/99-bootstrap/50-audio 共约 20 条）、多次发牌协商面板、结算面板 + 颁奖台、主要 confirm 弹窗（离开/站起/强制站起/重置邀请/加时）**；`setLang` 里也重渲染房间列表（`window._lastRooms`）让切换即时生效。**待续**：`renderMatchInfo`（比赛设置详情）、买入/补码 modal、生涯统计/牌谱回顾、签到面板 body、收件箱渲染、管理面板（`90-admin.js` 优先级最低），以及零散 confirm/label——继续用 `L()` 逐块补。⚠️ 工作量不小 + 有个结构性坑：**服务端 emit 给玩家看的桌面文案（弃牌/加注/赢池…）目前全是中文硬编码，且是一条广播发给全桌**——同房两人想要不同语言，一条广播满足不了。建议分层做：①**先做纯客户端静态 UI**（登录/大厅/设置/按钮/标签/收件箱模板，抽成 zh/en 字典 + `t(key)`），收益最大、风险最小；②服务端桌面提示要么改成发**结构化 key+参数**让各客户端本地渲染（大改，可参考 `nameOf` 那种集中改法），要么这批先保持中文/双语。**别一上来就想全量翻译。**

**管理 / 运维**
11. **管理员功能继续扩充** —— 用户评价「目前只是大致框架」。可加封禁 / 解封、批量操作、审计定时报表、更细的牌谱详情。（房间「免密进入/强制解散」已加，见管理员批次。）
   - ~~⚠️ 具体缺口一例：**管理员没法给牌桌发实时公告**~~ **✅ 已做（2026-09-16）**。原记录：现有 `/api/admin/broadcast` **只写站内信**，正在打牌的人当场看不到（2026-08-10 要通知玩家重启时撞上过，只能靠站内信 + 口头说）。应补一个「向房间广播 `table_notice`」的入口。
12. **最大房间数限制**（按服务器内存配置）。
13. ~~**🕐 时间显示要脱离服务器时区**~~ **✅ 已做（2026-09-14，1.6.0）**：服务端现已一处时间格式化都没有，广播改发剩余时长 `{ms|dur}`、绝对钟点走 `{at|time}` 由客户端按自己时区渲染。原记录：—— `src/socket/events/table-control-events.js` 里房主调整结束时间后广播的「⏱ 预计结束时间 HH:MM」用的是 `toLocaleTimeString('zh-CN', …)`，**跟服务器系统时区走**。迁到 AWS（默认 UTC）时直接差了 8 小时，靠把服务器时区设成 `Asia/Shanghai` 临时对齐了。正解是**下发时间戳、让客户端本地格式化**——走向全球化后玩家不都在中国时区，硬编码任何一个时区都是错的。（✅ 签到的日边界不受影响，那段是显式 `Date.now()+8*3600*1000`，写法正确可参考。）
14. ~~**📉 备份失效要能主动告警**~~ **✅ 已做（2026-09-16，见上「运维两件」）**。原记录： —— 2026-09-08 的教训：备份 cron 因 CRLF **静默失败 11 天**没人知道，因为它只往 `backup.log` 里写、没人读。已在**本机每周拉取脚本**里加了「服务器最新备份超过 48h 未更新就报错」的探针，但那每周才跑一次。应在**服务器端的审计 cron**（每天 04:30，本来就有发信能力）里也加一条：检查最近 24h 有没有产出备份，没有就发告警邮件。**一个只写日志、没人读的检查等于没有检查。**

> **已完成，已从清单移除**（2026-08-11 复核逐条确认）：链式 straddle（并修了「同一人吃两档」）、表情连发 + 动态表情、牌面 4 种样式（原来那个死开关已修）、牌谱滚动到底自动加载、牌谱每家牌型名、收件箱结构化、筹码下注飞入动画（`20-socket.js` 里按下注额变大触发）、显示「下注后剩余筹码 / 占底池 % / 对手需跟」、9-max 紧凑度（布局两轮重做：水印回桌心、座位精确反解半径、公共牌按几何算尺寸）。

## 🎁 玩家反馈 P2 + 管理员功能 + 若干修复（2026-08-08 下半场，测试服已验收）
- **结算颁奖台**：🥇老板(亏最多) 🥈MVP(赢最多) 🥉力工(手数最多)，纯娱乐调侃；少于 2 人不评、必须真亏/真赢才评老板/MVP。`buildRanking` 补 `handsPlayed`/`avatar`，新增 `buildAwards`。收件箱从「只有一行第几名/盈亏」补成 手数 + 本场称号 + 完整排名。（称号后来按用户要求去掉了括号说明。）
- **横竖屏切换**（设置 → 屏幕布局：自动/竖屏/横屏）：⚠️ **真正的原因是 `body { max-width: 720px }` 把整个应用锁在中间一条**，所以最初只改牌面大小根本没用。横屏解除该限制让牌桌铺满页面；`auto` 按视口宽高比 ≥1.3 判定并监听 resize。
- **头像库换新**：删旧 a1–a12（玩家嫌丑），新增 **b1–b27** 扑克+道场主题（`tools/gen-avatars.js` 生成）。`scripts/clear-legacy-avatars.js` 把指向旧头像的用户清成 null → 回退首字母色块。⚠️ **`tar` 解包不会删除服务器上已移除的文件**，部署后要手动 `rm avatars/a*.svg`。
- **改名**：用朋友 PR 里的显示名方案（2–16 字符、保留字、24h 冷却），未改成金币计费。
- **解散等本手打完**：房主点解散时若牌局进行中 → 挂 `pendingDissolve`、提示「本手打完后解散」，本手结束才真正结算/解散。SNG 解散逻辑抽成 `sng-match-service.dissolveSngRoom` 供两处共用。
- **🔴 全押亮牌后仍能弃牌（经济漏洞）**：`advanceStage` 全押亮牌分支只设了 `allinRevealed`，**没清 `actionOnIdx`** → 仍指着最后行动者 → 他按钮亮着、服务端回合校验也放行、`fold` 分支又没全押判断 → **已全押的人能把牌弃掉、白丢池权**（run-it 协商时最明显）。修：①亮牌时 `actionOnIdx=-1`；②`player_action` 增加 `!canAct(player)` 兜底拒绝（不依赖客户端藏按钮）。与「全押者离场被误判弃牌」同属一类，两条路径现已都堵上。
- **管理员功能**（⚙️ 面板 6 个 Tab）：玩家 / 房间总览 / 钱包流水 / 牌谱 / 审计 / 站内信。
  - 房间总览 `GET /api/admin/rooms`、钱包流水 `GET /api/admin/wallet/:username`、牌谱 `GET /api/admin/hands/:username`（服务端顺带抽出被查者自己的底牌与净盈亏，前端别再去 seats/results 里翻）、网页审计 `GET /api/admin/audit`（**复用 `tools/audit-chips.js --json`，不另写判定逻辑**，保证网页/cron/手跑三处结论一致）、站内信 `POST /api/admin/broadcast`（单人或全体）。
  - **房间总览加了两个管理动作（2026-08-28，测试服）**：每个房间「▶️ 免密进入」+「🛑 解散」。socket 事件 `admin_join_room`/`admin_dissolve_room`（`src/socket/events/admin-room-events.js`，均校验 `socket.user.isAdmin`）。免密进入=绕过房间码/`entryLocked` 直接 `authorize`+`playRoom`+`handleJoinRoom`；解散=复用房主解散逻辑（进行中挂 `pendingDissolve` 等本手打完，现金桌 `endCashTable`、SNG `dissolveSngRoom`）。E2E：免密进入 canPlay=true / 非管理员被拒 / 解散后房间消失 全通。
  - **带备注补偿 `POST /api/admin/adjust-gold`**：记「变动多少+为什么」而不是把金币设成某个数字；走钱包流水留痕、`requestId` 幂等、扣成负数拒绝、备注必填。
  - 用户评价：目前只是大致框架，后续还要继续优化扩充。
- ### 🧱 布局稳定性（反复踩了 3 次，值得记住）
  - **座位被裁/遮挡**：不再手调百分比，改两层结构性保证——① `#ring-layer` 留安全内边距（顶部 118px，容纳名字/下注徽章/对手牌/胜率徽章的向上探出）；② 半径按**实际可用像素**动态夹取（座位块是固定像素尺寸，屏幕越小占比越大，写死百分比必然溢出）。新增 `seatfit` 测试：竖屏/横屏 × 2~9 人 × 5 种屏幕全枚举断言每个座位完整可见——**当场就抓出小屏 2~9px 溢出**。
  - **房间信息水印与座位打架**：水印原来用 `top:20%` 百分比定位，窄屏时座位被夹取后向中间收就会压住它。改为固定在**顶部安全带**（合规横幅之下、座位环起点之上），几何上不可能重叠。
  - **⚠️ 最坑的一次：CSS 注释写坏**。改说明时多打了一个注释结束符 → 注释提前闭合 → 后面两行中文变成游离文本 → **CSS 解析器把它连同下一条 `#ring-layer{...}` 整条当成非法选择器丢弃** → 座位乱飞、贴着座位定位的「开始」悬浮球飘到屏幕中间；切一下横竖屏（触发 resize 重排）才恢复。**排查了两轮才找到**，因为 `seatfit` 等用例是用**正则**读 CSS 取数值的，正则照样匹配到 `top: 118px` → 全绿通过，**测试读到了内容却不知道文件本身是坏的**。
    → 新增 `tools/check-css.js` 并接入 `npm run check`：注释配对 / 花括号配平 / **规则之外不得有裸文本**。反向对照验证：还原坏注释立刻报出 3 处（带行号）。
  - **进桌瞬间座位挤成一条线**：牌桌尺寸未定那一帧量到残缺值。修：`ringLayerSize` 挡掉 <240px 的测量（沿用上次有效值）+ **`ResizeObserver` 盯住牌桌区域**，尺寸真正确定/变化就整体重排（覆盖进桌/坐下/切横竖屏/窗口缩放/地址栏收起）。半径下限提到 rx26/ry24 作最后保险。

## ✅ SQLite 切换已完成（2026-08-08，测试服 + 香港生产均已上线）
- **四步走全部完成**：①审计脚本改读 SQLite → ②合 main + 测试服首次迁移与验收 → ③备份脚本改 SQLite 并做恢复演练 → ④生产切换。
- **生产迁移结果（与切换前基线逐项一致）**：用户 30 / 金币总额 699,993 / 站内消息 226 / 牌谱 10,090 / 反馈 1 / 管理员账号；`integrity: ok`、外键错误 0。数据库在 `/root/PokerGame/data/pokerdojo.sqlite`（代码目录之外，部署不覆盖）。
- **切换前快照 + 异地副本**：`/root/PokerGame/backups/legacy-pre-sqlite-20260808-152918`，已 `scp` 到本机 `backups_offsite/` 并核对（30 / 699,993 / 10,090）。旧 JSON/JSONL 保留但不再写入。
- **⚠️ 测试服首次迁移直接段错误（幸亏先上测试服）**：`better-sqlite3@13` 要求 **Node >=22**，而**两台服务器都是 Node v20.20.2** → `Segmentation fault` 崩在迁移脚本。已把依赖降到 **`^12`**（支持 `20.x||22.x||23.x||24.x`，同时覆盖服务器与本机 v22）。**选降依赖而不是升级服务器 Node——在数据迁移的同时更换运行时＝同时冒两个险。**
  - 这次崩溃反而**验证了故障保护有效**：旧数据分毫未动、未生成正式库（不留半成品被误认为可用）、PM2 保持 `stopped` 拒绝带不确定数据启动。
- **两个 cron 必须同步改（否则静默失效，最危险）**：切换后 `hands.jsonl`/`data.json` **冻结不再更新**。
  - 备份：旧 `backup.sh`（cp data.json/hands.jsonl）→ **`scripts/backup-cron.sh`**，用 SQLite Online Backup 生成一致性快照（WAL 下 cp 主库文件会得到**损坏备份且平时看不出来**），并**备份完立刻校验备份文件本身**，不 ok 就失败退出保留现场。cron `0 4 * * *`，留 30 份（单份 71MB，磁盘 86G 富余）。
  - 审计：`tools/audit-chips.js` 改为**优先读 SQLite**（`POKER_DB_PATH` / `--db`），并加**数据源自检**——「SQLite 已存在却还在读 JSONL」判为配置错误直接告警；「单纯数据旧」只提示不告警（低活跃期误报会把告警变吵，那才真危险）。cron `30 4 * * *` 已带 `POKER_DB_PATH`。**两个 cron 都已立刻实跑验证，没等到次日凌晨才发现坏。**
- **验收**：测试服——我的 6 项回归 + 朋友 25 个测试 + 客户端 jsdom 全过；**pm2 restart 后牌局/座位/筹码完整恢复（7940→7940）**；备份→还原→完整性 ok→数据可读→**审计可直接对备份文件运行**（事后追查不必碰生产库）；测试服全部 1975 手筹码守恒。生产——数据基线全对、登录接口正常查库、日志无新错误。
- 💡 **排障备忘**：生产 `poker-error.log` 里的 `db is not defined`(2026-07-26) 与测试服的 `EXTRA_MAX is not defined`(2026-07-17) 都是**历史日志**，早已修复，别再当成新问题。`https://pokerdojo.space` 在 KAUST 网络打不开是**域名被过滤**（见前文备忘），直连 IP 与服务器本地自测均 200。

## 🔴 CRLF 导致香港每日备份静默失败 11 天（2026-09-08，迁移 AWS 时才发现）

- **现象**：新机上跑备份 cron 报 `cannot execute: required file not found`。顺手查香港，发现**同样是坏的**——
  `backup.log` 里刷满 `/bin/sh: 1: .../backup-cron.sh: not found`，**最后一次成功的每日备份是 `pokerdojo-20260828_040001.sqlite`，到发现时已整整 11 天没有任何自动备份**。
- **根因**：`core.autocrlf=true`（Windows 默认）→ git 索引里是 LF、**工作区被检出成 CRLF**；而 `deploy.sh` 打包的是**工作区文件**，
  于是把 CRLF 一路传到 Linux。内核按 shebang 去找 **`/bin/bash
`** 这个不存在的解释器 → `required file not found`。
  仓库**没有 `.gitattributes`**，没有任何东西强制 `.sh` 保持 LF。
- **修**：
  - 根因：新增 `.gitattributes`（`*.sh text eol=lf`），并重新检出把工作区归一化成 LF。
  - 存量：两台服务器 `sed -i "s/
$//" scripts/*.sh` 就地修复，并**立即实跑验证**（香港 `integrity: ok`、57 用户/26,462 手；AWS 同样通过）。
- ⚠️ **教训（比 bug 本身重要）**：**cron 失败是静默的**——它只往日志里写，没人看。这 11 天里备份为零，
  而唯一的保险是人工做的那份异地副本。这和「告警变吵会被忽略」是同一类问题的两面：
  **一个只写日志、没人读的检查，等于没有检查。**
  → ~~待办~~ **✅ 已做（2026-09-16）**：独立的 `tools/check-backups.js`，生产 cron `45 4 * * *`，查新鲜度/能否打开/内容合理性/份数/异地副本，异常发告警邮件。见「运维两件」一节。
- 💡 顺带发现：`deploy.sh` 打包工作区而非 `git archive`，所以**任何工作区与索引的差异都会被带上生产**。CRLF 只是其中一种表现。

## 🗄️ SQLite 持久化 + 重启恢复（朋友 PR #7，2026-08-08 合并）
- **来源**：dreamingwill 的 `merge/sqlite-database`（84 文件 / +6186 −1179）。正好实现待办里的**牌局状态持久化 + 重启恢复**（原 P0 第 2 条）。
- **内容**：SQLite 存储（用户/经济流水/牌谱/**活跃比赛快照**）、旧 data.json→SQLite 迁移、活跃牌局重启恢复、备份工具 `scripts/backup-sqlite.js`、部署防护、测试与部署文档 `docs/refactor/database/DEPLOYMENT.md`。
- **数据安全铁律（来自该 PR，必须遵守）**：
  - 数据库路径由 **`POKER_DB_PATH`** 指向**代码目录之外**，避免部署覆盖。
  - **SQLite 库文件、WAL/SHM、旧数据文件与密钥**一律不得提交 git、不得覆盖、不得打进部署包。
  - **备份必须用 `scripts/backup-sqlite.js` 生成一致性快照**——WAL 模式下**只复制主库文件会得到损坏/不完整的备份**。原 `backup.sh` 那套 cp/scp 对 SQLite 不再成立，迁移时务必同步改掉。
  - `data.json`/`hands.jsonl`/`feedback.jsonl` 迁移后仅作**回滚兜底**保留一段观察期，别急着删。
- **合并冲突处理原则（用户 2026-08-08 定）**：**以 main（线上可用版）为基准**，朋友的新增功能验证无 bug 后再并入，**不能因合并影响原有版本**；但**他的改法确实更好就采用他的**。
  - 已解决 6 处冲突：`run-it-service.js`（取 main 的 `const deadlineAt`——下方 emit 要引用它，朋友的内联写法会让 emit 拿到 undefined；同时保留他的 `persistence.commit`）、`poker-action-events.js`（保留 main 的 45s 窗口重置 + 叠加他的持久化提交）、`20-socket.js`（**采用他的 `tournament_over({winnerId})` 按 id 判断**，比原来按用户名更稳——他还加了可改显示名；同时保留 main 的 `eliminated` 观战）、`deploy.sh`/`deploy-test.sh`（tar 合并为 `avatars scripts src public tools`）、`CLAUDE.md`（**整份取 main**，见下）。
  - ⚠️ **`RUNIT_DECIDE_MS`**：该 PR 仍是 25000，main 是 **45000**（修线上 room150331 吞钱事故）。合并后已确认为 **45000**，别被改回去。
- ⚠️ **CLAUDE.md 差点被删掉 513 行**：该 PR 的「重写 claude.md」把本文件压成 95 行的精简开发指南，**历史/踩坑/待办全部删除且未迁移到别处**。合并时已整份保留 main 版。他主张的「CLAUDE.md 保持精简、历史进 CHANGELOG、设计进 docs/、想法进 Issue」思路本身合理，但**要迁移而不是删除**——若将来采用，先把这 500 行搬到 CHANGELOG/roadmap 再精简。

## 🪑 SNG 淘汰后可继续观战（2026-08-08）
- **反馈**：多人 SNG（类似 FT）里一旦有人筹码归零就被**直接踢回大厅**，连观战都不行——朋友局里想看别人打完却看不了，体验很差。
- **根因**：`seat-service.js` 的 `removeBustedPlayers` SNG 分支在淘汰时执行 `s.leave(roomId); s.currentRoom=null; lobbySockets.add(s.id); s.emit('busted_out')` —— 把 socket 直接踢出房间。
- **修**：淘汰只把他从 `game.players` 移出座位，**不再踢出 socket 房间**；由于「观众 = 房间内未在座者」（`listSpectators`），splice 之后他**自动就是观众**。改发新事件 `eliminated{canSpectate:true}`；客户端 `20-socket.js` 收到后**留在牌桌**并提示「💀 你已出局，可继续观战；想离开点退出房间」，不再 `showLobby()`。`busted_out` 保留给现金桌真·离场路径。
- 误点空座无害：`sit_down` 对非现金桌直接拒绝（"该房间无需坐下"）。
- **验证**：3 人 SNG（一人梭/一人跟/一人弃，保证只淘汰 1 人、另 2 人继续打）——被淘汰者收到 `eliminated{canSpectate:true}` ✅、淘汰后仍持续收到 `game_state`（还在房间）✅、出现在观众列表 ✅、未收到 `busted_out` ✅、**解散时也能正常收到 `room_dissolved` 回大厅（不被晾在房里）** ✅。回归 全押离场/多人全押离场/连点开始/raiseClosed/现金桌续局+SNG 开赛 全通。
  - ⚠️ 测试构造注意：三家同时全押会**两人一起出局、比赛立刻结束**，`removeBustedPlayers` 根本不执行，走不到该路径——必须让"只淘汰一人、其余继续"。

## 🛡️ 筹码守恒审计（经济安全网，2026-08-08，香港+测试服已上，已挂每日 cron）
- **动机**：上面那个「凭空造筹码」是玩家发现后反馈的。需要一张**不依赖预知 bug 形态**的网，让系统先发现。
- **原理**：扑克零和——**单手内 `Σ(结束筹码)` 必须 == `Σ(开始筹码)`**。不等即有钱凭空出现/消失，不管起因是边池、退还、多次发牌、离场处理还是将来的新功能。
- **`tools/audit-chips.js`**：`--room` / `--yesterday` / `--days N` 限范围；`--settle` 打印最终结算表（含修正 + 「合计应为 0」自检，之前手工算两次的活儿现在一条命令）；`--mail` 异常时告警邮件；`--json` 机器可读；**退出码 1 = 有异常**（供 cron）。
  - **合法「手中补码」用子集和识别**（一手可能多人同时补，如 15,000=10,000+5,000；只认整百的正向差额），不告警。⚠️ 初版只认单人 → 大量误报。
  - **非法行动特征**：同一玩家**同一街内**连续行动且第二次是弃牌（= 全押被跟后离场被误判弃牌的签名）。⚠️ **必须限定同街**——「某街跟注、下一街弃牌」完全合法，初版没限定导致 4 处误报。
- **实战校验（香港 10,090 手全量）**：独立复现 room130674 seq53 的 +16,508 及其非法行动特征，与人工分析完全一致；结算表自检合计为 0；正确识别 124 处补码、零误报。**时间分布显示 07-24 那批修复之后到 08-07 之间干净**，只剩已修的这一个洞（7/15、7/23 那 13 手是 07-24 修边池/退还之前的历史遗留，方向是筹码消失）。
- **每日巡检**：香港 cron `30 4 * * *` 跑 `--yesterday --mail`，日志 `/root/PokerGame/audit.log`。`mailer.js` 新增 `sendAlert()`（收件人 `alertTo` > `feedbackTo` > `user`）。**已实测邮件送达**——注意坑：`process.exit()` 会在异步邮件发出前结束进程，必须 `await`（否则告警是哑的）。
- ⚠️ **`deploy*.sh` 的 tar 已加 `tools` 目录**，否则发版会把审计脚本冲掉。
- **🩹 修：开了 ante 的桌上，合法补码被误报成凭空造币（2026-09-08，从一次异地备份的例行校验里挖出来的）**
  - **现象**：对 26,354 手全量审计报 18 处异常，其中**房间 832138（08-13）的 4 处是新的**，签名高度一致——差额正好是整数补码额（+10,000/+4,000/+40,000/+10,000），归因却是「一人 +X−10、其余每人 −10」。
  - **根因**：`contributions()` 只累加 `actions` 里的下注，而**前注 ante 是发牌前直接扣进底池的、根本不是一个 action**（`hand-service`「先收 Ante，再收盲注」）→ 每个交了前注的人都被少算一份，归因整体偏移 `−ante`。后果不是「差一点」：补码差额从 10,000 变成 **9,990，不再是整百** → `matchRebuySubset` 认不出来 → 合法补码被判成异常。832138 是全库唯一开 `ante=10` 的活跃桌（729 手），4 处异常全出自它。
  - **修**：`contributions()` 里按 `min(ante, startChips)` 把前注补进每个座位的投入（服务端收的就是这个值；实测 4,678 个 ante 座位全部发到牌且够交，`ante=0` 的路径完全不受影响）。
  - **验证（正反对照）**：修后 18→**14 处异常**、合法补码 158→**162**，正好平移 4 处；凭空合计变化 **−64,000 = 那 4 手 delta 之和**，数字自洽；**14 处已知真异常一处不少全部保留**（含 room130674 seq53 那个带非法行动特征的真 bug）。另构造注入测试：真补码→判合法 ✅、**注入 +777 凭空筹码→仍报警 ✅**。
  - ⚠️ **为什么值得修**：cron 每天 `--yesterday --mail`，**退出码 1 就发告警邮件**——只要有人在 ante 桌上补码就收一封假告警。本文件自己写过「告警一旦变吵就会被忽略，那才是真正的危险」，这正是那种情况。
- **🔴 补码判定升级为「查钱包账本」，当场抓出 2 手真·白拿筹码（2026-09-08，同一批）**
  - **起因**：上面那次 ante 修复做反向对照时发现的既有盲区——注入 **+5,000（整百）** 的凭空筹码**会被当成补码放过**。因为 `matchRebuySubset` 纯靠「正向整百」的启发式，而**牌谱 `seats` 里根本没有 `buyIn` 字段**（只有 userId/username/seat/avatar/startChips/hole），光看牌谱无法区分「补码」与「凭空多出来的筹码」。
  - **改法**：优先查 `wallet_transactions`（`transaction_type='cash_rebuy'`，`metadata_json.chips` = 补了多少筹码，`match_id` 关联牌局）——**有真金白银的扣款流水才算补码，没有就是凭空**。判定从「猜」变成「查账」。
    - 窗口用 **`[hand.ts, hand.completedAt]`**：补码要在**这一手进行当中**提交才会计入本手 `endChips`（seq123 实测流水比开手晚 11 秒）。手快结束时提交的落到下一手，那种情况本手 delta=0 走不到判定。
    - **必须按人筛**：同一时刻可能有坐出的玩家也在补码，他没被发牌、筹码不进本手 start/end（832138 seq177 实测窗口里两笔只有在座那位的 4,000 计入）。
    - 用**子集和**匹配账本金额（一手可能多人、或同一人多笔——seq274 是同一人两笔 20,000 合成 40,000）。
    - ⚠️ **只覆盖 2026-08-09（SQLite 上线）之后**：更早的牌谱没有 `matchId`、也没有流水 → **回退启发式**。报告里会打出「N/M 查钱包账本（权威）· 其余回退启发式」，免得以后误以为整份报告都是权威结论。全量实测覆盖 **38/176**（大部分补码发生在 8-09 之前）。
  - **🔴 当场抓出 2 手真问题**（启发式一直放过、这次才浮出来）：房间 245876、**2026-08-09**、`aaaaronx` **+8,000 和 +20,000（共 28,000 筹码）**。证据链：他在该场比赛的钱包流水只有 4 笔补码（序号 1/2/3/4，17:50–18:14），但筹码到账在 **19:50 和 20:38**，比最后一笔流水晚近两小时、**账上没有任何对应扣款**；且金额 8,000/20,000 **正好等于**早先 `:1` 和 `:2/:3/:4` 那几笔。→ 就是本文件记录的「**补码幂等键复用 → 白拿筹码**」（站起/回座让 `rebuySeq` 归零，同额重放时钱包返回 `applied:false` 不扣金币，外层没检查、筹码照给）。**那条记录自己写着「筹码守恒审计抓不到这一类」——现在查账版抓到了。**
    - 该 bug 已于 2026-08-09 修复；**全量扫描确认修复之后再无同类**（账本时代的异常只有这 2 手，且都在修复当天）。
    - 💰 **待定**：`aaaaronx` 多得 28,000 筹码（按现金桌兑出价 ≈ **2,800 金币**），是否用管理员补偿接口扣回由用户定（同 qilong 16,508 那次的处理方式）。
  - **验证**：新增 `audit-rebuy-ledger.test.js`（3 例，构造最小 fixture 库跑真实 CLI）：①有流水→判合法 / **无流水的整百凭空筹码→必报警（旧版放过的那一类）** / 无流水的 777→报警 / 守恒的正常手→不出现；②ante 桌合法补码不误报；③无账本时回退启发式不误报。
    - **反向对照（两条都咬得住）**：撤掉账本判定 → 例①挂；撤掉 ante 补正 → 例②挂。
    - ⚠️ **踩坑记 1**：例② 第一版**测不到它声称在测的东西**——带了账本就走账本路径，根本不经过 `contributions()`，撤掉 ante 补正照样全绿。改成去掉 `matchId/completedAt` 强制走启发式才咬得住。**写完测试必须验反向，否则等于没测。**
    - ⚠️ **踩坑记 2**：fixture 第一版让两家各扣 ante 却没把前注还给赢家 → 自己就不守恒（凭空蒸发 20），审计如实报了出来。**是 fixture 错了不是工具错了**，反而验证了工具是对的。
- **🧹 顺手修好 3 个「陈旧测试」（2026-09-08，`frontend-structure.test.js` 全绿，37/37）**
  - 这 3 项在 clean HEAD 上就是失败的（我 stash 验证过），**代码没问题，是测试没跟着功能更新**——加功能时漏改测试，久了就变成「反正一直红着」的噪音，和吵闹的告警一样会让人对失败脱敏。
  - ①**脚本清单缺 `03-i18n.js`**（2026-08-28 i18n 批次新增的文件）→ 补进 `EXPECTED_SCRIPTS`，并注明它必须排在 `05-utils.js` 之前（后面几乎每个文件都用 `t()`/`L()`）。
  - ②**断言「index.html 里完全不许有 `<style>`」**，但开场画面(#8①)**故意内联**了首屏关键 CSS——外链样式表到达前就得是深绿底，否则先闪一层白，比没有开场更难看。→ 改成守住真正的意图：**只许一块、必须含 `#boot-splash`、且 <2500 字符**；同时把内联脚本断言**加强**为「一个都不许有」（原来只认 `// ===== State =====` 这一个特征字符串，换个写法就漏）。
  - ③**断言中文字面量 `房间名：${invite.roomName}`**，而 i18n part3(2026-09-02) 已改成 `${L('房间名','Room')}：` → 改成断言**结构**（三段信息齐全、链接排在房间码之前）。⚠️ **教训：别在测试里断言会被 i18n 替换掉的中文字面量**，否则每次翻译都会制造假失败。
  - **反向对照 4 条全部咬得住**：去掉 `03-i18n.js` 引用 / 塞大块内联样式 / 塞任意内联脚本 / 邀请文案里去掉房间码 —— 各自精确报出对应那一项。

## 🔴 全押者中途离场被误判弃牌 → 凭空造筹码（2026-08-08，香港+测试服已上）
- **现象**：玩家反馈战绩面板**总盈亏不是零和**（截图合计 +15,268），有人"凭空赢"。
- **查证（香港 room130674，546 手，2026-08-07 12:24→18:14，7 人现金桌）**：写脚本逐手做**筹码守恒检查**（单手内 `sum(endChips)` 必须等于 `sum(startChips)`）——8 手不守恒共 +130,508，其中 **7 手是手中补码**（6000/20000/10000/8000/40000/10000/20000=114,000，chips 与 buyIn 同步增加，属正常），**只有 handSeq 53 是真 bug：+16,508 全部给了 qilong**。
- **根因**：`handSeq 53` 动作序列出现 `lzhkg2003 call(6921)` 后**紧接着 `fold`**——他 call 全押后**退出房间**，而 `leave_room` 把他改判弃牌。全押者被判弃牌会：①剥夺其池权；②让「未跟注退还」认为对手的注没被跟 → 错误退回 → **凭空造筹码**。共 **4 处**只判了 `!p.folded` 没判 `!p.allIn`：`seat-service.js:39`（站起/房主强制站起）、`membership-events.js:162`（留座离座）、`:200`（退出房间，现金桌+SNG 共用）。
- **修**：这 4 处统一加 `&& !p.allIn`。全押者本就无需再行动，留在局里正常跑马/摊牌即可（`activePlayers` 只按 `folded` 过滤，所以不影响摊牌），离座延后到本手结束（`vacateAfter`/`standing` 原有机制处理）。
- **那手的正确结果**：底池 16,508；qilong `TH JS` 在 `9H QH 8D` 已成 **Q 高顺子**，lzhkg2003 `6H 7D` 是 6-7-8-9 两头顺听牌，但**补 5 或 T 都只能组成更小的顺子（9 高 / T 高）→ 实为「听死牌」**，所以**这手 qilong 本来也该赢**；问题是他**被多给了一份 16,508**（正确净变化应是 +8,820，实际 +25,328）。河牌因牌局提前结束**从未发出**。
- **全场正确账（修正后合计恰为 0）**：ss +138,754 / aaaaronx +79,612 / ManManMan −4,541 / **qilong −33,175（账面 −16,667，多得 16,508）** / lzhkg2003 −24,320 / SL −70,000 / Mugglejinx −86,330。→ **qilong 应扣回 16,508**（金币按现金桌汇率折算后由管理员调整）。
- **验证**：新增 socket 复现测试（heads-up + **多人带死钱**两版）：全押后 `leave_room` → 筹码守恒 ✅、全押者 `folded=false` 保留池权 ✅，各跑 3~4 次稳定；**反向对照**（撤掉守卫）测试即失败，证明用例真能抓到该 bug。回归 现金桌续局/摊牌、SNG 开赛、连点开始、raiseClosed 全通 ✅。
- ⚠️ 教训：这类「凭空造币」用**每手筹码守恒**脚本扫牌谱即可发现，建议**定期跑**（数据资产已足够支撑事后审计）。

## 🤝 合并朋友 PR「提示音 + 实时盈亏」+ 横屏公共牌放大（2026-08-10）
- 朋友 `zrh-yhyh-wsl` 推了 `fix/stop-game-condition`（3 个提交）。按「新增功能验证无 bug 再并入」的原则**只挑了两个**：
  - **手机端可能没有行动提示音**（真 bug）：iOS Safari 的 `AudioContext` 从后台回前台会被挂起，而原来只在**第一次 click** 解锁一次。改为 `pointerdown/touchend/keydown/click` 都尝试解锁 + `visibilitychange` 回前台重新 `resume`，且 `beep()` 在 `state !== 'running'` 时直接不发声。
  - **进行中牌局的实时盈亏显示错**（真 bug）：战绩面板算 `chips - buyIn`，但**已下注的 `currentBet`/`committed` 和待生效的 `pendingRebuy` 不在 `chips` 里** → 一下注就显示成亏损。新增 `displayNet()` 把这几项加回去（`state-presenter` 相应下发 `committed`），`roomStatFor`（点头像看战绩）也同源。
- **第 3 个「现金桌到时暂停而不结算」——加了 5 分钟兜底后并入**：他的设计（到点不打断、本手打完暂停发牌、房主可 −30/−15/+15/+30/+60 调整结束时间）本身更好，但**房主掉线/关 App 就没人能结束这桌**，玩家筹码会被无限期锁着换不回金币（`scheduleEmptyCleanup` 只清**空**房，有人坐着的暂停房不清）。
  - 兜底（`TIME_UP_GRACE_MS = 5 分钟`）：到时暂停即启动，**房主一动手（调整结束时间）就撤销**；到点仍无人处理 → `endCashTable` 正常结算收桌。若兜底到点时**还在牌里**，不打断，挂 `pendingEnd` 等本手打完（`scheduleNextHand` 里 `pendingEnd` 优先于 `timeExpired`）。**重启恢复接着原来的剩余时间走**（`timeUpGraceAt` 进 state，不白送一个完整 5 分钟）。
  - 客户端：水印和比赛设置面板都显示「⏸️ 训练时间已到（约 N 分钟后自动结算）」，让全桌知道**不处理会发生什么**，而不是干等。
  - **加时档位保持原来的 +15/+30/+60/+120**：朋友那版还加了 −30/−15「提前结束」，但房主本来就有「⏸️ 暂停发牌」和「结束比赛」两个入口，多这一排是重复路径、还容易误点把桌子提前判到时（用户定）。
  - 🔴 **上测试服时被「发版后错误日志自查」当场抓到一个我自己引入的 bug**：新加的 `game.timeUpGraceTimer` 是 Node 的 `Timeout` 对象，**内部循环引用**（`_idlePrev/_idleNext` 互指），而活跃牌局快照是 `JSON.stringify(game)` —— 直接抛 `Converting circular structure to JSON`，**整个快照写不进去**，启动恢复时刷一片报错。修：把它加进 `game-serializer` 的 `TRANSIENT_KEYS`。
  - 顺手把这条变成关卡：`tools/check-timers.js` 交叉核对「所有存了 setTimeout/setInterval 的字段」是否都在 `TRANSIENT_KEYS` 里，已接入 `npm run check`。⚠️ 写检查器时踩了两次误报：只按名字匹配会把 `startTableTimer`/`restoreActionTimer` 这类**函数名**算进来；只取第一段属性会把 `clearTimeout(game.straddleDecision.timer)` 报成 `straddleDecision`（真正的字段是最后一段 `timer`）。反向对照（撤掉 `timeUpGraceTimer` 登记）确认能报出来。
  - 验证 `cash-timeup-grace.test.js`（假时钟快进）：5 分钟自动收桌 / 加时后撤销兜底 / 牌局中不打断只挂 pendingEnd / 重启接着原截止。**反向对照**（撤掉 `armTimeUpGrace`）4 个用例挂 3 个。
- **横屏(电脑)公共牌太小**（玩家实拍）：横屏牌桌铺满整页、中间很空，公共牌按 1.2 倍显得很小，**甚至比自己那两张(1.4 倍)还小**——公共牌本该是全桌焦点。改为**横屏 1.7 倍 / 竖屏保持 1.2 倍**（手机上那个大小玩家说正好），实际值仍由几何夹取兜底。你那台电脑 58 → 公共牌 **69.6 → 98.6px**（自己的牌 81.2），手机端 42.8px 不变。
- ⚠️ **顺带修了 `communityCardW` 的一个真错**：`#board` 是「底池行 + 公共牌行」**整体**垂直居中于 `boardTopPct`，所以公共牌那排的中心比 `boardTopPct` 低了半个底池行。原来漏了这个偏移 → 算不到刚好在牌下方的座位。放大到 1.7 倍时 **seatfit 当场抓出 4 处「被公共牌压住 31~38px」**，补上偏移后全绿。**放大一个元素同时验证它的碰撞模型，是这次没出事的原因。**

## 🎨 布局二轮 + Straddle 小标志（2026-08-10，玩家实拍反馈）
- **公共牌变小了**（上一轮为了不压筹码数字，按屏幕宽度一刀切封顶 → 正常手机上牌白白变小）。改为**按这一桌的实际座位几何算**（`communityCardW` 纯函数 + `syncCommunityWidth` 包装）：只有**竖直方向真的和公共牌那一排重叠**的座位才构成限制，能放大就放大到原来的 1.2 倍。用户那台手机（≈420 CSS 宽）2/4/6/9 人**全部恢复满尺寸 42.8px**。`seatfit` 改成直接调用同一个函数再断言「不压座位」，并交叉核对 JS 的 `BOARD_TOP_PCT` 与 CSS 的 `#board top%` 一致。
- **头像缩小**：`.avatar-block` 46→40、名字 `max-width` 74→64 → `SEAT_BOX` 96×78 → **90×68**。座位块变小既是玩家要求，也顺带把公共牌的横向空间让了出来（上一条能恢复满尺寸就是靠它）。
- **顶部再上移**：`#ring-layer` 顶部内边距 42→**28px**。9 人/大手机竖向铺开 **598→612px（79%）**。
- **Straddle 改成桌边小标志**（玩家：「现在要等他弃牌、不再参与这手了才问他下一手要不要 straddle，很不方便」）：
  - 服务端：邀请**一准备好就发**（删掉「轮到他行动就不显示」的门槛、`cancelVisibleStraddleForTurn` 改空实现），有效期**到下一手开始**（10 分钟只作兜底上限），`straddle_offer` 增发 `chainIndex`。
  - 客户端：打断式弹条 → **牌桌右边缘一个小牌子 `STR ×N`**（`#straddle-flag`）。**点一下 = 接受**，标志立刻消失并 toast「下一手 straddle X」，随后**下一位自动出现自己的标志**；没点的话**新一手开始（`hole_cards`）自动收掉**。不再有常驻倒计时定时器。
  - 验证：`dom-straddleflag.js`（显示 ×档位 / 点击只发一次 accept 且消失 / 新一手自动收掉 / 无常驻定时器）✅
- **顺带修「有金币却补不上码」的误导提示**：`chargeRebuy` 失败一律报「⚠️ 金币不足」，而真实原因是幂等键冲突 —— 玩家明明有钱却被告知余额不够。改成 `outcome.reason` 出参，只有真的余额不足才那么说，其余报「补码失败（原因），已回滚未扣款」。

## 🔴 补码幂等键复用 → 白拿筹码（2026-08-09，从生产 error.log 里挖出来的）
- **发现方式**：上完香港后例行看 `poker-error.log`，看到 **17 次** `[wallet] cash rebuy failed IDEMPOTENCY_CONFLICT`。不是玩家报的，是日志里翻出来的。
- **根因**：补码的幂等键是 `cash-rebuy:{matchId}:{userId}:{rebuySeq}`，而 **`rebuySeq` 存在内存里的座位对象上** —— 玩家**站起/回座**（或任何重建座位对象的路径）会让它归零，于是同一场比赛里同一个人第二轮补码又用 `:1`：
  - **金额不同** → 抛 `IDEMPOTENCY_CONFLICT` → 回滚，补码失败（就是那 17 条，玩家白点一次）
  - **金额相同** → 钱包直接返回 `applied:false`「这个键早处理过」**不扣金币**，而外面没检查这个标志，**筹码照给** = 白拿
- **修（两处）**：①幂等序号改为**按钱包账本里已有条数推导**（`db.wallet.countOperations(prefix) + 1`），跨重启/跨重新落座都唯一；②`commitWithWallet` 返回 `applied === false` 时**当失败回滚**——每次补码都是一次全新付款，「早就处理过」只可能意味着这次没扣钱，绝不能当成功。
- **验证**（`rebuy-idem.js`）：连补两次 → 站起 → 回座 → 再补同样金额，逐笔核对金币。**反向对照**（撤回修复）立刻复现白拿：第三次补码金币 `9463 → 9463`（应扣 220），筹码却到账 ✅ 抓得住。
- ⚠️ 同样是**筹码守恒审计抓不到**的一类：单手内筹码是守恒的，漏的是**金币没扣**，不在牌谱里。教训：**部署后要看 error.log**，这类只在特定路径触发的经济漏洞不会有人主动报。

## 🔴 链式 straddle：同一个人被问两档 + 凭空少扣筹码（2026-08-09，线上牌谱实锤）
- **现象**（玩家实拍）：UTG 的 pp 没有 STR 徽章，后一位 Owen 直接 `STR 4BB`；底池 9.5BB 而不是 11.5BB。
- **牌谱实锤**（room 368634 seq=11，14:14:51）：`straddle=[Owen(80), Owen(160)]` —— **两档都是 Owen 一个人**。而贴出循环是 `chips -= 80` 再 `chips -= 160`（共扣 240）、`currentBet` 被覆盖成 160 → **Owen 凭空少 80 且这 80 没进底池**（所以底池少 2BB）。
- **根因**：straddle 邀请是在**上一手局间**按「当时阵容」**预测**下一手位置发出的。上一手 Young 还没入座 → 预测 UTG = Owen，他接受 2BB；随后 **Young 中途入座**，阵容变了、位置整体挪一位 → 重新预测时 UTG=pp、UTG+1=Owen，**Owen 又被问了一次** 4BB。开手校验只查了「座位没变 / 金额对 / 筹码够」，**没查这个人是不是真的站在该档的位置上**，两档都放行。
- **修（两道）**：①`hand-service` 开手时**按本手真实阵容重算 BB 之后的顺序**，第 ci 档必须正好是 `realAfterBB[ci]` 本人，对不上就在那档截断；②`straddle-service.prepareChainDecision` 里**同一人已在链上就不再问**。另把扣款改成**差额扣**（`expectAmt - currentBet`），即使将来再出现重复也不会重复扣钱。
- **验证**：`straddle-dup.js` 做了**确定性复现**——在「第 1 档刚被接受」的那一刻让第 5 人入座，撤掉修复后立刻打出与线上完全一致的签名 `admin4(80) → admin4(160)` 且桌面下注 160≠该档 80；加回修复后 4 手链条全部 `80→160→320` 且无人重复 ✅。
- ⚠️ 这个 bug **筹码是「消失」而不是「多出」**，且牌谱里 `startChips` 是在贴完 straddle 之后取的快照 → **`audit-chips.js` 也抓不到**。位置/归属类错误只能靠规则测试。
- **顺带修显示**：`STR` 标记原来只标最后一档、且**该玩家后续下注后还一直挂着**（玩家反馈「没必要」）。改为**链上每一档都标**，但**只在他的下注还等于那笔 straddle 时**显示，他再下注/加注后自动变回普通下注徽章。

## 🔴 边池 × 多次发牌：短码从边池里分到了钱（2026-08-09，玩家实战反馈）
- **现象**：多人局里 A 短码全押做出主池，B/C 继续下注做出边池，之后剩下的两人协商发多次 —— **短码 A 却按「总底池」分到了钱**。正确规则：短码全押者最多只能赢下**整个主池**，边池他没资格碰；发多次也只是把**主池**再切成 N 份。
- **根因**（`run-it-service.js` `executeRunouts`）：直接拿 `game.pot`（总底池）`share = pot/n`，每一份判给**全场最强手**——**整条边池逻辑被绕过了**。单次摊牌走的是 `doShowdown` → `buildSidePots` 逐池分配，多次发牌这条路却是另写的一套，从一开始就没接上边池。
- **修**：`executeRunouts` 改为**先 `buildSidePots`，再逐池均分成 N 份**，每份只在**该池有资格的人**里比大小（与单次摊牌同一口径，余数并进第 1 份）。`buildSidePots` 经 `poker-service.js` 注入。另加兜底：某池若没有可比牌的有资格者，让全体参与者去争，避免那份钱卡在池里蒸发。
- **数据资产**：`hand.runIt.pots` 记录每次 runout 的**逐池明细**（主池/边池各自份额与赢家），`runit_award` 也带上 `pots`（增量字段，客户端可忽略）——多人边池局事后要能查清谁凭什么拿到哪份钱。
- **验证**（`runit-sidepot.js`，构造真实场景：A 全押 2000 → B/C 各跟 → 翻牌 B 下 3000/C 跟做出边池 → 转牌 C 弃牌 → A/B 发 2 次）：
  - 修复后：主池 6000 逐份争、边池 6000 **全额归 B**，A 最多拿到 6000（整个主池）、**从不出现在边池赢家里**，筹码守恒 ✅（连跑 3 次，含 A 连赢两份的情形）
  - **反向对照**（把逻辑改回总底池均分）连跑 4 次：A 分别拿到 0 / 6000 / 3000 / **12000** —— 最后一次直接把**整个底池含边池**卷走，正是玩家报的现象。证明用例真能抓到。
  - ⚠️ 这个 bug **筹码是守恒的**（钱没凭空多出来，只是分错了人），所以 `audit-chips.js` 那张网**抓不到**——归属类错误得靠规则测试。

## 🏷️ 改名后旧名字到处残留（2026-08-09，玩家反馈）
- **现象**：点「看转牌/看河牌」时那行提示显示的还是**改名之前**的名字。
- **根因**：**面向玩家的服务端文案一直用 `username`（账号名，改名不会变）**，而不是 `displayName`。不只是那一处——全库 **20 多处**都是（弃牌/过牌/跟注/下注/加注广播、超时、退还未跟注、入座/站起/离桌、补码、加时、亮牌、高牌定庄、盲注/straddle 播报、赢池结果…）。玩家只是先撞上最显眼的那一条。
- **修**：`src/account/display-name.js` 加 `nameOf(u) = displayName || username`，**所有 `emit` 给玩家看的文案统一走它**；`console.log`（服务器日志）**保留 username**——那里要的是稳定身份，不该跟着改名变。
- 另有两处自建对象只带了 `username` 没带 `displayName`，`nameOf` 拿不到新名字：`drawForButton` 的 `draws`、`game.straddles`——都补上了。**这类「中转对象丢字段」是同类 bug 的常见藏身处，加 nameOf 之后要顺带查一遍。**
- **验证**（`rename-notice.js`）：改名 → 打一手 → 点看翻牌，断言提示里**有新名字、且整手 7 条桌面提示无一含账号名**。冷却期改不了名也能测（改为读 `/api/me` 的当前 displayName 断言）。第一版跑出来就抓到了我漏掉的两处（`❌ admin1 弃牌`、`🎴 高牌定庄：admin1`）。

## 📱 手机端布局铺开：水印回桌心 + 座位不再挤中间（2026-08-08，测试服）
- **反馈**：手机端对比德扑之星，「他的布局更平铺更合理，我们太紧凑；他的文字在桌面中间、可以被公共牌覆盖，我们上方浪费了一块」。
- **根因（是我之前自己挖的坑）**：早先把房间信息水印 `#table-info` 钉到了顶部(`top:26px`)，为了不让座位撞上它，又给 `#ring-layer` 加了 `top:118px` 的大内边距 —— 于是顶部白占一条、整个座位环被压进中间，就是「上方浪费 + 太紧凑」。
- **修**：①**水印移回牌桌内圈**(`top:42%` ≈ 公共牌位置、`z-index:0`、宽 `min(42%,240px)`)——座位都在椭圆【周上】，内圈本来就空着，所以不会打架；`z-index` 最低 → **公共牌/底池压在它上面**，正是德扑之星那个观感。②`#ring-layer` 顶部内边距 **118px → 52px**（只留合规横幅+余量），座位环整体上移并变大。③`ringPos` 圆心 `cy 47→48`（取在上下夹取值大致相等处，把 `ry` 顶到最大）、`ry` 上限 36→44（实际由夹取决定）。④删掉 `M>=6` 时顶部座位 `y-=5` 的躲水印偏移——水印已不在顶部，而那是**夹取之后**的偏移、会顶破上边界。⑤`transform-origin` 同步 `50% 48%`，否则「坐下」整圈旋转会偏心。
- **效果**（seatfit 量化）：6 人大手机竖向铺开 **489px → 533px（占牌桌 63%→69%）**，最顶上的座位上移约 34px。
- **实拍复盘续修（同日，9-max 手机实拍）**：玩家反馈「最上面两人还能再往上、顶部还空着」「公共牌那 5 个虚线空框没必要显示」「公共牌压住了两侧玩家的后手数字」。
  - **🔴 真凶：`SEAT_BOX.h` 一直写的 81，实际是 96** —— 座位块是 `padding2 + 名字(11×1.25) + 名字下边距7 + gap1 + 头像46 + gap1 + 筹码上边距9 + 筹码(12×1.2) + padding2`。**少算的 15px 正好就是底部那行筹码数字**，所以夹取从没把它算进去，公共牌自然会压上去。同时 `overhangTop` 写 34 也不对——`.equity-badge top:-34px` 是相对**头像块**的，而头像块本身已在座位块内 23.8px 处，**实际只探出约 10px**。改成 `{w:78, h:96, overhangTop:11}`：上方少占 16px（座位能更靠上）、下方多留 8px（筹码数字不再被压）。**测试里也别再各写一份数字**——`seatfit` 改成直接从 `80-table-renderer.js` 正则读 `SEAT_BOX`（写两份就是上次对不上的原因）。
  - **半径改为按「这一桌真正用到的方向」精确反解**：位置是 `x=cx+rx·cos+dx`、`y=cy+ry·sin+dy`，边界约束对 rx/ry 都是线性的 → 逐座位反解各自允许的最大半径取最小。奇数人数收益最大：**9 人时没有任何座位落在正上方（|sin| 最大只有 0.94）**，旧写法却按 `sin=-1` 留够余量 → 顶上白空一条。`dx/dy`（M≥6 的微调）也一并算进约束，杜绝「夹取之后再偏移顶破边界」那个反复踩的坑。
  - **公共牌空位不再画虚线框**（`.empty-slot` 边框改 transparent）：**保留占位**所以发牌仍不跳动，只是看不见空框。
  - **公共牌整排宽度按屏幕封顶**：`#community { --comm-w: min(calc(var(--card-w)*1.2), calc((100vw - 210px)/5 - 5px)) }`——屏幕越窄座位占比越大、中间越挤，一律放大 1.2 倍必然压到座位。210 是扫出来的：K=205 刚好通过，取 210 留 5px 余量（`#runit-boards` 同步用同一公式）。
  - `#ring-layer` 顶部内边距 52→**42px**；`#board` 39%→**43%**、水印 42%→**44%**（落在两排座位之间的空带）。
  - **效果**：9 人/大手机竖向铺开 **516px(66%) → 592px(76%)**，最顶座位上移 **26px**；`seatfit` 新增**「公共牌不得压住座位」**断言（2~9 人 × 5 屏 × 横竖屏），改这几个公式必须一起跑。
- **验证**：`seatfit.js` 竖屏/横屏 × 2~9 人 × 5 种屏幕全部座位完整可见 ✅；新增 `dom-watermark.js`（水印 z-index 必须低于公共牌 / 必须贴着牌面不许飘回顶部 / 旋转支点必须与 cy 一致 / 不压座位）✅；dom-layout / dom-resize / dom-ringsize / dom-betinfo / dom-cardstyle / dom-hints / dom-podium / dom-inbox / dom-histscroll 回归全通 ✅。

## 🩹 实战验收三修：胜率被牌挡 + 无效加注无提示 + 房主连点开始（2026-08-02，测试服已上）
- 背景：用户在测试服验收上一批时发现的三个问题（四格房间码验收通过 ✅）。
- **①全押胜率徽章被牌挡住**：`.equity-badge` 原 `top:-14px; z-index:8`，而 `.opp-cards`（对手牌覆盖层）**居中于 46px 头像、向上探出约 7px+**，且**同为 z-index:8 但 DOM 靠后 → 牌盖住徽章**。修：`top:-34px`（抬到牌上沿之上）+ `z-index:12`（压过牌）。
- **②无效加注没有任何提示**（玩家以为按钮坏了）：根因是 **`server_msg` 客户端只 `console.log`、不显示在桌面**（早期"移除可视日志"留下的坑），所以服务端的「⚠️ 无效加注」拒绝玩家根本看不到。三层修：
  - 服务端 `state-presenter.js` 新增 **`raiseClosed`** 字段（当前行动者 `hasActed` 且 `currentBet-p.currentBet < lastRaiseSize` → 面对短码全押、行动未重开）。
  - 客户端 `80-table-renderer.js`：`raiseClosed` 时给加注按钮加 `.locked`（置灰但**仍可点**，`disabled` 会吞掉点击事件就弹不出提示了）；`70-actions.js` `openSizing()` 开头拦截 → `toast('⚠️ 前方是无效加注…只能跟注或弃牌')`。
  - `20-socket.js`：**所有 `⚠️` 开头的 server_msg 一律 toast**（已核实全部 `⚠️` 都是 `socket.emit` 私发拒绝、无一走 `io.in` 广播，不会误弹给别人）——以后任何服务端拒绝都不再"点了没反应"。
- **③房主连点「开始」反复高牌定庄**：`start_game` 只挡 `status==='running'`，但 **`status` 要到 `startHand` 才置 running**，而 `drawForButton` 的定庄动画有 **2.8s** 窗口，期间 status 仍是 waiting → 连点就反复定庄。修：`beginPlay` 加 **`game.beginning` 守卫**（`startHand` **开头**清除——含各 early return，否则开赛失败会把房间永久卡成点不动）；客户端 `startGame()` 加 3s 防抖 + 立即置灰按钮。
- **验证**（本机新环境重建测试脚本）：连发 6 次 `start_game` → `button_draw` 恰 1 次且牌局正常开出 ✅；`raiseClosed` 正确下发 true、正常局面无误报、服务端仍拒绝非法加注 ✅；jsdom 加载真实前端 → 无效加注弹提示且不展开面板/正常仍可加注/连点只发一次/徽章 top≤-30 且 z>8 ✅；回归：现金桌续局到第 2 手 + 摊牌、SNG 准备系统开赛（`tryStartHand→beginPlay`）均正常，守卫未挡住正常流程 ✅。

## 🃏 无效加注规则 + 四格房间码自动加入（2026-08-01，香港+测试服均已上）
- **无效加注（incomplete raise / 短码全押不重开下注）**：正常德扑规则——当前有人下注后，后方玩家全押的**总量不足「一个完整加注」**（增量 < `lastRaiseSize`）时，属**无效加注**，**不重开行动**：前方**已经行动过**的玩家只能**跟注补齐或弃牌，不能再加注**。修 `poker-action-events.js` raise 分支：①开头拒绝——`if (player.hasActed && (currentBet - player.currentBet) < lastRaiseSize)` → 只能跟/弃；②只有**完整加注**（`increment >= lastRaiseSize`）才 `hasActed=false` 重开他人行动，短码全押**不重开**（旧代码无条件重开=bug，导致短码全押后还能再加）。未行动过的人仍可正常加注。
  - 验证：3 人现金桌，A 开池加注 1200、短码方(2000)全押到 2000（增量 800 < 完整加注 1160）→ 已跟注的 B 尝试再加注被服务端拒绝（server_msg「无效加注」）、非法额未生效 ✅；sidepot/sidepot2/standflow/hostfeat/runit/repromw/sngnorunit 回归全通（正常加注/全押/边池未被破坏）。
- **四格房间码自动加入**：加入页原来是「1 个输入框 + 加入按钮」→ 改为**4 个独立数字格**（`#joinCodeBoxes .code-box`，OTP 风格）：只收数字、逐格自动跳、**输满 4 位自动 `join_by_code`**（无需点按钮，按钮已删）；退格回跳、支持粘贴分配、`invite_error`（房间不存在）时 `resetJoinCode(true)` 清空四格聚焦首格重输 + toast「房间不存在」。`window._joinSubmitting` 防抖（一次输满只发一次）。
  - 涉及：`index.html`（4 格）、`public/js/30-room.js`（`onCodeInput/onCodeKey/onCodePaste/getJoinCode/joinByCode/resetJoinCode`，删 `sanitizeJoinCode`）、`public/js/20-socket.js`（room_joined/invite_error 改调 `resetJoinCode`）、`public/css/30-lobby.css`（`.code-box` 样式）。验证：jsdom 加载真实 index.html+内联真实 public/js → 输满自动发一次(code=1234)/过滤非数字/输错清空/不足不发 全对 ✅。

## 🩹 多次发牌超时坑 + 全押胜率太小（2026-07-31，测试服已上，香港待用户验收）
- **线上真实事故(香港 seq297, room150331, 05:19)**：dek[T♣7♣] vs lhy[89o] 翻牌全押、协商发 2 次,但**结算成"单板赢家通吃"**——lhy 独吞 10040、dek 得 0、`runIt` 空、只落库一块板。**dek 被少算 5020,lhy 被多算 5020**(待用户用管理员手动补 dek +5020)。
- **根因(两个对照复现钉死)**：run-it 决策**没在 25s 窗口内完成同意**→触发 `resolveRunIt(1,'timeout')` 兜底→**退化成发 1 次、赢家通吃**,说好的两次和该分的钱都丢了。①快速同意→正确均分 ✅;②不同意(超时)→单板通吃(与 seq297 记录签名完全一致)。**代码算法没错,是超时窗口太紧 + 领先方那一下"同意"容易错过。**
- **修**：①`RUNIT_DECIDE_MS` 25s→**45s**;②落后方选号(`propose_runs`)后**为领先方重置一个完整 45s 窗口**(否则被压缩);③`runit_offer`/`runit_proposal` 下发 `deadlineAt`,客户端面板显示**倒计时** + **轮到我决策时振动+提示音**(`runitAlert`) + 同意按钮加大——让愿意同意的人不错过。
- **注意规则**：发多次**需领先方也同意**;领先方(大优势方)**故意不同意**时单次结算是"按规则正确的",dek 不算被坑;只有"双方都想发两次却没点完"才是被超时坑——本次属后者。
- **全押胜率徽章太小**：`.equity-badge` font 10.5px→**16px** + 加粗描边 + 呼吸放大 + z-index 提高,跑马时看得清了。
- **验证**：run-it 均分/SNG 结束/边池/站起/房主/单会话 回归全通;`deadlineAt` 已下发且为领先方重置;安全关卡通过。

## 🎯 SNG 结束流程修复 + 优雅关停待办（2026-07-26）
- **SNG 结束现状**：分出胜负(仅 1 人有筹码)时 `maybeEndSNG` **会自动结算**——奖金 `db.setGold` 发给冠军(落盘、崩溃不丢)+ 公布排名 + 站内信。但之前**不自动解散房间**,要手动退出/解散,而手动退出正好踩中那个 `db` 崩溃。
- **本批修的两个真·经济漏洞(逻辑洞,正是要提前抓的那种)**：
  1. **输家离场被错误退款**：`leave_room` 用 `status!=='running'` 判"开赛前退款",但**已结束**的 SNG(status='finished')也满足 → **输了的人点返回大厅居然退还报名费**(白拿钱)。改为只有 `status==='waiting'`(从未开赛)才退;新增 `finished` 分支=直接离开不退款。
  2. **解散重复发奖**：`dissolve_room` 对**已自然结束**的 SNG 会**再给筹码最多者发一次奖**(双倍)。加 `if(!game.tournamentOver)` 守卫,已结束只清房不再发奖/弹排名。
- **新增自动解散**：`maybeEndSNG` 分出胜负后**留 25s 看排名再自动解散房间**(`finalizeSngRoom`:清定时器/emit room_dissolved/踢回大厅/删房)。玩家看完排名自动回大厅,无需手动解散。
- **验证**：SNG 打到分出胜负→冠军净+90(扣110报名+200奖)、输家离场金币不变(不退款)、25s 后 room_dissolved 自动解散 ✅;sngnorunit/sngleave(开赛前退款仍在)/多次发牌/边池/站起/房主/单会话 回归全通 ✅。
- **🔜 优雅关停待办(用户 2026-07-26 定为高优先,真实提升)**：重启前**先广播提醒 → 等所有房间当前手牌打完 → 结算保存数据 → 再退出**。至少现金桌在关停时 `endCashTable`(在局筹码退回金币),不丢钱。**要点(用户强调)**:不能硬切,要等当前手结束+提前广播。
- ~~**🔜 更远待办(步骤2)**~~ **已完成（2026-08-08 SQLite 批次）**：**牌局状态持久化+重启恢复**(快照 roomGames 落盘、启动读回、玩家重连续打)——做到部署/重启对进行中的牌局近乎无感。核心诉求:**崩溃/重启绝不能影响正在进行的牌局(商业化底线)**。见 [[feedback_no_crashes_lint_gate]]。

## 🔴 重构引入的崩溃 bug 复查（2026-07-26，香港已上）
- **背景**：用户玩 SNG 结束后解散/退出时"服务器整体卡一下、无关的现金桌也没了"——实为**未捕获异常崩溃 → pm2 重启 → 内存所有牌局清空**。重构漏了依赖注入,是这类"运行到特定路径才 ReferenceError 崩全服"的 bug。
- **抓到并修的两个崩溃(同一类)**：①`db is not defined` @ `membership-events.js`(SNG 开赛前退出退还报名费分支漏解构 `db`);②`onActionTimeout is not defined` @ `poker-action-events.js`(**加时后再超时**,定时器调用未解构的函数→崩;有进程兜底后降级为"卡住该手")。都补上解构。
- **系统性复查(防还有第三个)**：用 **eslint `no-undef`** 全量扫 `src/`(专抓"用了没定义")——修完两个后**干净**;再写脚本**交叉核对**每个 event 文件从 `tableService`(89 key)/`config`/`runtime`/`bind`(77 key) 解构的名字是否真实存在——**全部对得上**,确认无同类隐患。（教训：重构后必跑 eslint no-undef,语法检查抓不到这类。）
- **进程级兜底**：`server.js` 加 `uncaughtException`/`unhandledRejection` 兜住未捕获异常，不再让一次 ReferenceError 直接带走全服。
  - 🔴 **2026-09-16 更正**：本条原先写着「**只记录不退出**」，**早就不是了**。SQLite 那批之后改成了 `server.js:129` → `shutdown(signal, error)` → **给所有房间存快照 → `db.close()` → `process.exit(1)`**，由 pm2 拉起来、启动时从快照恢复。这样比「带病继续跑」更好（uncaughtException 之后进程状态本就不可信），但也意味着**它崩过、重启过而没有任何人被通知** —— 见下方「看不见的欠账 · A5 崩溃告警」。
- **顺带两个玩法修**：①**SNG 不发多次**(`offerRunIt` 加 `roomType!=='cash'` 直接 return false,锦标赛固定发 1 次);②**掉线/离桌者给正常行动时间**(去掉 away 的 800ms 秒判)——防网络波动瞬断被直接弃牌,到点仍是无注过牌/面对下注才弃、重连重置计时。
- **验证**：SNG 全押不弹协商直接摊牌 ✅;SNG 退出退还+不崩 ✅;现金桌多次发牌/边池/站起/房主/单会话 回归全通 ✅。

## 🧩 朋友大重构 + UTG Straddle（2026-07-25，PR #5 已合并，测试服已上）
- **重构（朋友 dreamingwill/develop，"只拆文件不改逻辑"）**：`server.js` 3000 行 → 86 行组装入口 + 几十个 `src/` 模块（工厂函数 `createXxxService({deps})` + 依赖注入；唯一共享状态 `src/runtime.js` 的 `roomGames`）；`index.html` 4116 行 → 骨架 + `public/css/*` + `public/js/*`（数字前缀=加载顺序）。两个"总装车间"：`src/table/table-service.js`（拼大厅/座位/比赛/引擎）、`src/games/poker/poker-service.js`（拼德扑引擎子服务：rules/pot/showdown/state-presenter/hand-service/run-it/straddle）。socket 事件按类拆到 `src/socket/events/*`。设计文档见 `docs/refactor/`。
- **⚠️ 重构后 deploy 脚本必须打包 `src public` 目录**（我已修 `deploy.sh`/`deploy-test.sh`：tar 追加 `src public`，`data.json`→`data.json*`）——否则只传顶层文件会 `MODULE_NOT_FOUND` 宕机。改部署流程记得带上。
- **验证（我）**：朋友 13 个测试 + 我的 5 个 socket 回归（多次发牌/未跟注退还/边池合并/站起/房主）全过 → **我的修复行为完整保留**；straddle 端到端通过；jsdom 加载真实组件化前端 0 错误、关键函数齐全；express 本就 5.2.1（没动）。合并基点=我最新 main，**没丢任何修复**。
- **UTG Straddle（现状=单人开关版，默认关闭 `allowUtgStraddle`，上线不启用即安全）**：现金桌房主可开；开启后下一手 UTG 可选贴 2BB、preflop 最后行动；开手校验候选人=真 UTG+座位没变+筹码够，不符则作废；15s 决策窗口 + 局间 4.5s 兜底。服务端 `straddle-service.js` + `hand-service.js`；事件 `straddle_offer`/`straddle_decision`/`straddle_posted`。
- **🔜 Straddle 正确逻辑（用户 2026-07-25 反馈，后续再做，较复杂）**：应是**链式/连续 straddle（re-straddle）**——当前局进行时下一局 **UTG** 浮出"下一手要不要 straddle"；他点了→下一局 **UTG+1** 浮出 **straddle×2**（翻倍）；以此类推每人可再翻倍，**最多一路 straddle 到翻前 all-in**；每有人接受，**下一局开池的有效 BB 数也随之改变**。现在只是单人 UTG 版，先默认关不启用，等后续重做成链式。

## 📝 用户反馈待办（2026-07-23）
- ~~**🎲 allin 协商发多次**~~ **已完成**（见上「多次发牌」批次，2026-07-24）。
- **待改进**（2026-08-08 复查：1~3 均已完成，仅剩 4、5）：
  1. ~~**弃牌手牌看不清**~~ **已做**：牌面已无 `grayscale`（全库仅剩「离桌头像」「无效加注按钮」两处用到），改为 `.seat.folded { opacity: 0.5 }` = 只压暗、保留四色。
  2. ~~**看后续牌(rabbit)广播谁想看**~~ **已做**：服务端 `poker-card-events.js` 发 `table_notice`「某某 想看 翻牌/转牌/河牌」，客户端 `20-socket.js` → `showTableNotice` 显示在公共牌下方一行小字。
  3. ~~**🔴 退出房间战绩被清空(bug)**~~ **已修**：`renderStats` 分三组渲染——在座（🧍站起/💺留座/📴离桌/💤坐出 灰显）、`vacated` 站起围观（🧍围观 灰显）、`statsHistory` 已离开（🚪已离开 灰显）；且 07-16 起现金桌「退出房间」不再移除玩家而是 `standing=true` 保留座位与筹码，战绩自然不丢。
  4. **改名 + 头像库更新 + 允许自定义上传头像**（后续，与「玩家反馈待办」1、4 条重复）。
  5. **管理员功能扩充**：现在只有看玩家/改金币，后续加更多管理功能（后续）。

## 📝 玩家反馈待办（2026-07-25，仅记录，未做）
1. ~~**改名系统**~~ **✅ 已做（2026-08-08）**：用了朋友 PR 的显示名方案（2–16 字符、保留字、24h 冷却），**未改成金币计费**（用户当时定的）。原记录：花金币改名（暂定 500 金币），入口在**个人主页·资料**面板里操作。需服务端改用户名（校验唯一/长度/敏感词）+ 扣金币 + 广播新名。
2. ~~**横竖屏切换选项**~~ **已完成（2026-08-08）**：设置 → 屏幕布局（自动/竖屏/横屏），横屏解除 body 720px 限制铺满页面。
3. ~~**消息/收件箱 + 结算面板美化**~~ **✅ 已做（2026-09-06，1.3.2）**，含颁奖台。原记录：
   - 收件箱里的**历史战绩显示太简略**，后续优化（信息更全/更好看）。
   - 房间**解散/退出后的结算展示**目前"还行"，可再美化（具体怎么美化待想）。
   - **结算加"颁奖台"**（领奖台样式，放**头像 + ID**）：🥇**老板**=本场**亏损最多**（净盈亏最负，玩梗"输最多的请客当老板"，**不是最有钱**）、🥈**MVP**=**赢最多**（净盈利最多）、🥉**力工**=**手数最多**（打得最多的苦力）。三个维度各评一个称号，纯娱乐/调侃风格。
4. ~~**头像库更新**~~ **✅ 已做（2026-08-08）**：删旧 a1–a12，换成 b1–b27 扑克+道场主题。（**自定义上传仍未做**，见待办 #6。）原记录：现在可选头像太丑，换一批好看的（配合上面「改名+自定义上传头像」一起做）。

## 当前进度
- [x] C# 牌力评估原型（支持 5 / 6 / 7 张）
- [x] Node.js 移植（PokerLogic.js + LookupTables.js）
- [x] Socket.IO 多房间基础联机（加入房间、发牌、判赢家广播）
- [x] 分阶段游戏流程（Preflop → Flop → Turn → River → Showdown）
- [x] 押注系统 v1（带入筹码、盲注、Fold/Check/Call/Bet/Raise、自动推进、底池/玩家筹码显示）
- [x] 平局分池处理（Showdown 同分玩家均分底池，余数给第一位赢家）
- [x] 前端视觉化（绿毡桌面、CSS 扑克牌、玩家座位环绕、底池居中、行动高亮、移动端响应式）
- [x] 部署到生产服务器（阿里云 Ubuntu 24.04，pm2 守护，UFW + 安全组）
- [x] 用户系统 v1（注册/登录、JWT 鉴权、持久 UUID、金币经济、showdown 翻牌展示）
- [x] 牌桌 UX v2（准备系统自动开局、下注尺寸面板、最小加注规则、实时总底池、筹码/BB 切换、思考计时器+加时、发牌动画、去掉 log）
- [x] **大厅 + SNG 双人升盲**（登录→大厅→创建/加入房间→牌桌；房间列表、退出房间、邀请码加入、断线重连；盲注按级别时间递增、淘汰制、奖池给冠军）
- [x] **多人引擎 + 现金桌（A，已测）**：N 人通用引擎（按钮/盲注按人数定位、UTG 起手、真边池 buildSidePots）；**现金桌房型**（2–9 人固定盲注+ante，金币↔筹码买入兑出抽水 110/100 per1000）；淘汰/兑出离场/补码 rebuy；中途加入坐出本局；大厅 SNG/现金桌双栏创建；客户端环形座位（对手沿顶弧、自己置底）；**牌谱落库** hands.jsonl（玩家×模式×时序+思考时间+底牌）。已测：边池数学、3人流程、现金桌经济(9560=扣440抽水)、双人回归、牌谱写入
- [x] **A 打磨批次（已测，部署测试服）**：①现金桌清零→**坐出不淘汰**（保留座位、本手不发牌、可补码回来；仅 SNG 淘汰）②**坐下式入座**（现金桌先以观众进桌→点空座「坐下」弹买入 modal→`sit_down`；房主「开始」`start_game`）③**补码系统**（补充记分牌 modal+滑块+消耗/可用金币，**下一手生效**；**自动补码** autoRebuy）④**9 固定环形座位**（空座显示「坐下」，弧线绕桌）⑤**放射行动布局**（池比例做成小圆球+中央加注金球，下注/加注互斥只露一个）⑥**桌内菜单**整合（☰：桌面设置/补码/战绩/牌谱/比赛设置/退出/解散）⑦**当前战绩面板**（昵称/带入/手数/战绩+观众列表）⑧**牌谱回顾 UI**（`/api/my-hands` 按模式筛选、逐手展开看动作/公共牌/结果）⑨**比赛设置弹窗**。E2E socket 测试通过：坐出不淘汰、补码生效重启、牌谱落库
- [x] **A 牌桌重构批次（已测，部署测试服）**：⑩**固定座位**（`sit_down({seat})` 点选座位号；服务端 players 按 seat 排序=环桌顺序；按钮按 **buttonSeat** 轮转，与数组插删解耦；out-of-order 入座测试 0/3/1→排序 0/1/3、按钮 0→1→3 通过）⑪**全环形座位均匀分布**（`ringPos` 椭圆 M 等分，ringIndex 0=底部；任意座位入座后**旋转使自己永远在屏幕最下方**）⑫**紧凑座位块**（德扑之星式：左=头像+名字+筹码竖排，右=手牌；对手牌小、自己牌大；名字省略号防溢出）⑬**布局稳定不再回弹**（座位/底池/公共牌全绝对定位，行动浮层纯悬浮、不再改 paddingBottom 推动布局）⑭**底池+公共牌居中放大**、中央水印现金桌也显示⑮**站起围观** `stand_up`（兑出筹码变观众留房）+**留座离座** `reserve_leave`（保座+坐出，2 分钟自动站起兑出）+`sit_back` 回座⑯**现金桌解散**=各家兑出（不再像 SNG 判给筹码领先者）⑰**SNG 报名费档位** 110/220/550/1100（冠军 200/400/1000/2000=奖池×10/11 抽水 1/11）+开赛人数 2–9 可选
- [x] **牌桌交互修复批次（部署生产）**：⑱**行动悬浮球浮在「自己座位+手牌」正上方**（JS `positionActionBar` 按 hero 座位 rect 定位、透明无底板，不再遮挡手牌；图二德扑之星式）⑲**修复行动顺序非顺时针**（`ringPos` 改 `+` 使 ringIndex 递增=顺时针；座位号递增=行动顺序=视觉顺时针，下一位在我左手边）⑳去掉筹码前 💵 图标㉑下注球数值随 筹码/BB 设置一致（`fmtChips`）㉒**改名「德扑道场 Poker Dojo」**（title + 登录页）
- [x] **牌桌细节修复批次2（测试服）**：㉓**修复坐出残留 bug**（坐出/未发牌玩家收不到 hole_cards → 新一手 game_state phase=preflop 时统一清 showdownInfo/revealedCards，杜绝赢家手牌+牌型在他人继续游戏时残留）㉔**对手手牌覆盖显示在头像上**（亮牌/摊牌才显示、overlay 不撑宽座位 → 解决顶端/两侧超出屏幕）㉕**收窄环形半径**（rx37/ry38/cy46，对手不再超出边界）㉖**下注筹码移到座位上方**（不再压住筹码数）㉗**SNG 报名费档位选中态加深**（金色填充）㉘**分池飞币**：showdown_reveal 带 `pots[]`（主池在前），客户端**主池先飞向赢家、边池依次再飞**+「主池/边池」标签（3 人不等额 all-in 测试：主池6000→边池1 4000→边池2 2000）㉙**all-in 当下不弹补码**（仅本手结束真坐出 sittingOut 才弹）
- [x] **现金桌"训练赛"批次（测试服）**：现金桌即训练赛——创建去掉"我的买入筹码"，改 **训练时长档位 0.5/1/2/3/4/5/6h**（到点自动结束结算）；中央水印显示剩余时长倒计时；比赛设置弹窗房主可**比赛加时 +15/30/60/90/120 分**；**站起围观不再兑出筹码**（坐出旁观、筹码保留，仅 解散/退出/到点 才结算）；留座超时也转站起（不兑出）；战绩面板保留**已离开/淘汰玩家**（灰显🚪）+ 站起🧍/留座💺/离桌📴/坐出💤 灰显
- [x] **结束排名 + 站内消息系统（测试服）**：比赛结束/房主提前结束/SNG 结束 → 公布**按盈利排名**（现金=盈亏筹码、SNG=冠军→淘汰倒序盈亏金币）；在线玩家弹**结算排名面板**；所有参与者（含已离开/离线）进**收件箱**（database.js messages + `/api/my-messages`、`/api/messages/read`；大厅 📬 按钮带未读红点）。E2E 测试：排名 2 人、离开者也收到消息
- [x] **牌桌交互细节批次（测试服）**：弃牌后**保留显示自己手牌**（变暗）且摊牌可亮牌（含弃牌者）；摊牌**去掉飞币"主池/边池"文字**，改为**底池内标注主池/边池**（仅真·多人边池才显示，2 人未跟注回退不显示）；底池 💰 换成 **CSS 筹码图标**；行动悬浮球**上移 30px**不挡下注信息；快捷预设**半圆弧拓宽放平**；过牌/跟注**合并一个按钮**；弃牌→红✕、加注金球居中、跟注居右、整体缩小；牌面数字放大(0.5×)；坐下入座**音效+整圈旋转动画**
- [ ] **A 仍待打磨**：9-max 密集时紧凑度细调、级别升级（现金桌可选升盲）
- [x] **A 个人栏 + 生涯统计（测试服）**：大厅 👤 个人主页(资料/生涯战绩/牌谱 三 Tab)；**头像选择从设置面板挪到个人栏·资料**；`stats.js` 从 hands.jsonl 聚合 **VPIP/PFR/3bet/弃3bet/C-bet/AF/WTSD/W$SD/总手数/总盈亏/最大赢取/平均思考** + **盈亏曲线(SVG)**，`/api/my-stats?mode=` 按 全部/现金/SNG 筛选；合成牌谱单测通过(VPIP/PFR/3bet/C-bet/net 数值正确)
- [x] **B 聊天 + 表情（测试服）**：桌内 💬 聊天抽屉(消息列表+输入+发送)、**快捷短语**(好牌/AllIn/不服…)、**Emoji 表情**一键发；服务端 `chat_msg`/`emote` 房间广播+限频(0.6s/0.8s)；收到后**座位上方冒泡**(聊天文字气泡 / 表情大图上飘动画)；语音🎤 暂留占位。E2E：广播双收、限频生效。快捷短语/Emoji 是默认集，待用户定具体
- [ ] **下一批**：预操作（提前勾选 过牌/弃牌/跟注）、全押跑马实时胜率%、筹码下注动画+摊牌赢额弹字、下注 UX 更轻盈、AI 对战、邮箱账号系统(等域名)、TLS(等域名)、破产救济/签到送币
- [x] ~~**待办(用户点名)**~~ **三条全部已做**：①滚动到底自动加载 ✅（IntersectionObserver）②多次发牌 run it N times ✅ ③战绩连续 ✅。原记录：①牌谱「加载更多」改滚动到底自动加载（现为手动按钮）②两人 all-in **协商发几次(run it N times)**——双方同意后逐次发不同 runout 分池，多人局固定发 1 次③战绩连续(见实战反馈修复批·待确认)
- [x] 重连（localStorage 记录房间，重连自动回桌；保留座位）
- [ ] 正式客户端（Android / iOS / PC）

## v1 已知限制
- **仅支持双人 heads-up**：双人专属行动顺序
- **采用标准 heads-up 规则**：按钮位 = 小盲(SB)，preflop 先动；非按钮位 = 大盲(BB)，postflop 先动
- **无真正边池**：用"未跟注退还"近似处理 2 人 all-in 不同筹码深度场景，3+ 玩家不同 all-in 金额需要真正的边池系统
- **Bet vs Raise 已区分**：本街无人下注 → Bet（最小 BB）；已有下注 → Raise（须大于当前注）
- **牌局进行中禁止加入**：避免座位索引混乱
- ~~**小盲/大盲写死 10/20**~~：已改为按房间 SNG 级别读取（`gameSB/gameBB`，初始 25/50 升盲表）

## 产品方向（房间类型）
项目目标支持两种房间类型：
- **SNG（Sit-N-Go 单桌锦标赛）**：盲注按时间递增、玩家淘汰制，当前优先级。需配置盲注级别（如 10/20→20/40→…）、涨盲间隔（如 5min）、起始筹码、奖励结构
- **Cash 现金桌（9-max）**：盲注固定，玩家可随时带入/离开。次优先级

服务器房间状态需扩展 `roomType` / `blindLevels` / `currentLevel` / `levelStartTime` 等字段；现有写死的 SMALL_BLIND/BIG_BLIND 应改为从房间配置读取。

### 经济模型（含抽水，后期实现 —— 当前 SNG 是简化版：固定 50 入场、冠军通吃）
- **多人桌 = 现金桌**：2–9 人，盲注固定，随时带入/离开。
- **SNG 锦标赛奖励**：
  - 报名费**可配置**（50 / 100 / 200 / …），不再写死 50
  - 奖池**抽水**：冠军不拿全部。例：2 人共 100 奖池，冠军拿约 85（其余 ~15 为平台抽水/服务费）；多人按奖励结构分配（如前 N 名）
- **现金桌买入/兑换有价差（抽水）**：
  - 买入：例 **110 金币 → 1000 筹码**
  - 离场：例 **1000 筹码 → 100 金币**
  - 即每 1000 筹码进 110、出 100，平台抽 ~10%
- 实现时：`config.buyIn` 可配置；结算时按 `rake` 比例计算冠军/离场金额；现金桌带入/兑出走 `gold↔chips` 汇率（买入价 > 卖出价）。

### 大厅 / 房间创建（参考"德扑之星"商业产品截图）
登录后先进**大厅页**（而非直接进牌桌）：顶部「加入比赛（输入邀请码）」+「创建比赛」两个入口，下方按 `训练赛 / SNG` 分类列出房间卡片（房名、房主、牌型、人数 x/max、级别时间、报名/即将开赛按钮）。

创建房间分两种模式，各自配置项：

**① SNG（单桌锦标赛，含双人 heads-up 升盲）** —— 当前优先
- 牌型：国际扑克 / 短牌 / 奥马哈（先只做国际扑克）
- 比赛牌局名字
- 开赛人数：2–9（2 即 heads-up）
- 报名费 / 记分牌类型（独立记分牌）
- 初始记分牌（起始筹码）：5K–30K
- 级别时间（涨盲间隔）：3–10 min
- 级别结构（盲注递增表）：标准模式
- 奖励分配
- 玩法：盲注按 `级别时间` 递增、淘汰制

**② 训练赛（现金桌 / 好友多人对战）** —— 次优先，多人引擎就绪后做
- 牌型、比赛名字
- 基础分（盲注 SB/BB）：10/20 起，可选到 500
- Ante 设置：0–80
- 级别升级设置（可选开启递增）
- 单次最小带入：2K–8K
- 带入记分牌上限：无限制 / 16K–60K
- 训练时长：0.5–6h
- 单桌最大人数：2–9
- 坐满自动开始：手动 / 2–9 人
- 玩法：盲注固定（除非开启升级），玩家随时带入/离开

> ⚠️ 多人（3–9）需要先补齐**多人游戏引擎**：真正的行动顺序（UTG 起手）、真正边池、按钮轮转。当前 server.js 仅实现双人 heads-up。可先做大厅 + 房间创建 UI 与配置存储，多人对局逻辑随后跟进。多人桌面布局参考 8-max 环形座位截图。

## 开发约定
- **JS 端（PokerServer/）是权威服务器逻辑**，C# 端仅作算法参考原型，新功能只需实现在 JS 端，无需保持双端同步。
- 游戏状态存储在服务器内存的 `roomGames` 对象中，Key 为房间号。
- Socket 事件命名使用 snake_case（如 `join_room`、`start_deal`）。

## 🏷️ 版本号（2026-08-11 加）
- **规则**：`主.次.修订`，形式像 semver 但**判定按玩家视角**，不是 API 兼容性（这是自用产品、没有下游依赖者，硬套 semver 只会每次纠结「算不算 breaking」）。
  - **主** = 大改版 / 玩法结构变了（AI 对战、房型重做）
  - **次** = 新功能（链式 straddle、多次发牌、到时暂停）
  - **修订** = 修 bug / 小调整
- **只在上生产时手动涨**（改 `PokerServer/package.json` 的 `version`），**测试服部署不涨号**——否则号涨得毫无意义。CHANGELOG 按版本号分节。
- **构建信息自动生成**：deploy 脚本写 `PokerServer/build-info.json`（git 短 SHA + 构建时间 + env），不入库但**打进部署包**。`GET /api/version` 返回 `1.1.0 (2026-08-11 · 19c04f5)`。
- **部署脚本最后自动核对线上版本** —— 取代过去「SSH 上去 grep 某个新增关键字」确认部署是否生效的土办法。
- 🔴 **最有价值的一条：前端 / 服务端【两个】版本同时显示**。APK 是薄壳、每次加载线上页面，真正坑人的不是「服务端哪版」而是**玩家的 WebView 缓存了旧前端** —— 服务端已经新了，他手机上跑的还是几天前的 JS，然后报一个早就修过的 bug。
  - 前端构建号 `CLIENT_BUILD` 是**打包进 00-state.js 的常量**（部署时 sed 替换 `__BUILD__`），所以它反映的是**玩家手里那份 JS**；和 `/api/version` 一比就知道是不是缓存。
  - 不一致时设置面板那行标黄并提示「前端是旧的，请下拉刷新」。`dom-version.js` 覆盖三种情形（一致 / 前端旧 / 本地开发不误报）。
- **🐞 反馈自动带版本**（版本号真正发挥作用的地方）：玩家点 🐞 提交反馈时，客户端自动附上前端构建号，服务端补上自己的版本，**一起写进 `feedback` 记录和那封提醒邮件**。前端构建号不在服务端版本串里 → 邮件里直接标「⚠️ 前端是缓存的旧版，先让他刷新再排查」。
  - 为什么必须自动带：**指望玩家自己去设置面板翻出版本号复制过来，基本不会发生**。只放在设置面板里等于「有但用不上」。
- **Android 壳独立一套**：`versionCode` 必须单调递增整数（Play 硬性要求），`versionName` 给人看；壳很少发版，与游戏版本**解耦**，别绑一起。CI 里 `versionName` 取 `mobile/package.json`、`versionCode` 用 `github.run_number`（天然递增）。
- 🔴 **APK 必须固定签名**（2026-08-11 发现，2026-08-28 才真正修好）：APK 的签名必须**每次构建都一样**，否则新版装不到旧版上（Android 报「应用未安装」），朋友每次更新都得先卸载。固定密钥 = 本机 `.secrets/pokerdojo.keystore`（gitignore，**必须备份**，丢了=换身份全员重装），**SHA256 指纹 `19:ED:…:38:81`**，alias `androiddebugkey`/storepass `android`。b64 存 GitHub Secret `ANDROID_KEYSTORE_B64`。
  - **🩹 真正的坑（2026-08-28 排查大半天）**：光把密钥还原到 `~/.android/debug.keystore` **根本没用**——**Capacitor 工程里 AGP 会用 runner 镜像自带的那把 debug.keystore（指纹每次不同）**，改 Secret / 覆盖 `~/.android` / `find` 覆盖所有 debug.keystore / 往 build.gradle 注入 `signingConfig` **统统改不动它**。**最终解法：构建完 `assembleDebug` 后，用 `apksigner sign --ks <固定keystore> ... pokerdojo.apk` 强制重签**——无论 Gradle 怎么签，产物一定是 19:ED。
  - **验签也曾自己写错**：一开始按证书 **Owner 名**判定（`CN=Android Debug`），可固定 debug 密钥的 Owner 本来就是 `CN=Android Debug` → 只要配了 Secret 就永远误判失败。改为**按 SHA256 指纹**比对（硬编码 `19:ED:…`，指纹是公开信息）。
  - **排障技巧**：GitHub 未鉴权 REST API 限流 60/时、且**日志/artifact 下载要鉴权**读不到；用「让 workflow 把指纹/产物 APK 发到一个临时 release（`ci-diag`），再从 release 的**资源下载 URL**取回本地验签」绕开——**资源下载不算 API 配额**。本机没 JDK 时，JKS 指纹可用 Python 解析（openssl 读不了 JKS）、APK 指纹可 openssl 从 `META-INF/*.RSA` 算。
  - ⚠️ **换钥匙的一次性代价**：修好前朋友装的是随机签名的旧包，切到 19:ED 后**要卸载重装一次**；之后所有更新都能直接覆盖。`android-latest` 直链已是 19:ED。
  - ⚠️ 仍是 **debug 版**（`debuggable=true`）；上架 Google Play 需 `assembleRelease` + 独立 release 密钥 + 隐私政策（待办）。

## 部署（运维细节见私有文档）
- **两套环境**：`deploy-test.sh`（测试服，pm2 `poker-test`）日常迭代；`deploy.sh "msg"`（生产=香港，pm2 `poker`）正式发布，无参则仅同步不走 git。两者均**从仓库根目录运行**，tar 排除 data.json/hands.jsonl/secret.key/mail.json（不覆盖生产数据）。
- **🩺 发版后错误日志自查（2026-08-10 加，`deploy.sh`/`deploy-test.sh` 最后一步）**：重启**前**记下 pm2 错误日志的字节数（`/tmp/poker_errsize_<app>`），重启 12 秒后**只读这之后新增的部分**（并滤掉部署自身产生的 `[shutdown] signal=`），有内容就打出来。
  - **为什么固定成流程**：连着三个经济漏洞（边池分钱 / straddle 少扣 / 补码白拿）里有**两个是靠翻 error.log 发现的**——玩家自己不会报，「点了没反应」「补不上码」他们只当是自己的问题；**筹码守恒审计也抓不到**（钱没凭空多、只是分错人，或者漏的是金币根本不在牌谱里）。
  - ⚠️ 第一版用 awk 在 heredoc 里拼程序，转义被截断 → **误报「没有新错误」的假通过**。改成字节偏移法后做了**反向对照**：往错误日志塞一行假错误，自查确实报了出来。检查器本身也要验反向，否则等于没有。
- **🔒 部署前安全关卡（2026-07-26 加）**：`deploy.sh`/`deploy-test.sh` 会先跑 `npm run check`（= `eslint` 的 `no-undef` + `tools/check-destructures.js` 解构交叉核对），**不过就中止部署**——专挡"用了但没定义/解构了不存在的名字"这类运行时才崩全服的隐患（`db`/`onActionTimeout` 就是这类）。首次需在 `PokerServer` 里 `npm install`（eslint 是 devDependency，服务器 `--omit=dev` 不装）。改代码后 lint 报错=有崩溃隐患,先修再部署。
- **⚠️ 上香港前必查有没有人在玩**（2026-07-25 教训：曾在有人玩时上香港把人挤掉线）：`pm2 restart` = 进程重启 = **内存 `roomGames`(所有进行中牌局)清空**——不只是掉线，玩家**买入的金币已扣、筹码只在内存**，重启没走结算 → **在局筹码/金币直接丢失**。所以：默认只上测试服；上香港前 SSH 查近期 `[deal]` 日志/连接数确认无人在玩，有人就等空闲再上。
- **🔜 待办（优雅关停）**：重启前对所有活跃现金桌 `endCashTable` 结算 / 退还在局筹码到金币，避免生产重启丢玩家筹码（现无持久化，纯内存）。
- **⚠️ 生产 IP / SSH 别名 / 服务器路径 / 密钥位置 / 管理员引导 / 备份 / mail.json 等运维敏感细节已拆到私有 `OPS.local.md`（gitignore，不入库）**——公开仓库不再收录。改动部署流程时看那份。

## 管理员账号

### 创建 / 升级管理员
- 服务器上执行 `node create-admin.js <用户名> <密码>`（不存在则建号设管理员+初始 1 万金币；已存在则直接升管理员）。具体登录/路径见私有 `OPS.local.md`。

### 管理员功能
登录后右上角出现 **⚙️ 管理** 黄色按钮，点击展开管理面板：
- 查看所有用户的用户名、当前金币、是否为管理员（👑 标记）
- 直接修改任意玩家（包括自己）的金币数量并点「确认」提交

### 管理员 API（供脚本调用）
需在请求头携带管理员 JWT：`Authorization: Bearer <token>`
- `GET  /api/admin/users` — 返回所有用户列表
- `POST /api/admin/set-gold` — body: `{ "username": "玩家名", "gold": 50000 }`

## 下一步规划

### 用户系统（替代当前的临时 socket.id）
- [x] 注册 / 登录机制
- [x] 持久玩家身份（UUID 存数据库，跨刷新和断线保留）
- [x] 用户名 / 金币余额
- [x] 金币兑换筹码机制（入桌带入时从金币扣除，离桌时返还）
- [x] 头像系统（选择头像：27 个内置头像 b1–b27）／ [ ] **自定义上传仍待做**
- [x] 玩家手牌历史记录与赢亏统计（牌谱回顾 + 生涯统计已实现）

### 房间管理
- [x] 大厅页面（登录后先进大厅；创建/加入入口 + SNG 房间列表）
- [x] 退出房间按钮（牌桌顶栏「← 退出房间」，回大厅；waiting 阶段退出退还报名费）
- [x] 房间创建（SNG：名字/初始记分牌/级别时间/报名费；`create_room` 事件，生成 6 位房间号）
- [x] 房间列表 / 快速加入（大厅实时 `room_list`，卡片「加入」按钮，或输入房间号）
- [x] 空房间自动清理（`scheduleEmptyCleanup` 3 分钟宽限，可凭房号回来）
- [ ] 最大房间数限制（按服务器内存配置）
- [x] 加入已有房间 vs 创建新房间区分（`create_room` vs `join_room`，独立房间号）
- [x] 训练赛（现金桌/多人 3–9）房间创建

### 已知 Bug
- [x] **重新登录后残留上一把牌面信息**：登出/回大厅时 `showLobby()` 清空本地渲染状态（lastState/holeCards/community）
- [x] **10 点牌显示为 T**：扑克牌面中 10 的 rank 编码为 `T`，前端展示时已替换为 `10`（formatCard）

### 界面设置面板（已完成，参考"德扑之星"桌面设置截图）
右上角「⚙️ 设置」打开面板（localStorage 持久）：
- [x] **桌面风格**：孔雀蓝 / 翡翠绿 / 太空灰 / 宝石紫（body.theme-* 改 #table-area 渐变）
- [x] **扑克风格**：标准 / **四色扑克**（黑桃黑·红心红·方块蓝·梅花绿，body.four-color + .suit-* 类）
- [x] **自定义快捷下注**：从 25/33/50/60/67/75/80/100/125/150% 自选，选中的出现在下注面板（最小/All-in 固定）；`settings.quickBets` 持久
- [x] **牌型高亮（实时）**：服务端 `bestHandFrom` 每次 broadcastState 私发各玩家 `my_hand`（当前最强 5 张的 community/hole 索引 + 牌型名）；前端绿环高亮我牌型用到的牌 + 显示「当前：一对/同花/…」
- [x] 显示BB / 游戏音效 开关并入面板（与右上角按钮双向同步）
- [x] 牌面样式 ✅ 已做 4 种（四色/标准/黑面/大字）；**更精致的牌面美术**仍待补（需美术）

### Showdown 视觉改进
- [x] 翻开对手底牌（showdown_reveal 在对手座位翻牌展示）
- [x] 显示获胜牌型名称（赢家座位显示「🏆 同花/葫芦/…」，`HandEvaluator.handCategory`）
- [x] **高亮组成最强 5 张牌**：服务端 `HandEvaluator.bestHand` 算出赢家 7 选 5，下发 `bestCommunity`/`bestHole`/`category`；前端将这 5 张「拉出+金光」(pullOut)，其余牌变灰 (dimOut)
- [x] **亮牌重置局间倒计时**：每主动亮一张牌，`scheduleNextHand` 重置 5s，给看牌时间
- [x] 隐藏 / 移除底部文字日志（已移除可视 log，server_msg 改走 console.log 调试）

### 下注 UX 升级
- [x] All-in 一键（尺寸面板「All-in」快捷，置满后下注/加注即全下）
- [x] 牌局进行中补码（rebuy）：补充记分牌 modal（滑块+消耗/可用金币），`pendingRebuy` **下一手生效**；现金桌清零坐出可补码回来；**自动补码** autoRebuy
- [x] 池子百分比快捷按钮（最小 / ½池 / ¾池 / 底池 / All-in；标准池比例加注公式）
- [x] 加注金额滑块（拖动调整，与输入框/快捷按钮联动）
- [x] 手动输入框保留，与快捷按钮联动
- [x] **最小加注规则**：加注须 ≥ 上次加注增量（preflop 即 2×BB），服务端强制，all-in for less 例外
- [x] 显示"下注后剩余筹码 / 占底池 % / 对手需跟" ✅ 已做（2026-08-09 夜间批次）

### 动画与音效
- [x] 发牌入场动画（手牌/公共牌翻入，交错延迟，只在真正发新牌时触发）
- [x] 公共牌翻开动画（Flop/Turn/River 依次显示，仅新增牌动画 + 交错延迟）
- [x] **池子拨给赢家动画**：摊牌/弃牌获胜时金币 🪙 从底池飞向赢家座位（flyCoinsToWinner）
- [x] 筹码下注动画 ✅ 已做（`20-socket.js` 按下注额变大从头像抛一枚筹码）
- [x] **完整行动音效**：下注/加注（筹码声）、跟注、过牌（敲桌）、全下（上扬）、获胜（琶音）、弃牌；服务端 `sfx` 事件驱动（已测 allin/win）
- [x] **赢家座位金色脉冲高亮**（showdown 赢家 .winner）
- [x] **收注动画**：每条街结束，各家下注筹码 🪙 飞向底池（按 currentBet 归零检测）
- [x] **掉线/离桌指示**：对手掉线或离桌时座位显示「📴 离桌/掉线」徽标 + 头像灰化（state.away，已测）
- [x] **比赛结算弹窗**：替代浏览器 alert，正式结算面板（🏆 冠军/奖池 + 返回大厅按钮 + 夺冠音）
- [x] 行动玩家呼吸高亮 + 倒计时（初始 15s）+ "加时" 按钮（+15s/次，单次上限 2min，超时自动 check/fold）
- [x] **逐张发牌动画**：开局每人交替逐张飞入（我的牌 + 对手牌背都依次发，延迟 0/130/260/390ms）+ 发牌嗒嗒音
- [x] **音效**（Web Audio 合成，无音频文件）：发牌、轮到我「叮」、剩 5s 警告、翻公共牌、弃牌嗖、亮牌叮、**所有按钮点击反馈音**；右上角 🔊/🔇 开关（localStorage 持久）
- [x] **弃牌动画 + 音效**：弃牌时该玩家的牌向下旋转飞出淡出（自己显示实际牌、对手显示背面）+ 弃牌音
- [x] **主动亮牌（双方均可，含弃牌方）**：非摊牌（弃牌结束）局间，任一玩家可点自己底牌「点哪张亮哪张」亮给对手，翻转动画 + 亮牌音（`show_card`/`show_cards`，局间窗口 5s）
- [ ] 音效补充：Bet/Raise、All-in、获胜专属音

### 已完成的牌桌 UX
- [x] **准备系统**：开赛前点准备，全员准备(≥2人)自动开赛；**开赛后自动续局**（每局结束 3.5s 后自动发下一局，无需重新准备）
- [x] **实时总底池** / **筹码·BB 单位切换** / **10 点显示修正**

### 牌桌 UI 待办（做多人 A 时一并改）
- [x] **放射状行动布局**：池比例做成小圆球 + 中央「加注」金球（更大带光环）；下注/加注互斥只露一个，减少冗余
- [x] **头像信息紧凑化**：德扑之星式座位块（左=头像+名字+筹码，右=手牌），全绝对定位环桌、自己旋到底部、布局稳定不回弹（见 [[project_room_types]] 多人）
- [x] 庄家 D 标志（替代 SB/BB 文字）；自己座位置底；行动按钮圆球化；下注筹码移到牌上方不压牌
- [x] **修复 all-in 后跟注方仍被要求行动**：改为「至多 1 名活跃玩家能行动（其余全押）→ 无更多下注，直接亮牌跑马」（`bettingClosed = act.filter(canAct).length<=1`，已测不等额筹码场景）

### AI 对战系统（规划，后期）
- [ ] 加入 AI 机器人对手：玩家可与 AI 对战（练习/凑人/填空座）。需在服务端实现机器人玩家（按策略自动行动：弃/跟/加注/全下），可分难度档；多人桌可混入 AI 补位

### 数据统计 & 牌谱（规划，后期）
- [x] **个人数据统计** ✅ 已做（`stats.js` + 个人主页·生涯战绩）。原记录：VPIP（自愿入池率）、PFR（翻前加注率）、总手数、胜率、盈亏、3-bet 率、ATS 等；需服务端按手记录每位玩家的入池/加注/摊牌行为并落库聚合
- [x] **牌谱回顾系统**：每手完整历史落库 hands.jsonl（各街动作+思考时间、底牌、公共牌、结果）；客户端「📜 牌谱回顾」面板 `GET /api/my-hands?mode=` 按模式筛选、逐手展开看动作/公共牌/结果。**按玩家归档**（见顶部「数据资产与可迁移性」）
- [x] 逐手**回放动画** ✅ 已做（`#replay-overlay` + `42-replay.js`）；**底池演变曲线**仍未做

### 头像系统
- [x] **可选头像**：设置面板头像网格（**本地打包** 12 个 SVG 头像 `/avatars/a1-12.svg` + 「无」=首字母色块，无外网依赖）；选择后 `set_avatar` 持久化到用户库（database.js avatar 字段）并广播，对手也能看到；座位渲染图片，加载失败回退首字母
- [x] 座位圆形头像 + 名字哈希配色 + 环形倒计时（绕头像，`actionTotalMs` 驱动进度）
- [x] 下注筹码移到玩家与底池之间（不再压名字）
- [x] 网络延迟显示（`latency_ping/pong`，右上角 📶 Xms，按延迟变色）
- [x] 公共牌固定 5 槽位（未发的占位空槽，发牌不跳动）
- [ ] 头像上传（自定义图片）；deploy 脚本已含 `avatars` 目录
- [ ] 其余待办：发牌瞬间防误触、横屏布局、座位绝对定位环绕（多人桌铺垫）

### 布局/交互精简（续）
- [x] **快捷下注最多 5 个**，防止按钮过多遮挡牌面
- [x] **翻前/翻后独立快捷下注**：翻前按 BB 倍数（2/2.5/3…，amount = N×BB），翻后按底池 %（因翻前底池太小，% 几乎都被 clamp 到最小，无意义）
- [x] **快捷下注支持自定义输入**：设置面板可输入任意 BB 倍数 / 百分比添加（各上限 5 个）
- [x] **进房间后顶栏只留 全屏 + 设置**（隐藏用户名/金币/退出登录，`body.in-room .lobby-only`）
- [x] **all-in 先亮牌再发牌**：双方全押时先 `allin_reveal` 翻开双方底牌，再逐街跑马（验证亮牌时公共牌=0）
- [x] **修复退出后无法从列表重进**：`room_list` 个性化加 `isMember`，本房成员显示绿色「重新进入」可重连（之前进行中房间按钮变灰只能输房号）

### 布局精简（向商业软件对齐）
- [x] **房间信息改为桌面中央淡色水印**（房名/房间号/级别/盲注/距升盲），不再占顶栏；顶栏瘦成一行按钮 → 回收顶部约 1/3 空间
- [x] **对手座位紧凑化**：牌背缩到 78%、padding 减小，占一小格
- [x] **整屏自适应用 `100dvh`**（而非 vh）扣除手机浏览器工具栏高度；新增「⛶ 全屏」按钮彻底隐藏工具栏（iOS Safari 不支持网页全屏 → 提示加到主屏）
- [x] **下注交互精简**：快捷池比例按钮一键直接下注/加注（按钮上显示对应金额）；滑条+输入框默认收起，点「下注…/加注…」才展开精调面板（滑条+输入+确认）
- [ ] 进一步：按住展开（long-press）、对手筹码以桌面筹码形式呈现、加分环形按钮布局

### 大厅/UI 修复
- [x] 修复横向滚动条（html/body overflow-x hidden）与「加入」按钮超框（输入框 min-width:0、按钮 flex-shrink:0）
- [x] 修复 user-bar 按钮被 `margin-left:auto` 推出屏幕（改 .gold margin-right:auto）
- [x] **去掉「单位」按钮**：改为点击自己座位的筹码数切换 筹码/BB 显示（设置面板的显示BB开关仍在）
- [x] ~~报名费固定 50~~ → **SNG 报名费档位可选** 110/220/550/1100（`SNG_BUYIN_TIERS`），冠军实得 = 奖池×10/11（抽 1/11≈9%）：2 人 200/400/1000/2000
- [x] **进房间显示房间号**：牌桌顶栏「房名 · 房间号 XXXXXX · 级别 · 盲注」，方便邀请好友

### 体验完善（本批）
- [x] **看后续牌（rabbit hunt）**：弃牌结束的局间，任一玩家点「🐰 看翻牌/转牌/河牌」逐步发出剩余公共牌仅供观看（`rabbit_deal`），不影响结果，重置局间倒计时
- [x] **all-in 跑马**：双方全押后自动逐街发公共牌（每街间隔 `RUNOUT_DELAY` 1.4s 供观看），无需点击；验证 0→3→4→5→showdown 分步
- [x] **涨盲等待本局结束**：涨盲时间到时若牌局进行中，挂起（topbar 显示「⏫本局后升盲」），本局结束后再升盲并重启倒计时（`pendingLevelUp`/`applyPendingLevelUp`）
- [x] **整屏自适应**：flex 列布局填满视口不滚动；牌面尺寸 `clamp(30px, min(11vw,7.2vh), 46px)` 随屏幕宽/高较小者连续缩放，适配不同手机；窄屏/矮屏 media query 压缩间距

### SNG 升盲与房间规则（本批）
- [x] **盲注表 25/50 起**，SB=BB/2，每级 BB ×1.3~1.5 取整（行业标准结构，见 `STANDARD_BLIND_LEVELS`）；默认初始记分牌 10000 = 200BB
- [x] **创建表单显示 BB 数**：初始记分牌滑块实时显示「10,000 (200BB)」
- [x] **退出/解散规则**：开赛前退出=退报名费+移除座位（不结算判赢）；开赛后退出=离桌挂机（保留座位、盲注照扣、可重连、超时快速弃牌直至自然淘汰）；房主有「解散房间」按钮（仅开赛后），结束比赛、奖池给当前筹码领先者
- [x] 修复 bug：开赛前一方退出会错误地立即判另一方夺冠（已改为只在 showdown 结算时判定淘汰）
